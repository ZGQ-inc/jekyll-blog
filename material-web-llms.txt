### Install Material Web Components with npm

Source: https://github.com/material-components/material-web/blob/main/docs/quick-start.md

Install the Material Web components library using npm. This is the recommended approach for production environments.

```shell
npm install @material/web
```

--------------------------------

### Divider Usage Examples

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Examples demonstrating how to use the Divider component in various configurations.

```APIDOC
## Divider Usage

### Full Width Dividers
Use full width dividers to separate larger sections of unrelated content.

```html
<section>
  <p>Lorem ipsum...</p>
  <md-divider></md-divider>
  <p>Lorem ipsum...</p>
</section>
```

### Inset Dividers
Use inset dividers to separate related content within a section.

```html
<section>
  <p>Material 2</p>
  <md-divider inset></md-divider>
  <p>Material 3</p>
</section>
```

### Leading Inset Dividers
Use `inset-start` to indent the divider from the leading side.

```html
<section>
  <p>Material 2</p>
  <md-divider inset-start></md-divider>
  <p>Material 3</p>
</section>
```

### Trailing Inset Dividers
Use `inset-end` to indent the divider from the trailing side.

```html
<section>
  <p>Material 2</p>
  <md-divider inset-end></md-divider>
  <p>Material 3</p>
</section>
```
```

--------------------------------

### Filled Button Theming Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Demonstrates how to apply custom theme tokens to a filled button for a unique appearance. This example sets shape, font, and primary colors.

```html
<style>
:root {
  --md-filled-button-container-shape: 0px;
  --md-filled-button-label-text-font: system-ui;
  --md-sys-color-primary: #006A6A;
  --md-sys-color-on-primary: #FFFFFF;
}
</style>

<md-filled-button>Filled</md-filled-button>
```

--------------------------------

### Outlined Button Theming Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Illustrates theming an outlined button with custom styles. This example adjusts the button's shape, font, primary color, and outline color.

```html
<style>
:root {
  --md-outlined-button-container-shape: 0px;
  --md-outlined-button-label-text-font: system-ui;
  --md-sys-color-primary: #006A6A;
  --md-sys-color-outline: #6F7979;
}
</style>

<md-outlined-button>Outlined</md-outlined-button>
```

--------------------------------

### List Items with Start Slot Icons

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Shows how to add icons to the start of list items using the `start` slot. This is useful for visual cues or actions associated with each list item. Supports SVG or `<md-icon>` elements.

```html
<md-list style="max-width: 300px">
  <md-list-item>
    Lit
    <svg  slot="start" style="height: 24px" viewBox="0 0 160 200">
      <path
        fill="currentColor"
        d="m160 80v80l-40-40zm-40 40v80l40-40zm0-80v80l-40-40zm-40 40v80l40-40zm-40-40v80l40-40zm40-40v80l40-40zm-40 120v80l-40-40zm-40-40v80l40-40z"
      />
    </svg>
  </md-list-item>
  <md-divider></md-divider>
  <md-list-item>
    Polymer
    <md-icon slot="start">polymer</md-icon>
  </md-list-item>
  <md-divider></md-divider>
  <md-list-item>
    Angular
    <svg slot="start" style="height: 24px" viewBox="0 0 250 250">
      <polygon points="108,135.4 125,135.4 125,135.4 125,135.4 142,135.4 125,94.5   " />
      <path
        d="M125,30L125,30L125,30L31.9,63.2l14.2,123.1L125,230l0,0l0,0l78.9-43.7l14.2-123.1L125,30z M183.1,182.6h-21.7h0
          l-11.7-29.2H125h0h0h-24.7l-11.7,29.2h0H66.9h0L125,52.1l0,0l0,0l0,0l0,0L183.1,182.6L183.1,182.6z"
      />
    </svg>
  </md-list-item>
</md-list>
```

--------------------------------

### Themed Menu Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

This example showcases how to theme a menu component using CSS custom properties. It customizes the menu's container color and shape, as well as individual menu item styles, to match a specific design aesthetic.

```html
<style>
  :root {
    background-color: #f4fbfa;
    --md-menu-container-color: #f4fbfa;
    --md-menu-container-shape: 0px;
    --md-sys-color-on-surface: #161d1d;
    --md-sys-typescale-body-large-font: system-ui;
  }
  md-menu-item {
    border-radius: 28px;
  }
  md-menu-item::part(focus-ring) {
    border-radius: 28px;
  }
  /* Styles for button and not relevant to menu */
  md-filled-button {
    --md-sys-color-primary: #006a6a;
    --md-sys-color-on-primary: #ffffff;
  }
</style>

<span style="position: relative">
  <md-filled-button id="theming-anchor">Themed menu</md-filled-button>
  <md-menu id="theming-menu" anchor="theming-anchor">
    <md-menu-item>
      <div slot="headline">Apple</div>
    </md-menu-item>
    <md-menu-item>
      <div slot="headline">Banana</div>
    </md-menu-item>
    <md-menu-item>
      <div slot="headline">Cucumber</div>
    </md-menu-item>
  </md-menu>
</span>

<script type="module">
  const anchorEl = document.body.querySelector("#theming-anchor");
  const menuEl = document.body.querySelector("#theming-menu");

  anchorEl.addEventListener("click", () => {
    menuEl.show();
  });
</script>
```

--------------------------------

### Filled Tonal Button Theming Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Shows how to theme a filled tonal button using custom tokens. This example customizes the button's shape, font, and container/on-container colors.

```html
<style>
:root {
  --md-filled-tonal-button-container-shape: 0px;
  --md-filled-tonal-button-label-text-font: system-ui;
  --md-sys-color-secondary-container: #CCE8E7;
  --md-sys-color-on-secondary-container: #051F1F;
}
</style>

<md-filled-tonal-button>Tonal</md-filled-tonal-button>
```

--------------------------------

### Install Rollup and Node Resolve Plugin

Source: https://github.com/material-components/material-web/blob/main/docs/quick-start.md

Install Rollup and the `@rollup/plugin-node-resolve` plugin to bundle your JavaScript code and resolve bare module specifiers. This is a common setup for production builds.

```shell
npm install rollup @rollup/plugin-node-resolve
```

--------------------------------

### Unbounded Ripple Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/ripple.md

An example demonstrating an unbounded ripple within a button. Ensure the container and ripple elements have the correct styles and positioning.

```html
<style>
  .container {
    border-radius: 50%;
    height: 32px;
    width: 32px;

    /* Needed for unbounded ripple */
    display: flex;
    place-content: center;
    place-items: center;
    position: relative;
  }

  md-ripple.unbounded {
    /* Needed for unbounded ripple */
    border-radius: 50%;
    inset: unset;
    height: 64px;
    width: 64px;
  }
</style>
<button class="container">
  <md-ripple class="unbounded"></md-ripple>
</button>
```

--------------------------------

### Themed Image List Item Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Demonstrates how to apply custom themes to an image list item, affecting height, width, and shape. This example uses custom CSS properties to override default token values.

```html
<style>
  :root {
    background-color: #f4fbfa;
    --md-list-container-color: #f4fbfa;
    --md-list-item-label-text-color: #161d1d;
    --md-list-item-leading-image-height: 50px;
    --md-list-item-leading-image-width: 100px;
    --md-list-item-leading-image-shape: 25px;
  }
  img {
    width: 40px;
  }
</style>
<md-list>
  <md-list-item headline="Wide Cat">
    <img slot="start" src="https://placekitten.com/200/100">
  </md-list-item>
  <md-list-item headline="Round kitty cat">
    <img slot="start" src="https://placekitten.com/202/101">
  </md-list-item>
  <md-list-item headline="Softe cate">
    <img slot="start" src="https://placekitten.com/212/106">
  </md-list-item>
</md-list>
```

--------------------------------

### Basic Slider Examples

Source: https://github.com/material-components/material-web/blob/main/docs/components/slider.md

Demonstrates the basic usage of the md-slider component for continuous, discrete with ticks, and range selection.

```html
<md-slider></md-slider>
<md-slider ticks value="50"></md-slider>
<md-slider range value-start="25" value-end="75"></md-slider>
```

--------------------------------

### Themed Slider Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/slider.md

Demonstrates how to apply custom theming to the slider component by overriding token values.

```html
<style>
:root {
  /* System tokens */
  --md-sys-color-primary: #006A6A;

  /* Component tokens */
  --md-slider-handle-shape: 0px;
}
</style>

<md-slider
  range
  value-start="25"
  value-end="75"
  ticks
  step="5"
></md-slider>
```

--------------------------------

### Text Button Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Renders a text button. Use for the lowest priority actions, especially when presenting multiple options.

```html
<md-text-button>Text</md-text-button>
```

--------------------------------

### List Items with Images

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Demonstrates how to display images within list items using the 'start' slot. Each image is styled to a fixed width.

```html
<md-list style="max-width: 300px;">
  <md-list-item>
    Cat
    <img slot="start" style="width: 56px" src="https://placekitten.com/112/112">
  </md-list-item>
  <md-divider></md-divider>
  <md-list-item>
    Kitty Cat
    <img slot="start" style="width: 56px" src="https://placekitten.com/114/114">
  </md-list-item>
  <md-divider></md-divider>
  <md-list-item>
    Cate
    <img slot="start" style="width: 56px" src="https://placekitten.com/116/116">
  </md-list-item>
</md-list>
```

--------------------------------

### Outlined Button Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Renders a standard outlined button. Use for medium-emphasis actions.

```html
<md-outlined-button>Outlined</md-outlined-button>
```

--------------------------------

### Chip Set with Actions

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Shows how to group chips within a chip set for a new event interface. This example includes filter and assist chips for event settings.

```html
<h3>New event</h3>
<md-chip-set>
  <md-filter-chip label="All day"></md-filter-chip>
  <md-assist-chip label="Add to calendar"></md-assist-chip>
  <md-assist-chip label="Set a reminder"></md-assist-chip>
</md-chip-set>
```

--------------------------------

### Accessibility Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Ensure accessibility by providing an aria-label for the select component.

```html
<component-name aria-label="Example">

```

--------------------------------

### Popover-positioned Menu Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Use popover positioning for menus that need to render above all other content, especially when a common relative parent with the anchor is not feasible. This example demonstrates setting `positioning="popover"` and includes basic JavaScript for opening/closing the menu.

```html
<!-- Note the lack of position: relative parent. -->
<div style="margin: 16px;">
  <md-filled-button id="usage-popover-anchor">Open popover menu</md-filled-button>
</div>

<!-- popover menus do not require a common ancestor with the anchor. -->
<md-menu positioning="popover" id="usage-popover" anchor="usage-popover-anchor">
  <md-menu-item>
    <div slot="headline">Apple</div>
  </md-menu-item>
  <md-menu-item>
    <div slot="headline">Banana</div>
  </md-menu-item>
  <md-menu-item>
    <div slot="headline">Cucumber</div>
  </md-menu-item>
</md-menu>

<script type="module">
  const anchorEl = document.body.querySelector('#usage-popover-anchor');
  const menuEl = document.body.querySelector('#usage-popover');

  anchorEl.addEventListener('click', () => { menuEl.open = !menuEl.open; });
</script>
```

--------------------------------

### Assist Chip Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Assist chips represent automated actions. Use them to trigger smart actions that span multiple apps, appearing dynamically and contextually.

```html
<h3>A restaraunt location</h3>
<md-chip-set>
  <md-assist-chip label="Add to my itinerary">
    <md-icon slot="icon">calendar</md-icon>
  </md-assist-chip>
  <md-assist-chip label="12 mins from hotel">
    <md-icon slot="icon">map</md-icon>
  </md-assist-chip>
</md-chip-set>
```

--------------------------------

### CDN Setup for Material Web Components

Source: https://github.com/material-components/material-web/blob/main/docs/quick-start.md

Use this HTML snippet to quickly prototype with Material Web components via a CDN. It includes font imports, an import map for resolving module specifiers, and basic component usage.

```html
<head>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <script type="importmap">
    {
      "imports": {
        "@material/web/": "https://esm.run/@material/web/"
      }
    }
  </script>
  <script type="module">
    import '@material/web/all.js';
    import {styles as typescaleStyles} from '@material/web/typography/md-typescale-styles.js';

    document.adoptedStyleSheets.push(typescaleStyles.styleSheet);
  </script>
</head>
<body>
  <h1 class="md-typescale-display-medium">Hello Material!</h1>
  <form>
    <p class="md-typescale-body-medium">Check out these controls in a form!</p>
    <md-checkbox></md-checkbox>
    <div>
      <md-radio name="group"></md-radio>
      <md-radio name="group"></md-radio>
      <md-radio name="group"></md-radio>
    </div>
    <md-outlined-text-field label="Favorite color" value="Purple"></md-outlined-text-field>
    <md-outlined-button type="reset">Reset</md-outlined-button>
  </form>
  <style>
    form {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
    }
  </style>
</body>
```

--------------------------------

### Import Material Web Components in JavaScript

Source: https://github.com/material-components/material-web/blob/main/docs/quick-start.md

Import individual component definitions from the installed `@material/web` package. Ensure you import only the components you need to optimize bundle size.

```javascript
// index.js
import '@material/web/button/filled-button.js';
import '@material/web/button/outlined-button.js';
import '@material/web/checkbox/checkbox.js';
```

--------------------------------

### Check if string starts with prefix using `string_ext.starts-with`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.starts-with` to determine if a string begins with a specified prefix. This is useful for pattern matching or validating string formats.

```scss
@debug string_ext.starts-with('var(--foo)', 'var('); // true
```

--------------------------------

### Theming a Switch Component

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Customize the appearance of the switch component by overriding its design tokens. This example demonstrates setting system and component tokens for color and shape.

```html
<style>
  :root {
    /* System tokens */
    --md-sys-color-primary: #006a6a;
    --md-sys-color-on-primary: #ffffff;
    --md-sys-color-outline: #6f7979;
    --md-sys-color-surface-container-highest: #dde4e3;

    /* Component tokens */
    --md-switch-handle-shape: 0px;
    --md-switch-track-shape: 0px;
  }
</style>

<md-switch></md-switch>
<md-switch selected></md-switch>
```

--------------------------------

### Set Body Medium Typescale Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/theming/typography.md

Example of setting the size and line-height for the body-medium typescale using CSS custom properties.

```css
:root {
  --md-sys-typescale-body-medium-size: 1rem;
  --md-sys-typescale-body-medium-line-height: 1.5rem;
  /* ... */
}
```

--------------------------------

### Customize FAB Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Apply custom theme tokens to a FAB to change its appearance. This example demonstrates overriding container color, icon color, shape, and icon size.

```html
<style>
  :root {
    --md-sys-color-surface-container-high: #e3e9e9;
    --md-sys-color-primary: #006a6a;
    --md-fab-container-shape: 0px;
    --md-fab-icon-size: 36px;
    background-color: #f4fbfa;
  }
</style>
<md-fab aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### Outlined Select Theming Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Customize the appearance of an outlined select component and its dropdown menu using CSS custom properties.

```html
<style>
:root {
  --md-outlined-select-text-field-outline-color: #6e7979;
  --md-outlined-select-text-field-container-shape: 0px;
  --md-outlined-select-text-field-input-text-color: #005353;
  --md-outlined-select-text-field-input-text-font: system-ui;
}

md-outlined-select::part(menu) {
  --md-menu-container-color: #f4fbfa;
  --md-menu-container-shape: 0px;
}
</style>

<md-outlined-select>
  <md-select-option selected value="apple">
    <div slot="headline">Apple</div>
  </md-select-option>
  <md-select-option value="tomato">
    <div slot="headline">Tomato</div>
  </md-select-option>
</md-outlined-select>

```

--------------------------------

### Theming an Assist Chip

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Customize the appearance of an assist chip by overriding CSS custom properties. This example sets the container shape, label text font, and colors.

```html
<style>
:root {
  --md-assist-chip-container-shape: 0px;
  --md-assist-chip-label-text-font: system-ui;
  --md-sys-color-outline: #6F7979;
  --md-sys-color-on-surface: #191C1C;
}
</style>

<md-assist-chip label="Assist"></md-assist-chip>
```

--------------------------------

### Filled Select Theming Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Customize the appearance of a filled select component and its dropdown menu using CSS custom properties.

```html
<style>
:root {
  --md-filled-select-text-field-container-shape: 0px;
  --md-filled-select-text-field-container-color: #f7faf9;
  --md-filled-select-text-field-input-text-color: #005353;
  --md-filled-select-text-field-input-text-font: system-ui;
}

md-filled-select::part(menu) {
  --md-menu-container-color: #f4fbfa;
  --md-menu-container-shape: 0px;
}
</style>

<md-filled-select>
  <md-select-option selected value="apple">
    <div slot="headline">Apple</div>
  </md-select-option>
  <md-select-option value="tomato">
    <div slot="headline">Tomato</div>
  </md-select-option>
</md-filled-select>

```

--------------------------------

### Animate Elevation Changes

Source: https://github.com/material-components/material-web/blob/main/docs/components/elevation.md

Animate elevation changes by applying `transition-duration` and `transition-timing-function` to the surface. The example shows how to change elevation on `:active` state.

```html
<style>
  .surface {
    /* ... */
    transition-duration: 250ms;
    transition-timing-function: ease-in-out;

    --md-elevation-level: 3;
  }

  .surface:active {
    --md-elevation-level: 0;
  }
</style>
<div class="surface">
  <md-elevation></md-elevation>
  <!-- Content -->
</div>
```

--------------------------------

### Theming an Input Chip

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Customize the appearance of an input chip by overriding CSS custom properties. This example sets the container shape, label text font, and colors.

```html
<style>
:root {
  --md-input-chip-container-shape: 0px;
  --md-input-chip-label-text-font: system-ui;
  --md-sys-color-outline: #6f7979;
  --md-sys-color-on-surface: #191c1c;
}
</style>

<md-input-chip label="Input"></md-input-chip>
```

--------------------------------

### Theming a Suggestion Chip

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Customize the appearance of a suggestion chip by overriding CSS custom properties. This example sets the container shape, label text font, and colors.

```html
<style>
:root {
  --md-suggestion-chip-container-shape: 0px;
  --md-suggestion-chip-label-text-font: system-ui;
  --md-sys-color-outline: #6f7979;
  --md-sys-color-on-surface: #191c1c;
}
</style>

<md-suggestion-chip label="Suggestion"></md-suggestion-chip>
```

--------------------------------

### Button Types Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Demonstrates the five common types of Material Web buttons: elevated, filled, filled tonal, outlined, and text. Use these buttons to initiate actions in your application.

```html
<md-elevated-button has-icon>
  <md-icon slot="icon">add</md-icon>
  Elevated
</md-elevated-button>
<md-filled-button>Filled</md-filled-button>
<md-filled-tonal-button>Tonal</md-filled-tonal-button>
<md-outlined-button>Outlined</md-outlined-button>
<md-text-button>Text</md-text-button>
```

--------------------------------

### Basic Usage of Material Web Components in HTML

Source: https://github.com/material-components/material-web/blob/main/docs/quick-start.md

Use the imported Material Web components as custom HTML elements. This example demonstrates using a checkbox and buttons after importing them in your JavaScript module.

```html
<script type="module" src="./index.js"></script>

<label>
  Material 3
  <md-checkbox checked></md-checkbox>
</label>

<md-outlined-button>Back</md-outlined-button>
<md-filled-button>Next</md-filled-button>
```

--------------------------------

### Replace prefix at start of string with `string_ext.replace-start`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.replace-start` to replace a specific prefix at the beginning of a string with a new value. This is useful for modifying string beginnings, like URL paths or variable names.

```scss
@debug string_ext.replace-start('var(--foo)', 'var(', ''); // "--foo)"
```

--------------------------------

### Radio Theming Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/radio.md

Customizes the primary color and icon size for radio buttons using CSS custom properties. This snippet shows how to apply theming tokens.

```html
<style>
:root {
  --md-sys-color-primary: #006A6A;
  --md-radio-icon-size: 16px;
}
</style>

<md-radio name="group"></md-radio>
<md-radio name="group" checked></md-radio>
<md-radio name="group"></md-radio>
```

--------------------------------

### Manual Validation Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Manually control the error state and message for text fields, useful when driven by application state logic.

```html
<md-outlined-text-field
    label="Username"
    value="jdoe"
    error
    error-text="Username is not available">
</md-outlined-text-field>
```

--------------------------------

### Theming a Basic List

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Customize the appearance of a basic list and its items by overriding CSS custom properties. This example sets custom colors and fonts for the list container, labels, and supporting text.

```html
<style>
  :root {
    --md-list-container-color: #f4fbfa;
    --md-list-item-label-text-color: #161d1d;
    --md-list-item-supporting-text-color: #3f4948;
    --md-list-item-trailing-supporting-text-color: #3f4948;
    --md-list-item-label-text-font: system-ui;
    --md-list-item-supporting-text-font: system-ui;
    --md-list-item-trailing-supporting-text-font: system-ui;
  }
  [slot="trailing-supporting-text"] {
    width: 30px;
    text-align: end;
  }
</style>
<md-list>
  <md-list-item type="button">
    <div slot="headline">Apple</div>
    <div slot="supporting-text">In stock</div>
    <div slot="trailing-supporting-text">+100</div>
  </md-list-item>
  <md-list-item type="button">
    <div slot="headline">Banana</div>
    <div slot="supporting-text">In stock</div>
    <div slot="trailing-supporting-text">56</div>
  </md-list-item>
  <md-list-item type="button">
    <div slot="headline">Cucumber</div>
    <div slot="supporting-text">Low stock</div>
    <div slot="trailing-supporting-text">5</div>
  </md-list-item>
</md-list>
```

--------------------------------

### Themed Elevated Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Demonstrates theming an elevated button by overriding CSS custom properties. This example customizes shape, font, and colors.

```html
<style>
:root {
  --md-elevated-button-container-shape: 0px;
  --md-elevated-button-label-text-font: system-ui;
  --md-sys-color-surface-container-low: #FAFDFC;
  --md-sys-color-primary: #191C1C;
}
</style>

<md-elevated-button>Elevated</md-elevated-button>
```

--------------------------------

### Customize Branded FAB Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Apply custom theme tokens to a branded FAB. This example overrides container color, shape, icon size, and label text font, and demonstrates both regular and extended branded FABs.

```html
<style>
  :root {
    --md-sys-color-surface-container-high: #e3e9e9;
    --md-sys-color-on-surface: #161d1d;
    --md-fab-branded-icon-size: 48px;
    --md-fab-branded-container-shape: 0px;
    background-color: #f4fbfa;
  }
</style>
<md-branded-fab size="small" aria-label="Add">
  <svg slot="icon" viewBox="0 0 36 36">
    <path fill="#34A853" d="M16 16v14h4V20z"></path>
    <path fill="#4285F4" d="M30 16H20l-4 4h14z"></path>
    <path fill="#FBBC05" d="M6 16v4h10l4-4z"></path>
    <path fill="#EA4335" d="M20 16V6h-4v14z"></path>
    <path fill="none" d="M0 0h36v36H0z"></path>
  </svg>
</md-branded-fab>
<md-branded-fab size="small" label="Add">
  <svg slot="icon" viewBox="0 0 36 36">
    <path fill="#34A853" d="M16 16v14h4V20z"></path>
    <path fill="#4285F4" d="M30 16H20l-4 4h14z"></path>
    <path fill="#FBBC05" d="M6 16v4h10l4-4z"></path>
    <path fill="#EA4335" d="M20 16V6h-4v14z"></path>
    <path fill="none" d="M0 0h36v36H0z"></path>
  </svg>
</md-branded-fab>
```

--------------------------------

### Theming Secondary Tabs

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Customize the appearance of secondary tabs using CSS custom properties. This example applies a custom font and size to the tab labels.

```html
<style>
:root {
  /* System tokens */
  --md-sys-color-surface: #f7faf9;
  --md-sys-color-primary: #005353;

  /* Component tokens */
  --md-secondary-tab-label-text-font: cursive, system-ui;
  --md-secondary-tab-label-text-size: 0.8em;
}
</style>

<md-tabs>
  <md-secondary-tab>Tab 1</md-secondary-tab>
  <md-secondary-tab>Tab 2</md-secondary-tab>
  <md-secondary-tab>Tab 3</md-secondary-tab>
</md-tabs>
```

--------------------------------

### Outlined Icon Button with Check Icon

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Example of an outlined icon button containing a check icon. This is a basic usage pattern.

```html
<md-outlined-icon-button>
  <md-icon>check</md-icon>
</md-outlined-icon-button>
```

--------------------------------

### Theming Divider with CSS Custom Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Customize the divider's appearance using CSS custom properties. This example changes the color and thickness of the divider.

```html
<style>
:root {
  --md-sys-color-outline-variant: #BEC9C8;
  --md-divider-thickness: 2px;
}
</style>

<section>
  <p>Lorem ipsum...</p>
  <md-divider></md-divider>
  <p>Lorem ipsum...</p>
</section>
```

--------------------------------

### Avoid Sass Ext for Simple Type Checks

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Shows an example where using `sass/ext/type` for a simple null check does not reduce boilerplate and adds an unnecessary API dependency. Prefer built-in Sass for such cases.

```scss
// Using sass/ext here does not reduce boilerplate, and adds another API to
// learn and maintain.
@use '@material/web/sass/ext/type';

@function value-or-default($value, $default) {
  @if type.matches($value, 'null') {
    @return $default;
  }
  @return $value;
}
```

--------------------------------

### Define System Color Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/theming/README.md

Define `--md-sys-color` tokens to establish dynamic color roles that map to components. Refer to the color guide for detailed token information.

```css
:root {
  --md-sys-color-primary: red;
  --md-sys-color-secondary: blue;
}
```

--------------------------------

### Filter Chip Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Filter chips use tags or descriptive words to filter content. They can be used as an alternative to toggle buttons or checkboxes.

```html
<h3>Choose where to share</h3>
<md-chip-set>
  <md-filter-chip label="Docs"></md-filter-chip>
  <md-filter-chip label="Slides" selected></md-filter-chip>
  <md-filter-chip label="Sheets" selected></md-filter-chip>
  <md-filter-chip label="Images"></md-filter-chip>
</md-chip-set>
```

--------------------------------

### Basic List with Various Item Types

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Demonstrates how to create a list with different types of list items, including simple text, headlines with supporting text, and link items. Use this for general-purpose lists.

```html
<md-list style="max-width: 300px;">
  <md-list-item>
    Fruits
  </md-list-item>
  <md-divider></md-divider>
  <md-list-item>
    Apple
</md-list-item>
  <md-list-item>
    <div slot="headline">Cucumber</div>
    <div slot="supporting-text">Cucumbers are long green fruits that are just as long as this multi-line description</div>
  </md-list-item>
  <md-list-item
      type="link"
      href="https://google.com/search?q=buy+kiwis&tbm=shop"
      target="_blank">
    <div slot="headline">Shop for Kiwis</div>
    <div slot="supporting-text">This will link you out in a new tab</div>
    <md-icon slot="end">open_in_new</md-icon>
  </md-list-item>
</md-list>
```

--------------------------------

### Menu with ARIA roles for Combobox

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

This example demonstrates a menu used in an autocomplete scenario. It configures the menu and menu items with appropriate ARIA roles and attributes for accessibility, such as `role="listbox"` for the menu and `type="option"` for menu items.

```html
<!--
  A simplified example of an autocomplete component – missing javascript logic for interaction.
-->
<md-filled-text-field
    id="textfield"
    type="combobox"
    aria-controls="menu"
    aria-autocomplete="list"
    aria-expanded="true"
    aria-activedescendant="1"
    value="Ala">
</md-filled-text-field>
<md-menu
    id="menu"
    anchor="textfield"
    role="listbox"
    aria-label="states"
    open>
  <md-menu-item type="option" id="0">
    <div slot="headline">Alabama</div>
  </md-menu-item>
  <md-divider role="separator" tabindex="-1"></md-divider>
  <md-menu-item type="option" id="1" selected aria-selected="true">
    <div slot="headline">Alabama</div>
  </md-menu-item>
</md-menu>
```

--------------------------------

### Theme Text Button with CSS Variables

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Apply custom theming to a text button by overriding CSS variables. This example sets the font to system-ui and the primary color to a teal shade.

```html
<style>
:root {
  --md-text-button-label-text-font: system-ui;
  --md-sys-color-primary: #006A6A;
}
</style>

<md-text-button>Text</md-text-button>
```

--------------------------------

### Define System Typography Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/theming/README.md

Define `--md-sys-typescale` tokens to establish typescale roles that map to components. Refer to the typography guide for detailed token information.

```css
:root {
  --md-sys-typescale-body-medium-size: 1rem;
  --md-sys-typescale-body-medium-line-height: 1.5rem;
}
```

--------------------------------

### Customizing Component Styles with CSS

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/fab/theming-fab.html

Apply custom styles to Material Web Components by defining CSS custom properties. This example shows how to override default colors and shapes for a FAB component.

```css
.styled-example {
  --md-sys-color-surface-container-high: #e3e9e9;
  --md-sys-color-primary: #006a6a;
  background-color: #f4fbfa;
}

.styled-example.fab {
  --md-fab-icon-size: 36px;
  --md-fab-container-shape: 0px;
}
```

--------------------------------

### Theming an Icon List Item

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Customize the appearance of list items that include leading and trailing icons. This example applies custom colors and sizes to icons, and demonstrates conditional styling for an 'unhealthy' item.

```html
<style>
  :root {
    background-color: #f4fbfa;
    --md-list-container-color: #f4fbfa;
    --md-list-item-label-text-color: #161d1d;
    --md-list-item-leading-icon-color: #006a6a;
    --md-list-item-trailing-icon-color: #006a6a;
    --md-list-item-leading-icon-size: 20px;
    --md-list-item-trailing-icon-size: 20px;
  }
  md-list-item.unhealthy {
    --md-list-item-trailing-icon-color: #ba1a1a;
  }
</style>
<md-list>
  <md-list-item>
    <div slot="headline">Eggs</div>
    <md-icon slot="start">egg</md-icon>
    <md-icon slot="end">recommend</md-icon>
  </md-list-item>
  <md-list-item class="unhealthy">
    <div slot="headline">Ice Cream</div>
    <md-icon slot="start">icecream</md-icon>
    <md-icon slot="end">dangerous</md-icon>
  </md-list-item>
  <md-list-item>
    <div slot="headline">Orange</div>
    <md-icon slot="start">nutrition</md-icon>
    <md-icon slot="end">recommend</md-icon>
  </md-list-item>
</md-list>
```

--------------------------------

### Switch with Icons

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Shows how to configure switches to display icons, either for both states or only the selected state.

```html
<md-switch icons></md-switch>
<md-switch icons selected></md-switch>

<md-switch icons show-only-selected-icon></md-switch>
<md-switch icons show-only-selected-icon selected></md-switch>
```

--------------------------------

### Theming Primary Tabs

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Customize the appearance of primary tabs by overriding CSS custom properties. This example sets a custom font and font size for tab labels.

```html
<style>
:root {
  /* System tokens */
  --md-sys-color-surface: #f7faf9;
  --md-sys-color-primary: #005353;

  /* Component tokens */
  --md-primary-tab-label-text-font: cursive, system-ui;
  --md-primary-tab-label-text-size: 0.8em;
}
</style>

<md-tabs>
  <md-primary-tab>Tab 1</md-primary-tab>
  <md-primary-tab>Tab 2</md-primary-tab>
  <md-primary-tab>Tab 3</md-primary-tab>
</md-tabs>
```

--------------------------------

### Apply Custom Styles to Material Components

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/iconbutton/theming-standard.html

Use CSS to apply custom styles to Material Components. This example shows how to set a background color and override icon button sizes and colors.

```css
.styled-example { background-color: white; }
.styled-example.red { background-color: #fff8f6; }
.styled-example.standard { --md-icon-button-icon-size: 32px; --md-sys-color-on-surface-variant: #dc362e; }
```

--------------------------------

### Define System Shape Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/theming/README.md

Define `--md-sys-shape` tokens to establish corner shapes used in components. Refer to the shape guide for detailed token information.

```css
:root {
  --md-sys-shape-corner-small: 4px;
  --md-sys-shape-corner-medium: 6px;
  --md-sys-shape-corner-large: 8px;
}
```

--------------------------------

### Basic Select Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Demonstrates the basic usage of both outlined and filled select components with options. Includes a blank option for accessibility.

```html
<md-outlined-select>
  <md-select-option aria-label="blank"></md-select-option>
  <md-select-option selected value="apple">
    <div slot="headline">Apple</div>
  </md-select-option>
  <md-select-option value="apricot">
    <div slot="headline">Apricot</div>
  </md-select-option>
</md-outlined-select>

<md-filled-select>
  <md-select-option aria-label="blank"></md-select-option>
  <md-select-option value="apple">
    <div slot="headline">Apple</div>
  </md-select-option>
  <md-select-option value="apricot">
    <div slot="headline">Apricot</div>
  </md-select-option>
</md-filled-select>
```

--------------------------------

### Define Color Scheme with CSS Custom Properties

Source: https://github.com/material-components/material-web/blob/main/docs/theming/color.md

Define a color scheme using CSS custom properties, following the --md-sys-color-<token> naming convention. This example shows primary and on-primary tokens, and how to apply them in custom components.

```css
:root {
  /* Generated from Material Theme Builder Figma plugin
     or `material-color-utilities`. */
  --md-sys-color-primary: #006A6A;
  --md-sys-color-on-primary: #FFFFFF;
  --md-sys-color-primary-container: #6FF7F6;
  --md-sys-color-on-primary-container: #002020;
  /* ... */
}

/* Usage in custom components */
.primary {
  background: var(--md-sys-color-primary);
  color: var(--md-sys-color-on-primary);
}
```

--------------------------------

### Removable Filter Chip Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Filter chips can be made removable by adding a trailing remove icon. Use this for options that can be deselected or removed from a set.

```html
<h3>Colors</h3>
<md-chip-set>
  <md-filter-chip label="Red" removable selected></md-filter-chip>
  <md-filter-chip label="Yellow" removable></md-filter-chip>
  <md-filter-chip label="Blue" removable></md-filter-chip>
  <md-filter-chip label="Green" removable></md-filter-chip>
</md-chip-set>
```

--------------------------------

### string_ext.starts-with

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Checks if a given string begins with a specified prefix.

```APIDOC
## `string_ext.starts-with`

### Description
Checks if a string starts with a given prefix.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to test.
- **$prefix** (string) - Required - The prefix to check.

### Return Value
- **boolean** - True if the string starts with the given prefix, false otherwise.

### Example
```scss
@debug string_ext.starts-with('var(--foo)', 'var('); // true
```
```

--------------------------------

### Theme Elevation Shadow Color

Source: https://github.com/material-components/material-web/blob/main/docs/components/elevation.md

Customize the elevation's shadow color using the `--md-sys-color-shadow` token. This example sets a custom shadow color and elevation level.

```html
<style>
  .surface {
    position: relative;
    border-radius: 16px;
    height: 64px;
    width: 64px;
  }

  :root {
    --md-elevation-level: 5;
    --md-sys-color-shadow: #006A6A;
  }
</style>
<div class="surface">
  <md-elevation></md-elevation>
  <!-- Content -->
</div>
```

--------------------------------

### Customize Extended FAB Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Customize the theming of an extended FAB by overriding CSS custom properties. This example sets container color, icon color, shape, and icon size.

```html
<style>
  :root {
    --md-sys-color-surface-container-high: #e3e9e9;
    --md-sys-color-on-surface: #161d1d;
    --md-sys-color-primary: #006a6a;
    --md-fab-container-shape: 0px;
    --md-fab-icon-size: 36px;
    background-color: #f4fbfa;
  }
</style>
<md-fab label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### Theming Focus Ring

Source: https://github.com/material-components/material-web/blob/main/docs/components/focus-ring.md

Customize the focus ring's appearance using CSS custom properties for shape, width, and color. This example demonstrates overriding default token values.

```html
<style>
md-focus-ring {
  --md-focus-ring-shape: 0px;
  --md-focus-ring-width: 2px;
  --md-focus-ring-active-width: 4px;
  --md-sys-color-secondary: #4A6363;
}
</style>

<button>
  <md-focus-ring></md-focus-ring>
</button>
```

--------------------------------

### Customize Checkbox Theme with CSS Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/components/checkbox.md

Apply custom theme tokens to modify the appearance of Material Design checkboxes. This example demonstrates changing the container shape and system colors.

```html
<style>
  :root {
    /* System tokens */
    --md-sys-color-primary: #006a6a;
    --md-sys-color-on-primary: #ffffff;
    --md-sys-color-on-surface-variant: #3f4948;

    /* Component tokens */
    --md-checkbox-container-shape: 0px;
  }
</style>

<md-checkbox touch-target="wrapper"></md-checkbox>
<md-checkbox touch-target="wrapper" checked></md-checkbox>
```

--------------------------------

### Get intersection of two lists

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Returns a new list containing elements that are present in both input lists. Useful for finding common items between two collections.

```scss
list_ext.intersection($listA, $listB) //=> list

/// @param {list} $listA - The first list to compare.
/// @param {list} $listB - The second list to compare.
/// @return {list} All items in $listA that are also in $listB.
```

```scss
$listA: ('apple', 'banana', 'cherry', 'date');
$listB: ('banana', 'cherry', 'apple');
$listC: ('apple', 'banana', 'date');

@debug list_ext.intersection($listA, $listB); // ('apple', 'banana', 'cherry')
@debug list_ext.intersection($listA, $listC); // ('apple', 'banana')
```

--------------------------------

### string_ext.replace-start

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Replaces a specified prefix at the beginning of a string with a new replacement string.

```APIDOC
## `string_ext.replace-start`

### Description
Returns a new string that replaces a prefix at the start of the string.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to be searched.
- **$prefix** (string) - Required - The prefix string to replace.
- **$replacement** (string) - Required - The value to replace the prefix.

### Return Value
- **string** - The string with the prefix replaced.

### Example
```scss
@debug string_ext.replace-start('var(--foo)', 'var(', ''); // "--foo)"
```
```

--------------------------------

### Scope Component Tokens with CSS Selectors

Source: https://github.com/material-components/material-web/blob/main/docs/theming/README.md

Scope component tokens using CSS selectors to apply specific styles to elements. This example targets all `<md-filled-button>` instances within the selector.

```css
.square-buttons {
  /* Changes all <md-filled-button> instances matching the selector */
  --md-filled-button-container-shape: 0px;
}
```

--------------------------------

### Inset Divider Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Use inset dividers to separate related content within a section. This example shows a divider separating two design system names.

```html
<section>
  <p>Material 2</p>
  <md-divider inset></md-divider>
  <p>Material 3</p>
</section>
```

--------------------------------

### Accessible Icon with Codepoint and Aria-label

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

When using icons by codepoint, wrap the codepoint in a span with an `aria-label` attribute for screen reader accessibility. This example shows an accessible home icon.

```html
<md-icon tabindex="-1"><span aria-label="home">&#xe88a</span></md-icon>
```

--------------------------------

### Strictly get map value by key

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Retrieves a value from a map using a key. Throws an error if the key does not exist, ensuring that expected keys are always present.

```scss
map_ext.get-strict($map, $key, $keys)

/// @param {map} $map - The map to retrieve the value from.
/// @param {string} $key - The key of the value to retrieve.
/// @param {list} $keys - Additional keys to retrieve deeply nested values.
/// @return {*} The value at the given key.
/// @throw Error if the key does not exist in the map.
```

```scss
$map: (
  'name': 'foo',
  'value': blue,
);

@debug map_ext.get-strict($map, 'name'); // 'foo'
@debug map_ext.get-strict($map, 'bar'); // ERROR: Key "bar" expected but not found in $map: ('name': 'foo', 'value': blue)
```

--------------------------------

### Load Material Web Components from CDN

Source: https://github.com/material-components/material-web/blob/main/README.md

This HTML snippet demonstrates how to load Material Web components using a CDN and import maps. It includes basic typography styles and form elements like checkboxes, radio buttons, and text fields. Ensure the Roboto font is linked and all necessary components are imported.

```html
<head>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <script type="importmap">
    {
      "imports": {
        "@material/web/": "https://esm.run/@material/web/"
      }
    }
  </script>
  <script type="module">
    import '@material/web/all.js';
    import {styles as typescaleStyles} from '@material/web/typography/md-typescale-styles.js';

    document.adoptedStyleSheets.push(typescaleStyles.styleSheet);
  </script>
</head>
<body>
  <h1 class="md-typescale-display-medium">Hello Material!</h1>
  <form>
    <p class="md-typescale-body-medium">Check out these controls in a form!</p>
    <md-checkbox></md-checkbox>
    <div>
      <md-radio name="group"></md-radio>
      <md-radio name="group"></md-radio>
      <md-radio name="group"></md-radio>
    </div>

    <md-outlined-text-field label="Favorite color" value="Purple"></md-outlined-text-field>

    <md-outlined-button type="reset">Reset</md-outlined-button>
  </form>
  <style>
    form {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
    }
  </style>
</body>
```

--------------------------------

### Basic Full Width Divider Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Use full width dividers to separate larger sections of unrelated content. This example shows a divider separating two paragraphs.

```html
<section>
  <p>Lorem ipsum...</p>
  <md-divider></md-divider>
  <p>Lorem ipsum...</p>
</section>
```

--------------------------------

### Fixed-positioned Menu Example

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Use fixed positioning as a fallback for browsers that do not support the Popover API. This positions the menu relative to the window, meaning it will not scroll with the anchor. The `positioning="fixed"` attribute is used.

```html
<!-- Note the lack of position: relative parent. -->
<div style="margin: 16px;">
  <md-filled-button id="usage-fixed-anchor">Open fixed menu</md-filled-button>
</div>

<!-- Fixed menus do not require a common ancestor with the anchor. -->
<md-menu positioning="fixed" id="usage-fixed" anchor="usage-fixed-anchor">
  <md-menu-item>
    <div slot="headline">Apple</div>
  </md-menu-item>
  <md-menu-item>
    <div slot="headline">Banana</div>
  </md-menu-item>
  <md-menu-item>
    <div slot="headline">Cucumber</div>
  </md-menu-item>
</md-menu>

<script type="module">
  const anchorEl = document.body.querySelector('#usage-fixed-anchor');
  const menuEl = document.body.querySelector('#usage-fixed');

  anchorEl.addEventListener('click', () => { menuEl.open = !menuEl.open; });
</script>
```

--------------------------------

### Apply Branded Styles to FAB

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/fab/theming-branded.html

Use custom CSS properties to apply branded styles to a Floating Action Button (FAB). This example sets custom colors, fonts, icon sizes, and container shapes.

```css
.styled-example.branded { --md-sys-color-on-surface: #161d1d; --md-fab-branded-label-text-font: system-ui; --md-fab-branded-icon-size: 48px; --md-fab-branded-container-shape: 0px; }
```

--------------------------------

### Theming a Filter Chip

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Customize the appearance of a filter chip by overriding CSS custom properties. This example sets the container shape, label text font, and colors, including the selected container color.

```html
<style>
:root {
  --md-filter-chip-container-shape: 0px;
  --md-filter-chip-label-text-font: system-ui;
  --md-sys-color-outline: #6f7979;
  --md-sys-color-on-surface: #191c1c;
  --md-sys-color-secondary-container: #cce8e7;
}
</style>

<md-filter-chip label="Filter"></md-filter-chip>
```

--------------------------------

### Add Accessibility Labels to Tabs

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Apply `aria-label` attributes to `<md-tabs>` and individual tabs for improved accessibility, especially for icon-only tabs. This example demonstrates labels for primary tabs.

```html
<md-tabs aria-label="Content to view">
  <md-primary-tab aria-label="Photos">
    <md-icon slot="icon">photo</md-icon>
  </md-primary-tab>
  <md-primary-tab aria-label="Videos">
    <md-icon slot="icon">videocam</md-icon>
  </md-primary-tab>
  <md-primary-tab aria-label="Music">
    <md-icon slot="icon">audiotrack</md-icon>
  </md-primary-tab>
</md-tabs>
```

--------------------------------

### Accessibility with Role Separator

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Add a `role="separator"` attribute to non-decorative dividers that should be announced by assistive technology. This example shows a list with dividers, some decorative and one announced.

```html
<ul>
  <li>Item one</li>
  <md-divider inset></md-divider>
  <li>Item two</li>
  <md-divider role="separator"></md-divider>
  <li>Item three</li>
  <md-divider inset></md-divider>
  <li>Item four</li>
</ul>
```

--------------------------------

### List Theming Files

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Iterates through collections.theming and displays the 'name' property of each file. Files are sorted by 'data.order'.

```html
{% for file in collections.theming|filtersort('data.order') %} {{ file.data.name }} {% endfor %}
```

--------------------------------

### Implementing Submenus in a Menu

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

This snippet demonstrates how to create nested submenus within an md-menu. Ensure the root md-menu has the 'has-overflow' attribute set to 'true' to enable submenu display. The example also shows how to anchor submenus and use icons for visual cues.

```html
<!-- Note the position: relative style -->
<span style="position: relative">
  <md-filled-button id="usage-submenu-anchor">
    Menu with Submenus
  </md-filled-button>
  <!-- Note the has-overflow attribute -->
  <md-menu has-overflow id="usage-submenu" anchor="usage-submenu-anchor">
    <md-sub-menu>
      <md-menu-item slot="item">
      <div slot="headline">Fruits with A</div>
        <!-- Arrow icons are helpful affordances -->
        <md-icon slot="end">arrow_right</md-icon>
      </md-menu-item>
      <!-- Submenu must be slotted into sub-menu's menu slot -->
      <md-menu slot="menu">
        <md-menu-item>
          <div slot="headline">Apricot</div>
        </md-menu-item>
        <md-menu-item>
          <div slot="headline">Avocado</div>
        </md-menu-item>

        <!-- Nest as many as you want and control menu anchoring -->
        <md-sub-menu
            menu-corner="start-end"
            anchor-corner="start-start">
          <md-menu-item slot="item">
            <div slot="headline">Apples</div>
            <!-- Arrow icons are helpful affordances -->
            <md-icon slot="start">
              arrow_left
            </md-icon>
          </md-menu-item>
          <md-menu slot="menu">
            <md-menu-item>
              <div slot="headline">Fuji</div>
            </md-menu-item>
            <md-menu-item>
              <div slot="headline" style="white-space: nowrap;">Granny Smith</div>
            </md-menu-item>
            <md-menu-item>
              <div slot="headline" style="white-space: nowrap;">Red Delicious</div>
            </md-menu-item>
          </md-menu>

        </md-sub-menu>
      </md-menu>
    </md-sub-menu>

    <md-menu-item>
      <div slot="headline">Banana</div>
    </md-menu-item>
    <md-menu-item>
      <div slot="headline">Cucumber</div>
    </md-menu-item>
  </md-menu>
</span>

<script type="module">
  const anchorEl = document.body.querySelector('#usage-submenu-anchor');
  const menuEl = document.body.querySelector('#usage-submenu');

  anchorEl.addEventListener('click', () => { menuEl.open = !menuEl.open; });
</script>
```

--------------------------------

### Theming Circular Progress Indicator

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Customize the circular progress indicator's appearance by setting CSS custom properties. This example changes the size, active indicator width, and primary color.

```html
<style>
:root {
  --md-circular-progress-size: 32px;
  --md-circular-progress-active-indicator-width: 20;
  --md-sys-color-primary: #006A6A;
}
</style>

<md-circular-progress value="0.5"></md-circular-progress>
```

--------------------------------

### Chips with Icons and Avatars

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Illustrates how to add icons to assist chips and avatar images to input chips. Icons enhance clarity for actions, while avatars personalize input chips.

```html
<md-chip-set>
  <md-assist-chip label="Add to calendar">
    <md-icon slot="icon">event</md-icon>
  </md-assist-chip>

  <md-input-chip label="Ping Qiang" avatar>
    <img slot="icon" src="...">
  </md-input-chip>
</md-chip-set>
```

--------------------------------

### List Component Files

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Iterates through collections.component and displays the 'name' property of each file. Files are sorted alphabetically by 'data.name'.

```html
{% for file in collections.component|filtersort('data.name') %} {{ file.data.name }} {% endfor %}
```

--------------------------------

### Customize Checkbox Focus Ring with CSS ::part()

Source: https://github.com/material-components/material-web/blob/main/docs/support.md

Access and style internal parts of Material Web Components using the `::part()` CSS pseudo-element. This example targets the `focus-ring` part of an `md-checkbox` to adjust its size.

```css
md-checkbox::part(focus-ring) {
  width: 32px;
  height: 32px;
}
```

--------------------------------

### Basic Button Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Demonstrates the basic usage of outlined and filled buttons. Use these for primary and secondary actions.

```html
<md-outlined-button>Back</md-outlined-button>
<md-filled-button>Complete</md-filled-button>
```

--------------------------------

### Input Chips with Avatars

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Demonstrates how to create input chips with user avatars. Use the 'avatar' attribute and a slotted 'icon' for image display.

```html
<md-outlined-text-field label="Attendees" type="email"></md-outlined-text-field>

<md-chip-set>
  <md-input-chip label="Ping Qiang" avatar>
    <img slot="icon" src="...">
  </md-input-chip>
  <md-input-chip label="Thea Schröder" avatar>
    <img slot="icon" src="...">
  </md-input-chip>
</md-chip-set>
```

--------------------------------

### Basic Switch Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Demonstrates the default and selected states of the md-switch component.

```html
<md-switch></md-switch>
<md-switch selected></md-switch>
```

--------------------------------

### Themed Filled Text Field

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/textfield/theming-filled.html

Applies custom theming properties to a filled text field for a specific theme. This example demonstrates overriding shape, typography, color, and text field specific styles.

```css
figure.themed {
  background-color: #f4fbfa;
}

figure.themed.filled {
  --md-filled-text-field-container-shape: 0px;
  --md-sys-typescale-body-large: 400 1rem system-ui;
  --md-sys-color-primary: #006a6a;
  --md-sys-color-surface-container-highest: #e0e3e2;
  --md-filled-text-field-label-text-color: #3f4948;
  --md-filled-text-field-input-text-color: #161d1d;
}
```

--------------------------------

### Customize Button Colors with CSS Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/support.md

Use `--md-sys-color-*` tokens to change the primary and on-primary colors of filled buttons. This example shows how to set them to black and yellow, and how to use error color tokens.

```css
/* Buttons use the `primary` key color */
md-filled-button.spooky {
  --md-sys-color-primary: black;
  --md-sys-color-on-primary: yellow;
}

md-filled-button.error {
  --md-sys-color-primary: var(--md-sys-color-error);
  --md-sys-color-on-primary: var(--md-sys-color-on-error);
}
```

--------------------------------

### Basic Progress Indicators

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Use md-circular-progress and md-linear-progress for determinate and indeterminate progress. Set the 'value' attribute for determinate progress.

```html
<md-circular-progress value="0.75"></md-circular-progress>
<md-circular-progress indeterminate></md-circular-progress>

<md-linear-progress indeterminate></md-linear-progress>
<md-linear-progress value="0.5"></md-linear-progress>
```

--------------------------------

### Customize Filled Icon Button Styles

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/iconbutton/theming-filled.html

Use CSS custom properties to override default styles for filled icon buttons. This example sets custom dimensions, icon size, shape, primary color, and padding.

```css
.styled-example.filled {
  --md-filled-icon-button-container-width: 80px;
  --md-filled-icon-button-container-height: 80px;
  --md-filled-icon-button-icon-size: 40px;
  --md-filled-icon-button-container-shape: 0px;
  --md-sys-color-primary: #dc362e;
  padding-block: 8px;
}
```

--------------------------------

### Style Tonal Icon Button with Custom Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/iconbutton/theming-filled-tonal.html

Use these CSS custom properties to override default styles for tonal icon buttons. This example sets custom width, height, shape, icon size, and container color.

```css
.styled-example.tonal { --md-filled-tonal-icon-button-container-width: 80px; --md-filled-tonal-icon-button-container-height: 80px; --md-filled-tonal-icon-button-container-shape: 0px; --md-filled-tonal-icon-button-icon-size: 40px; --md-sys-color-secondary-container: #006A6A; padding-block: 8px; }
```

--------------------------------

### Accessible SVG Icon with Aria-label

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

For SVG icons, add an `aria-label` attribute directly to the SVG element to provide an accessible name for screen readers. This example shows an accessible paper airplane icon.

```html
<md-icon tabindex="-1"><svg aria-label="paper airplane" viewBox="0 0 48 48"><path d="M6 40V8l38 16Zm3-4.65L36.2 24 9 12.5v8.4L21.1 24 9 27Zm0 0V12.5 27Z"/></svg></md-icon>
```

--------------------------------

### string_ext.trim-start

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Removes whitespace from the beginning of a string.

```APIDOC
## `string_ext.trim-start`

### Description
Trims leading whitespace from the start of a string.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to trim.

### Return Value
- **string** - The string with whitespace trimmed from the start.

### Example
```scss
@debug string_ext.trim-start('  foo bar  '); // "foo bar  "
```
```

--------------------------------

### Extract deep fallback value from var() string

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `var.deep-fallback` to get the final concrete fallback value from a nested CSS `var()` string. Returns null if no fallback exists. Throws an error for invalid input.

```scss
var.deep-fallback($var) //=> string

/// @param {string} $var - A custom property `var()` string.
/// @return {string} The deep fallback value of the `var()` string. May be null if the `var()` does not have a fallback value.
/// @throw If the value is not a custom property.
```

```scss
$var: var(--foo, var(--bar, 8px));
@debug var.deep-fallback($var); // "8px"
```

--------------------------------

### List About Files

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Iterates through collections.about and displays the 'name' property of each file. Files are sorted by 'data.order'.

```html
{% for file in collections.about|filtersort('data.order') %} {{ file.data.name }} {% endfor %}
```

--------------------------------

### Linear Progress with Buffer

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Add a `buffer` attribute to `<md-linear-progress>` to display a buffer alongside the progress. This is useful for communicating both known and unknown progress.

```html
<md-linear-progress value="0.5" buffer="0.8"></md-linear-progress>
```

--------------------------------

### Trim leading whitespace with `string_ext.trim-start`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.trim-start` to remove whitespace characters from the beginning of a string. This is useful for cleaning up user input or formatted text.

```scss
@debug string_ext.trim-start('  foo bar  '); // "foo bar  "
```

--------------------------------

### Basic Checkbox Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/checkbox.md

Demonstrates standalone, checked, and indeterminate states of the md-checkbox component. Ensure the touch-target attribute is set to 'wrapper' for proper interaction.

```html
<md-checkbox touch-target="wrapper"></md-checkbox>
<md-checkbox touch-target="wrapper" checked></md-checkbox>
<md-checkbox touch-target="wrapper" indeterminate></md-checkbox>
```

--------------------------------

### MdSubMenu Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Methods available on the MdSubMenu component for programmatic control.

```APIDOC
## MdSubMenu Methods

### Description

Methods that can be called on the `md-sub-menu` component to control its state.

### Methods

- **show**() - Returns: `Promise<void>` - Shows the submenu.
- **close**() - Returns: `Promise<void>` - Closes the submenu.
```

--------------------------------

### Bundle JavaScript with Rollup

Source: https://github.com/material-components/material-web/blob/main/docs/quick-start.md

Use the Rollup CLI to create a bundled JavaScript file from your entry point (`index.js`). The `-p @rollup/plugin-node-resolve` flag ensures that node modules are correctly resolved.

```shell
npx rollup -p @rollup/plugin-node-resolve index.js -o bundle.js
```

--------------------------------

### Basic Text Field Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Demonstrates the basic usage of both filled and outlined text field components with a label and pre-filled value.

```html
<md-filled-text-field label="Label" value="Value">
</md-filled-text-field>

<md-outlined-text-field label="Label" value="Value">
</md-outlined-text-field>
```

--------------------------------

### Basic Icon Button Styles

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Demonstrates the different visual styles of icon buttons available in Material Web. Use these for compact action displays.

```html
<md-icon-button>
  <md-icon>check</md-icon>
</md-icon-button>
<md-filled-icon-button>
  <md-icon>check</md-icon>
</md-filled-icon-button>
<md-filled-tonal-icon-button>
  <md-icon>check</md-icon>
</md-filled-tonal-icon-button>
<md-outlined-icon-button>
  <md-icon>check</md-icon>
</md-outlined-icon-button>
```

--------------------------------

### Menu Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Methods available on the md-menu component.

```APIDOC
## Menu Methods

### `getBoundingClientRect()`
- **Returns**: `DOMRect`
- **Description**: Gets the bounding rectangle of the menu.

### `getClientRects()`
- **Returns**: `DOMRectList`
- **Description**: Gets the client rectangles of the menu.

### `close()`
- **Returns**: `void`
- **Description**: Closes the menu.

### `show()`
- **Returns**: `void`
- **Description**: Shows the menu.

### `activateNextItem()`
- **Returns**: `MenuItem`
- **Description**: Activates the next item in the menu. If at the end of the menu, the first item will be activated.

### `activatePreviousItem()`
- **Returns**: `MenuItem`
- **Description**: Activates the previous item in the menu. If at the start of the menu, the last item will be activated.

### `reposition()`
- **Returns**: `void`
- **Description**: Repositions the menu if it is open. Useful for the case where document or window-positioned menus have their anchors moved while open.
```

--------------------------------

### Standard FAB with Icon

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Use this snippet to create a standard Floating Action Button with an icon. Ensure you provide an accessible label.

```html
<md-fab aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### Eleventy Configuration for Component Stories

Source: https://github.com/material-components/material-web/blob/main/catalog/site/stories/stories.html

This configuration sets up Eleventy to process component data, filter for components with a dirname, and define permalinks for story pages. It also includes computed data for the component's directory name.

```javascript
{
  pagination: {
    data: "collections.component",
    size: 1,
    alias: "component",
    before: components => {
      // remove any components that don't have a dirname
      return components.filter(component => component.data.dirname)
    }
  },
  permalink: "components/{{component.data.page.fileSlug}}/stories/index.html",
  fullHeightContent: "true",
  collections: ["stories"],
  eleventyComputed: {
    dirname: ({component}) => component.data.dirname,
  }
}
```

--------------------------------

### Use Sass Ext for Type Checking and Map Operations

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Demonstrates using `@material/web/sass/ext/assert` and `@material/web/sass/ext/map_ext` for type checking and retrieving map values, reducing boilerplate compared to built-in Sass modules.

```scss
@use '@material/web/sass/ext/assert';
@use '@material/web/sass/ext/map_ext';

@mixin theme($values) {
  $values: assert.is-type($values, 'map');
  --primary: #{map_ext.get-strict($values, 'primary')};
  --secondary: #{map_ext.get-strict($values, 'secondary')};
  --tertiary: #{map_ext.get-strict($values, 'tertiary')};
}
```

--------------------------------

### Prefer Built-in Sass for Simple Null Checks

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Demonstrates the preferred way to handle a simple null check using built-in Sass functionality, avoiding external dependencies and unnecessary complexity.

```scss
// For simple use cases, built-in Sass functionality should be preferred.
@function value-or-default($value, $default) {
  @if $value == null {
    @return $default;
  }
  @return $value;
}
```

--------------------------------

### HTML and JavaScript for Document-Positioned Menu

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

This snippet demonstrates how to implement a document-positioned menu. It includes the necessary HTML structure for the button anchor and the menu itself, along with JavaScript to handle opening and closing the menu when the button is clicked. Note the absence of a `position: relative` parent, which is a key requirement for document positioning.

```html
<!-- Note the lack of position: relative parent. -->
<div style="margin: 16px;">
  <md-filled-button id="usage-document-anchor">Open document menu</md-filled-button>
</div>

<!-- document menus do not require a common ancestor with the anchor. -->
<md-menu positioning="document" id="usage-document" anchor="usage-document-anchor">
  <md-menu-item>
    <div slot="headline">Apple</div>
  </md-menu-item>
  <md-menu-item>
    <div slot="headline">Banana</div>
  </md-menu-item>
  <md-menu-item>
    <div slot="headline">Cucumber</div>
  </md-menu-item>
</md-menu>

<script type="module">
  const anchorEl = document.body.querySelector('#usage-document-anchor');
  const menuEl = document.body.querySelector('#usage-document');

  anchorEl.addEventListener('click', () => { menuEl.open = !menuEl.open; });
</script>
```

--------------------------------

### Basic Linear Progress Indicators

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Use `<md-linear-progress>` for indeterminate or determinate progress. Indeterminate shows animation without a specific value, while determinate requires a `value` attribute.

```html
<md-linear-progress indeterminate></md-linear-progress>
<md-linear-progress value="0.6"></md-linear-progress>
```

--------------------------------

### Toggle Icon Button Functionality

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Illustrates how to create toggleable icon buttons that change their appearance when selected. This is ideal for binary actions like favoriting or bookmarking.

```html
<div>
  <md-icon-button toggle>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-icon-button>
  <md-filled-icon-button toggle>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-filled-icon-button>
  <md-filled-tonal-icon-button toggle>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-filled-tonal-icon-button>
  <md-outlined-icon-button toggle>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-outlined-icon-button>
</div>
<div>
  <md-icon-button toggle selected>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-icon-button>
  <md-filled-icon-button toggle selected>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-filled-icon-button>
  <md-filled-tonal-icon-button toggle selected>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-filled-tonal-icon-button>
  <md-outlined-icon-button toggle selected>
    <md-icon>close</md-icon>
    <md-icon slot="selected">check</md-icon>
  </md-outlined-icon-button>
</div>
```

--------------------------------

### Compare Browser Elements with Material Elements

Source: https://github.com/material-components/material-web/blob/main/docs/intro.md

Illustrates the difference between standard HTML form elements and their Material Web Component equivalents. Use Material components for a Material Design-compliant UI.

```html
<!-- Browser elements -->
<form>
  <label>
    Email
    <input type="email" name="email" required>
  </label>
  <label>
    Subscribe
    <input type="checkbox" name="subscribe">
  </label>

  <button type="reset">Reset</button>
  <button>Submit</button>
</form>

<!-- Material elements -->
<form>
  <md-outlined-text-field label="Email" type="email"
      name="email" required></md-outlined-text-field>
  <label>
    Subscribe
    <md-checkbox name="subscribe"></md-checkbox>
  </label>

  <md-text-button type="reset">Reset</md-text-button>
  <md-outlined-button>Submit</md-outlined-button>
</form>
```

--------------------------------

### Divider Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Information on how to theme the Divider component using CSS custom properties.

```APIDOC
## Theming

Divider supports [theming](../theming/README.md) and can be customized with CSS custom property tokens.

### Tokens

| Token                    | Default value                |
| ------------------------ | ---------------------------- |
| `--md-divider-color`     | `--md-sys-color-outline-variant` |
| `--md-divider-thickness` | `1px`                        |

### Example

```html
<style>
:root {
  --md-sys-color-outline-variant: #BEC9C8;
  --md-divider-thickness: 2px;
}
</style>

<section>
  <p>Lorem ipsum...</p>
  <md-divider></md-divider>
  <p>Lorem ipsum...</p>
</section>
```
```

--------------------------------

### Customize MWC with CSS Design Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/intro.md

Demonstrates how to use CSS custom properties (design tokens) to style Material Web Components. This allows for theming and customization of components.

```css
:root {
  --md-sys-color-primary: olive;
  --md-sys-color-secondary: tomato;
  --md-ref-typeface-brand: 'Open Sans';
  --md-ref-typeface-plain: system-ui;
}
```

--------------------------------

### Show a Themed Menu with JavaScript

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/theming.html

Control the visibility of a Material Web menu using a JavaScript event listener. Attach a click listener to an anchor element to trigger the menu's `show()` method.

```javascript
const anchorEl = document.body.querySelector("#theming-anchor");
const menuEl = document.body.querySelector("#theming-menu");

anchorEl.addEventListener("click", () => {
  menuEl.show();
});
```

--------------------------------

### var.create

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Creates a CSS custom property `var()` string.

```APIDOC
## `var.create`

### Description
Creates a CSS custom property `var()` string.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
@debug var.create(--foo); // "var(--foo)"
@debug var.create(--foo, 8px); // "var(--foo, 8px)"
```

### Response
#### Success Response (200)
- **string**: A custom property `var()` string.

#### Response Example
N/A
```

--------------------------------

### MdElevatedButton API

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Provides details on the properties available for the MdElevatedButton component, including their attributes, types, default values, and descriptions.

```APIDOC
## MdElevatedButton <md-elevated-button>

### Description
Provides details on the properties available for the MdElevatedButton component, including their attributes, types, default values, and descriptions.

### Properties

#### `softDisabled`
- **Attribute**: `soft-disabled`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Whether or not the button is "soft-disabled" (disabled but still focusable). Use this when a button needs increased visibility when disabled. See https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/#kbd_disabled_controls for more guidance on when this is needed.

#### `href`
- **Attribute**: `href`
- **Type**: `string`
- **Default**: `''`
- **Description**: The URL that the link button points to.

#### `download`
- **Attribute**: `download`
- **Type**: `string`
- **Default**: `''`
- **Description**: The filename to use when downloading the linked resource. If not specified, the browser will determine a filename. This is only applicable when the button is used as a link (`href` is set).

#### `target`
- **Attribute**: `target`
- **Type**: `"" | "_blank" | "_parent" | "_self" | "_top"`
- **Default**: `''`
- **Description**: Where to display the linked `href` URL for a link button. Common options include `_blank` to open in a new tab.

#### `trailingIcon`
- **Attribute**: `trailing-icon`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Whether to render the icon at the inline end of the label rather than the inline start. _Note:_ Link buttons cannot have trailing icons.

#### `hasIcon`
- **Attribute**: `has-icon`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Whether to display the icon or not.

#### `name`
- **Attribute**: `name`
- **Type**: `string`
- **Default**: `undefined`
- **Description**: The HTML name to use in form submission.

#### `disabled`
- **Attribute**: `disabled`
- **Type**: `boolean`
- **Default**: `undefined`
- **Description**: Whether or not the element is disabled.

#### `type`
- **Attribute**: `type`
- **Type**: `string`
- **Default**: `'submit'`
- **Description**: A string indicating the form submission behavior of the element. - submit: The element submits the form. This is the default value if the attribute is not specified, or if it is dynamically changed to an empty or invalid value. - reset: The element resets the form. - button: The element does nothing.

#### `value`
- **Attribute**: `value`
- **Type**: `string`
- **Default**: `''`
- **Description**: The value of the button. When combined with a `name`, the submitting button's name/value will be added to the form.

#### `form`
- **Attribute**: (none)
- **Type**: `HTMLFormElement`
- **Default**: `undefined`
- **Description**: The associated form element with which this element's value will submit.

#### `labels`
- **Attribute**: (none)
- **Type**: `NodeList`
- **Default**: `undefined`
- **Description**: The labels this element is associated with.
```

--------------------------------

### Suggestion Chips

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Illustrates the use of suggestion chips for presenting dynamic suggestions to the user, such as search filters or possible responses.

```html
<h3>Suggested reply</h3>
<md-chip-set>
  <md-suggestion-chip label="I agree"></md-suggestion-chip>
  <md-suggestion-chip label="Looks good to me"></md-suggestion-chip>
  <md-suggestion-chip label="Thank you"></md-suggestion-chip>
</md-chip-set>
```

--------------------------------

### Branded FAB Sizes (Medium and Large)

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Demonstrates the default medium and large sizes for Branded FABs. Set the `size` attribute to control the dimensions.

```html
<md-branded-fab aria-label="Add">
  <svg slot="icon" viewBox="0 0 36 36">
    <path fill="#34A853" d="M16 16v14h4V20z"></path>
    <path fill="#4285F4" d="M30 16H20l-4 4h14z"></path>
    <path fill="#FBBC05" d="M6 16v4h10l4-4z"></path>
    <path fill="#EA4335" d="M20 16V6h-4v14z"></path>
    <path fill="none" d="M0 0h36v36H0z"></path>
  </svg>
</md-branded-fab>
<md-branded-fab size="large" aria-label="Add">
  <svg slot="icon" viewBox="0 0 36 36">
    <path fill="#34A853" d="M16 16v14h4V20z"></path>
    <path fill="#4285F4" d="M30 16H20l-4 4h14z"></path>
    <path fill="#FBBC05" d="M6 16v4h10l4-4z"></path>
    <path fill="#EA4335" d="M20 16V6h-4v14z"></path>
    <path fill="none" d="M0 0h36v36H0z"></path>
  </svg>
</md-branded-fab>
```

--------------------------------

### Text Field with Different Input Types

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Shows how to configure text fields for different input types like email and password, which can affect keyboard display and validation.

```html
<md-filled-text-field label="Username" type="email">
</md-filled-text-field>

<md-filled-text-field label="Password" type="password">
</md-filled-text-field>
```

--------------------------------

### Icon Button as a Link

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Shows how to convert an icon button into a clickable link by adding href and target attributes. Useful for navigation within an application.

```html
<md-icon-button href="https://google.com">
  <md-icon>check</md-icon>
</md-icon-button>
```

--------------------------------

### Inline CSS for Global Styles

Source: https://github.com/material-components/material-web/blob/main/catalog/site/stories/stories.html

Includes global CSS styles for the component stories. Ensure 'global.css' is available in the appropriate directory.

```html
{% inlinecss "global.css" %}
```

--------------------------------

### Menu with Anchor by Element Reference

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Shows how to anchor a menu to a button by directly referencing the button's HTMLElement. A parent with `position: relative` is necessary for proper layout.

```html
<span style="position: relative">
  <md-filled-button id="usage-anchor-2">Set with element ref</md-filled-button>
  <md-menu id="usage-menu-2">
    <md-menu-item>
      <div slot="headline">Apple</div>
    </md-menu-item>
    <md-menu-item>
      <div slot="headline">Banana</div>
    </md-menu-item>
    <md-menu-item>
      <div slot="headline">Cucumber</div>
    </md-menu-item>
  </md-menu>
</span>

<script type="module">
  // This example uses MdMenu.prototype.anchorElement to set the anchor as an
  // HTMLElement reference.
  const anchorEl = document.body.querySelector('#usage-anchor-2');
  const menuEl = document.body.querySelector('#usage-menu-2');
  menuEl.anchorElement = anchorEl;

  anchorEl.addEventListener('click', () => { menuEl.open = !menuEl.open; });
</script>
```

--------------------------------

### Menu with Anchor by ID Reference

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Demonstrates how to anchor a menu to a button using its ID. Ensure a parent element with `position: relative` is present for correct positioning.

```html
<span style="position: relative">
  <md-filled-button id="usage-anchor">Set with idref</md-filled-button>
  <md-menu id="usage-menu" anchor="usage-anchor">
    <md-menu-item>
      <div slot="headline">Apple</div>
    </md-menu-item>
    <md-menu-item>
      <div slot="headline">Banana</div>
    </md-menu-item>
    <md-menu-item>
      <div slot="headline">Cucumber</div>
    </md-menu-item>
  </md-menu>
</span>

<script type="module">
  // This example uses anchor as an ID reference
  const anchorEl = document.body.querySelector('#usage-anchor');
  const menuEl = document.body.querySelector('#usage-menu');

  anchorEl.addEventListener('click', () => { menuEl.open = !menuEl.open; });
</script>
```

--------------------------------

### Basic Chip Types

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Demonstrates the four basic types of chips: assist, filter, input, and suggestion. Use these for common actions, content filtering, user-entered information, and dynamic suggestions respectively.

```html
<md-chip-set>
  <md-assist-chip label="Assist"></md-assist-chip>
  <md-filter-chip label="Filter"></md-filter-chip>
  <md-input-chip label="Input"></md-input-chip>
  <md-suggestion-chip label="Suggestion"></md-suggestion-chip>
</md-chip-set>
```

--------------------------------

### Built-in Sass for Map Operations

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Illustrates the equivalent logic using built-in Sass modules (`sass:map`, `sass:meta`), highlighting the increased boilerplate required for type checking and map key retrieval.

```scss
@use 'sass:map';
@use 'sass:meta';

@mixin theme($values) {
  @if meta.type-of($values) != 'map' {
    @error 'Argument is not a map: #{meta.inspect($values)}';
  }
  @if not map.has-key($values, 'primary') {
    @error 'Map is missing key "primary": #{meta.inspect($values)}';
  }
  @if not map.has-key($values, 'secondary') {
    @error 'Map is missing key "secondary": #{meta.inspect($values)}';
  }
  @if not map.has-key($values, 'tertiary') {
    @error 'Map is missing key "tertiary": #{meta.inspect($values)}';
  }

  --primary: #{map.get($values, 'primary')};
  --secondary: #{map.get($values, 'secondary')};
  --tertiary: #{map.get($values, 'tertiary')};
}
```

--------------------------------

### Basic Material Dialog HTML Structure

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

Use this HTML structure to implement a basic dialog. Ensure system and component tokens are defined in CSS for proper styling. The dialog includes a headline and content area.

```html
<style>
:root {
  /* System tokens */
  --md-sys-color-surface-container-highest: #dde4e3;
  --md-sys-color-on-surface: #161d1d;
  --md-sys-color-on-surface-variant: #3f4948;
  --md-sys-typescale-body-medium: system-ui 16px/24px;
  --md-sys-typescale-headline-small: system-ui 24px/32px;

  /* Component tokens */
  --md-dialog-container-shape: 0px;
}
</style>

<md-dialog>
  <div slot="headline">Title</div>
  <div slot="content">Dialog content</div>
</md-dialog>
```

--------------------------------

### MdFocusRing Component API

Source: https://github.com/material-components/material-web/blob/main/docs/components/focus-ring.md

API documentation for the MdFocusRing component, detailing its properties, methods, and events.

```APIDOC
## MdFocusRing <md-focus-ring>

### Description
Focus rings are accessible outlines for components to show keyboard focus. They follow the same heuristics as `:focus-visible` to determine when they are visible.

### Properties
#### Path Parameters
- **visible** (boolean) - Optional - Makes the focus ring visible.
- **inward** (boolean) - Optional - Makes the focus ring animate inwards instead of outwards.
- **htmlFor** (string) - Optional - 
- **control** (HTMLElement) - Optional - 

### Methods
#### Path Parameters
- **attach** (control: HTMLElement) - Optional - 
- **detach** (_None_) - Optional - 

### Events
#### Success Response (200)
- **visibility-changed** (Event) - Fired whenever `visible` changes.
```

--------------------------------

### MdSuggestionChip Properties and Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

The `md-suggestion-chip` component offers properties for styling and behavior, along with an event for focus updates.

```APIDOC
## MdSuggestionChip `md-suggestion-chip`

### Properties

- `elevated` (boolean) - `false` - Whether the chip is elevated.
- `href` (string) - `''` - URL for a linkable chip.
- `download` (string) - `''` - The filename to use when downloading the linked resource. Applicable when `href` is set.
- `target` (string) - `''` - The target attribute for a linkable chip (`_blank`, `_parent`, `_self`, `_top`).
- `disabled` (boolean) - `false` - Whether or not the chip is disabled. Disabled chips are not focusable, unless `always-focusable` is set.
- `softDisabled` (boolean) - `false` - Whether or not the chip is "soft-disabled" (disabled but still focusable).
- `alwaysFocusable` (boolean) - `false` - When true, allow disabled chips to be focused with arrow keys.
- `label` (string) - `''` - The label of the chip.
- `hasIcon` (boolean) - `false` - Only needed for SSR. Add this attribute when a chip has a `slot="icon"` to avoid a Flash Of Unstyled Content.

### Events

- `update-focus` (Event) - Dispatched when `disabled` is toggled.
```

--------------------------------

### Generate Material Theme with Sass APIs

Source: https://github.com/material-components/material-web/blob/main/docs/support.md

Use Sass APIs to include light theme colors and typography, and conditionally include dark theme colors based on the `prefers-color-scheme` media query. Requires Material Web color and typography libraries.

```scss
@use '@material/web/color/color';
@use '@material/web/color/typography';

:root {
  @include color.light-theme;
  @include typography.theme;

  @media (prefers-color-scheme: dark) {
    @include color.dark-theme;
  }
}
```

--------------------------------

### Basic Dialog Structure

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

A basic dialog structure with a headline, content, and an action button. Use a form with method="dialog" to enable closing the dialog with buttons.

```html
<md-dialog>
  <div slot="headline">
    Dialog title
  </div>
  <form slot="content" id="form-id" method="dialog">
    A simple dialog with free-form content.
  </form>
  <div slot="actions">
    <md-text-button form="form-id">Ok</md-text-button>
  </div>
</md-dialog>
```

--------------------------------

### Button with Icon

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Shows how to add icons to buttons for enhanced communication and attention. Supports leading or trailing icons.

```html
<md-filled-tonal-button>
  Send
  <svg slot="icon" viewBox="0 0 48 48"><path d="M6 40V8l38 16Zm3-4.65L36.2 24 9 12.5v8.4L21.1 24 9 27Zm0 0V12.5 27Z"/></svg>
</md-filled-tonal-button>

<md-text-button trailing-icon>
  Open
  <svg slot="icon" viewBox="0 0 48 48"><path d="M9 42q-1.2 0-2.1-.9Q6 40.2 6 39V9q0-1.2.9-2.1Q7.8 6 9 6h13.95v3H9v30h30V25.05h3V39q0 1.2-.9 2.1-.9.9-2.1.9Zm10.1-10.95L17 28.9 36.9 9H25.95V6H42v16.05h-3v-10.9Z"/></svg>
</md-text-button>
```

--------------------------------

### MdSecondaryTab Component API

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

API documentation for the `<md-secondary-tab>` component.

```APIDOC
## MdSecondaryTab Component API

### Description
API documentation for the `<md-secondary-tab>` component.

### Properties
#### Path Parameters
- `isTab` (boolean) - Default: `true` - The attribute `md-tab` indicates that the element is a tab for the parent element, `<md-tabs>`. Make sure if you're implementing your own `md-tab` component that you have an `md-tab` attribute set.
- `active` (boolean) - Default: `false` - Whether or not the tab is selected.
- `hasIcon` (boolean) - Default: `false` - In SSR, set this to true when an icon is present.
- `iconOnly` (boolean) - Default: `false` - In SSR, set this to true when there is no label and only an icon.
- `selected` (boolean) - Default: `undefined` - 
```

--------------------------------

### MdInputChip Properties and Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

The `md-input-chip` component supports various properties for customization and emits events for user interactions.

```APIDOC
## MdInputChip `md-input-chip`

### Properties

- `avatar` (boolean) - `false` - Whether the chip displays an avatar.
- `href` (string) - `''` - URL for a linkable chip.
- `target` (string) - `''` - The target attribute for a linkable chip (`_blank`, `_parent`, `_self`, `_top`).
- `removeOnly` (boolean) - `false` - Whether the chip only has a remove button.
- `selected` (boolean) - `false` - Whether the chip is selected.
- `disabled` (boolean) - `false` - Whether or not the chip is disabled. Disabled chips are not focusable, unless `always-focusable` is set.
- `softDisabled` (boolean) - `false` - Whether or not the chip is "soft-disabled" (disabled but still focusable).
- `alwaysFocusable` (boolean) - `false` - When true, allow disabled chips to be focused with arrow keys.
- `label` (string) - `''` - The label of the chip.
- `hasIcon` (boolean) - `false` - Only needed for SSR. Add this attribute when a chip has a `slot="icon"` to avoid a Flash Of Unstyled Content.
- `handleTrailingActionFocus` (() => void) - `undefined` - Callback for handling focus on the trailing action.
- `ariaLabelRemove` (string) - `undefined` - ARIA label for the remove button.

### Events

- `remove` (Event) - Dispatched when the remove button is clicked.
- `update-focus` (Event) - Dispatched when `disabled` is toggled.
```

--------------------------------

### Include Bundled JavaScript in HTML

Source: https://github.com/material-components/material-web/blob/main/docs/quick-start.md

Reference the generated `bundle.js` file in your HTML using a standard `<script>` tag. This makes all your imported Material Web components and application logic available in the browser.

```html
<script src="./bundle.js"></script>
```

--------------------------------

### Text Field with Supporting Text

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Configures a filled text field with supporting text to provide additional guidance to the user.

```html
<md-filled-text-field
    label="Comments"
    supporting-text="Provide comments for the issue">
</md-filled-text-field>
```

--------------------------------

### MdSelectOption Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Properties for configuring individual MdSelectOption elements.

```APIDOC
## MdSelectOption Properties

### Description
Properties for MdSelectOption elements.

### Properties

- `disabled` (Attribute: `disabled`, Type: `boolean`, Default: `false`)
  Disables the item and makes it non-selectable and non-interactive.

- `selected` (Attribute: `selected`, Type: `boolean`, Default: `false`)
  Sets the item in the selected visual state when a submenu is opened.

- `value` (Attribute: `value`, Type: `string`, Default: `''`)
  Form value of the option.

- `type` (Type: `string`, Default: `'option' as const`)
  

- `typeaheadText` (Type: `string`, Default: `undefined`)
  

- `displayText` (Type: `string`, Default: `undefined`)
  
```

--------------------------------

### Text Field Labeling Strategies

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Illustrates different ways to label text fields: using a floating label, a placeholder, or an external label with an ARIA label for accessibility.

```html
<md-outlined-text-field label="Favorite food">
</md-outlined-text-field>

<md-outlined-text-field placeholder="email@domain.com">
</md-outlined-text-field>

<div>First name</div>
<md-outlined-text-field aria-label="First name">
</md-outlined-text-field>
```

--------------------------------

### Trim leading and trailing whitespace with `string_ext.trim`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.trim` to remove whitespace from both the beginning and end of a string. This is ideal for normalizing strings that may have extraneous whitespace.

```scss
@debug string_ext.trim('  foo bar  '); // "foo bar"
```

--------------------------------

### Apply Dark Theme with Sass

Source: https://github.com/material-components/material-web/blob/main/docs/support.md

This Sass code demonstrates how to include dark theme color tokens within a media query for `prefers-color-scheme: dark`. Ensure you have the Material Web color library included.

```scss
@use '@material/web/color/color';

:root {
  @media (prefers-color-scheme: dark) {
    @include color.dark-theme;
  }
}
```

--------------------------------

### Menu Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Configuration properties for the md-menu component.

```APIDOC
## Menu Properties

### `stayOpenOnFocusout`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Keeps the menu open when focus leaves the menu's composed subtree. NOTE: Focusout behavior will stop propagation of the focusout event. Set this property to true to opt-out of menu's focusout handling altogether.

### `skipRestoreFocus`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: After closing, does not restore focus to the last focused element before the menu was opened.

### `defaultFocus`
- **Type**: `FocusState`
- **Default**: `FocusState.FIRST_ITEM`
- **Description**: The element that should be focused by default once opened. NOTE: When setting default focus to 'LIST_ROOT', remember to change `tabindex` to `0` and change md-menu's display to something other than `display: contents` when necessary.

### `noNavigationWrap`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Turns off navigation wrapping. By default, navigating past the end of the menu items will wrap focus back to the beginning and vice versa. Use this for ARIA patterns that do not wrap focus, like combobox.

### `isSubmenu`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Whether or not the current menu is a submenu and should not handle specific navigation keys.

### `typeaheadController`
- **Type**: `TypeaheadController`
- **Default**: `function { ... }`
- **Description**: Handles typeahead navigation through the menu.

### `anchorElement`
- **Type**: `HTMLElement & Partial<SurfacePositionTarget>`
- **Default**: `undefined`
- **Description**: The element to which the menu is anchored.

### `items`
- **Type**: `MenuItem[]`
- **Default**: `undefined`
- **Description**: The array of menu items.
```

--------------------------------

### Apply Global Styles

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Applies global styles for Material Web components. Use this to ensure proper rendering and pending state management.

```css
body[dsd-pending] {
  display: none;
}

body[dsd-pending] {
  display: block !important;
}
```

--------------------------------

### MdSelect Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

These methods allow direct interaction with the MdSelect component to control its state and behavior.

```APIDOC
## MdSelect Methods

### Description
Methods for interacting with the MdSelect component.

### Methods

- `select(value: string): void`
  Selects an option given the value of the option, and updates MdSelect's value.

- `selectIndex(index: number): void`
  Selects an option given the index of the option, and updates MdSelect's value.

- `reset(): void`
  Reset the select to its default value.

- `showPicker(): void`
  Shows the picker. If it's already open, this is a no-op.

- `getUpdateComplete(): Promise<boolean>`
  

- `formResetCallback(): void`
  

- `formStateRestoreCallback(state: any): void`
  

- `click(): void`
  

- `checkValidity(): boolean`
  Checks the element's constraint validation and returns true if the element is valid or false if not.
  If invalid, this method will dispatch an `invalid` event.

- `reportValidity(): boolean`
  Checks the element's constraint validation and returns true if the element is valid or false if not.
  If invalid, this method will dispatch a cancelable `invalid` event. If not canceled, a the current `validationMessage` will be reported to the user.

- `setCustomValidity(error: string): void`
  Sets the element's constraint validation error message. When set to a non-empty string, `validity.customError` will be true and `validationMessage` will display the provided error.
  Use this method to customize error messages reported.
```

--------------------------------

### Open Fixed Menu with JavaScript

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/usage-fixed.html

Attaches a click event listener to an anchor element to toggle the open state of a fixed menu. Ensure the anchor and menu elements exist in the DOM.

```javascript
const anchorEl = document.body.querySelector("#usage-fixed-anchor");
const menuEl = document.body.querySelector("#usage-fixed");

anchorEl.addEventListener("click", () => {
  menuEl.open = !menuEl.open;
});
```

--------------------------------

### Open Dialog by Default

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

A dialog that is opened by default using the 'open' attribute.

```html
<md-dialog open>
  <div slot="content">
    A dialog that is opened by default.
  </div>
</md-dialog>
```

--------------------------------

### MdLinearProgress API

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

API documentation for the md-linear-progress component, including its properties.

```APIDOC
## MdLinearProgress <md-linear-progress>

### Description
Provides a linear progress indicator.

### Properties
#### Path Parameters
- **buffer** (number) - Optional - Buffer amount to display, a fraction between 0 and `max`. If the value is 0 or negative, the buffer is not displayed.
- **value** (number) - Optional - Progress to display, a fraction between 0 and `max`.
- **max** (number) - Optional - Maximum progress to display, defaults to 1.
- **indeterminate** (boolean) - Optional - Whether or not to display indeterminate progress, which gives no indication to how long an activity will take.
- **fourColor** (boolean) - Optional - Whether or not to render indeterminate mode using 4 colors instead of one.
```

--------------------------------

### Elevated Chips

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Demonstrates how to apply an elevated style to suggestion chips. Use elevated chips when they require visual prominence or protection, such as when overlaid on an image.

```html
<div>
  <img src="...">
  <md-chip-set>
    <md-suggestion-chip label="Share" elevated></md-suggestion-chip>
    <md-suggestion-chip label="Favorite" elevated></md-suggestion-chip>
  </md-chip-set>
</div>
```

--------------------------------

### string_ext.trim

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Removes whitespace from both the beginning and end of a string.

```APIDOC
## `string_ext.trim`

### Description
Trims leading and trailing whitespace from a string.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to trim.

### Return Value
- **string** - The string with leading and trailing whitespace trimmed.

### Example
```scss
@debug string_ext.trim('  foo bar  '); // "foo bar"
```
```

--------------------------------

### MdPrimaryTab Component API

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

API documentation for the `<md-primary-tab>` component.

```APIDOC
## MdPrimaryTab Component API

### Description
API documentation for the `<md-primary-tab>` component.

### Properties
#### Path Parameters
- `inlineIcon` (boolean) - Default: `false` - Whether or not the icon renders inline with label or stacked vertically.
- `isTab` (boolean) - Default: `true` - The attribute `md-tab` indicates that the element is a tab for the parent element, `<md-tabs>`. Make sure if you're implementing your own `md-tab` component that you have an `md-tab` attribute set.
- `active` (boolean) - Default: `false` - Whether or not the tab is selected.
- `hasIcon` (boolean) - Default: `false` - In SSR, set this to true when an icon is present.
- `iconOnly` (boolean) - Default: `false` - In SSR, set this to true when there is no label and only an icon.
- `selected` (boolean) - Default: `undefined` - 
```

--------------------------------

### Inline JavaScript for Theme Application

Source: https://github.com/material-components/material-web/blob/main/catalog/site/stories/stories.html

Applies saved themes to the component stories. Ensure 'apply-saved-theme.js' is available.

```html
{% inlinejs "inline/apply-saved-theme.js" %}
```

--------------------------------

### Toggle Menu with Element Reference

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/usage.html

This approach is useful when you need to explicitly set the anchor element for the menu. It also toggles the menu's open state on click.

```javascript
const anchorEl = document.body.querySelector("#usage-anchor-2");
const menuEl = document.body.querySelector("#usage-menu-2");

menuEl.anchorElement = anchorEl;

anchorEl.addEventListener("click", () => {
  menuEl.open = !menuEl.open;
});
```

--------------------------------

### Render Main Content

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Renders the main content of the page, converting Markdown to HTML. The `content` block holds the page's primary content.

```html
{% block content %}{{ content | mdMarkdown | safe }}{% endblock %}
```

--------------------------------

### MdSelect Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Provides methods to interact with the MdSelect component, such as selecting options, resetting the value, showing the picker, and performing validation.

```APIDOC
## MdSelect Methods

### `select(value)`

#### Description
Selects an option given the value of the option, and updates MdSelect's value.

#### Parameters
* **value** (any) - The value of the option to select.

#### Returns
* `void`

### `selectIndex(index)`

#### Description
Selects an option given the index of the option, and updates MdSelect's value.

#### Parameters
* **index** (number) - The index of the option to select.

#### Returns
* `void`

### `reset()`

#### Description
Resets the select to its default value.

#### Parameters
* None

#### Returns
* `void`

### `showPicker()`

#### Description
Shows the picker. If it's already open, this is a no-op.

#### Parameters
* None

#### Returns
* `void`

### `getUpdateComplete()`

#### Description
Returns a promise that resolves when the component has finished updating.

#### Parameters
* None

#### Returns
* `Promise<boolean>`

### `formResetCallback()`

#### Description
Callback for form reset.

#### Parameters
* None

#### Returns
* `void`

### `formStateRestoreCallback(state)`

#### Description
Callback for form state restoration.

#### Parameters
* **state** (any) - The state to restore.

#### Returns
* `void`

### `click()`

#### Description
Simulates a click on the select component.

#### Parameters
* None

#### Returns
* `void`

### `checkValidity()`

#### Description
Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch an `invalid` event.

#### Parameters
* None

#### Returns
* `boolean`

### `reportValidity()`

#### Description
Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch a cancelable `invalid` event. If not canceled, a the current `validationMessage` will be reported to the user.

#### Parameters
* None

#### Returns
* `boolean`

### `setCustomValidity(error)`

#### Description
Sets the element's constraint validation error message. When set to a non-empty string, `validity.customError` will be true and `validationMessage` will display the provided error. Use this method to customize error messages reported.

#### Parameters
* **error** (string) - The custom error message.

#### Returns
* `void`
```

--------------------------------

### MdOutlinedButton Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Configuration options for the MdOutlinedButton component.

```APIDOC
## MdOutlinedButton

### Description

Represents an outlined button component with various customization properties.

### Properties

- **softDisabled** (boolean) - Optional - Default: `false` - Whether or not the button is "soft-disabled" (disabled but still focusable).
- **href** (string) - Optional - Default: `''` - The URL that the link button points to.
- **download** (string) - Optional - Default: `''` - The filename to use when downloading the linked resource.
- **target** (string) - Optional - Default: `''` - Where to display the linked `href` URL for a link button.
- **trailingIcon** (boolean) - Optional - Default: `false` - Whether to render the icon at the inline end of the label.
- **hasIcon** (boolean) - Optional - Default: `false` - Whether to display the icon or not.
- **name** (string) - Optional - Default: `undefined` - The HTML name to use in form submission.
- **disabled** (boolean) - Optional - Default: `undefined` - Whether or not the element is disabled.
- **type** (string) - Optional - Default: `'submit'` - A string indicating the form submission behavior of the element.
- **value** (string) - Optional - Default: `''` - The value of the button.
- **form** (HTMLFormElement) - Optional - Default: `undefined` - The associated form element.
- **labels** (NodeList) - Optional - Default: `undefined` - The labels this element is associated with.
```

--------------------------------

### Standard Icon Button Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Customize the icon color, size, and state layer shape for a standard icon button. Overrides default tokens to change appearance.

```html
<style>
:root {
  --md-icon-button-icon-size: 32px;
  --md-sys-color-on-surface-variant: #dc362e;
  background-color: #fff8f6;
}
</style>

<md-icon-button>
  <md-icon>check</md-icon>
</md-icon-button>
```

--------------------------------

### Switch with Associated Labels

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Illustrates how to associate a label with a switch using either nested elements or the 'for' attribute.

```html
<label>
  Wi-Fi
  <md-switch selected></md-switch>
</label>

<label for="switch">Bluetooth</label>
<md-switch id="switch"></md-switch>
```

--------------------------------

### List Utilities

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Functions for comparing and intersecting lists.

```APIDOC
## `list_ext.are-equal`

### Description
Checks if two lists contain the same elements, regardless of their order.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$listA: ('apple', 'banana', 'cherry');
$listB: ('banana', 'cherry', 'apple');
$listC: ('apple', 'banana', 'date');

@debug list_ext.are-equal($listA, $listB); // true
@debug list_ext.are-equal($listA, $listC); // false
```

### Response
#### Success Response (200)
N/A

#### Response Example
N/A
```

```APIDOC
## `list_ext.intersection`

### Description
Returns the intersection of two lists.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$listA: ('apple', 'banana', 'cherry', 'date');
$listB: ('banana', 'cherry', 'apple');
$listC: ('apple', 'banana', 'date');

@debug list_ext.intersection($listA, $listB); // ('apple', 'banana', 'cherry')
@debug list_ext.intersection($listA, $listC); // ('apple', 'banana')
```

### Response
#### Success Response (200)
N/A

#### Response Example
N/A
```

--------------------------------

### MdSwitch Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Methods available on the MdSwitch component for validation and form state management.

```APIDOC
## MdSwitch Methods

### `formResetCallback()`
- **Returns**: `void`
- **Description**: Callback for form reset.

### `formStateRestoreCallback(state)`
- **Parameters**:
  - `state`: Any - The state to restore.
- **Returns**: `void`
- **Description**: Callback for form state restoration.

### `checkValidity()`
- **Returns**: `boolean`
- **Description**: Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch an `invalid` event.

### `reportValidity()`
- **Returns**: `boolean`
- **Description**: Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch a cancelable `invalid` event. If not canceled, a the current `validationMessage` will be reported to the user.

### `setCustomValidity(error)`
- **Parameters**:
  - `error` (string): The error message to set.
- **Returns**: `void`
- **Description**: Sets the element's constraint validation error message. When set to a non-empty string, `validity.customError` will be true and `validationMessage` will display the provided error. Use this method to customize error messages reported.
```

--------------------------------

### Replace all occurrences of pattern with `string_ext.replace-all`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.replace-all` to replace every occurrence of a pattern in a string with a specified replacement. This is useful for global text substitutions.

```scss
@debug string_ext.replace-all('foo bar baz', 'ba', 'qua'); // "foo quar quaz"
```

--------------------------------

### MdTextButton Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Configuration options for the MdTextButton component.

```APIDOC
## MdTextButton

### Description

Represents a text button component with various customization properties.

### Properties

- **softDisabled** (boolean) - Optional - Default: `false` - Whether or not the button is "soft-disabled" (disabled but still focusable).
- **href** (string) - Optional - Default: `''` - The URL that the link button points to.
- **download** (string) - Optional - Default: `''` - The filename to use when downloading the linked resource.
- **target** (string) - Optional - Default: `''` - Where to display the linked `href` URL for a link button.
- **trailingIcon** (boolean) - Optional - Default: `false` - Whether to render the icon at the inline end of the label.
- **hasIcon** (boolean) - Optional - Default: `false` - Whether to display the icon or not.
- **name** (string) - Optional - Default: `undefined` - The HTML name to use in form submission.
- **disabled** (boolean) - Optional - Default: `undefined` - Whether or not the element is disabled.
- **type** (string) - Optional - Default: `'submit'` - A string indicating the form submission behavior of the element.
- **value** (string) - Optional - Default: `''` - The value of the button.
- **form** (HTMLFormElement) - Optional - Default: `undefined` - The associated form element.
- **labels** (NodeList) - Optional - Default: `undefined` - The labels this element is associated with.
```

--------------------------------

### Map Utilities

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Functions for retrieving, splitting, picking, omitting, and renaming keys in maps.

```APIDOC
## `map_ext.get-strict`

### Description
The same as `map.get()` but throws an error if the key is not found.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$map: (
  'name': 'foo',
  'value': blue,
);

@debug map_ext.get-strict($map, 'name'); // 'foo'
@debug map_ext.get-strict($map, 'bar'); // ERROR: Key "bar" expected but not found in $map: ('name': 'foo', 'value': blue)
```

### Response
#### Success Response (200)
N/A

#### Response Example
N/A
```

```APIDOC
## `map_ext.split`

### Description
Splits a Map and returns a List pair with two new Maps: the first with the provided keys and the second without.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$map: (
  'focus': blue,
  'focus-within': blue,
  'hover': teal,
  'active': green,
);

$pair: map_ext.split($map, ('focus', 'focus-within'));

$map-with-focus-keys: list.nth($pair, 1);
@debug $map-with-focus-keys; // ('focus': blue, 'focus-within': blue)

$map-with-remaining-keys: list.nth($pair, 2);
@debug $map-with-remaining-keys; // ('hover': teal, 'active': green)
```

### Response
#### Success Response (200)
N/A

#### Response Example
N/A
```

```APIDOC
## `map_ext.pick`

### Description
Splits a Map and returns a new Map that only includes the provided keys.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$map: (
  'focus': blue,
  'focus-within': blue,
  'hover': teal,
  'active': green,
);

$map-with-focus-keys: map_ext.pick($map, ('focus', 'focus-within'));
@debug $map-with-focus-keys; // ('focus': blue, 'focus-within': blue)
```

### Response
#### Success Response (200)
N/A

#### Response Example
N/A
```

```APIDOC
## `map_ext.omit`

### Description
Splits a Map and returns a new Map that excludes the provided keys.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$map: (
  'focus': blue,
  'focus-within': blue,
  'hover': teal,
  'active': green,
);

$map-without-focus-keys: map_ext.omit($map, ('focus', 'focus-within'));
@debug $map-without-focus-keys; // ('hover': teal, 'active': green)
```

### Response
#### Success Response (200)
N/A

#### Response Example
N/A
```

```APIDOC
## `map_ext.rename-keys`

### Description
Returns the given map with any matching keys renamed according to the provided Map of keys to rename.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$map: ('foo': red);

$new-map: map_ext.rename-keys($map, ('foo': 'bar'));
@debug $new-map; // ('bar': red)
```

### Response
#### Success Response (200)
N/A

#### Response Example
N/A
```

--------------------------------

### Basic Radio Button Group

Source: https://github.com/material-components/material-web/blob/main/docs/components/radio.md

Demonstrates how to create a group of radio buttons with the same name attribute, allowing only one selection. Includes setting initial checked state and values for form submission.

```html
<form>
  <md-radio name="animals" value="cats"></md-radio>
  <md-radio name="animals" value="dogs"></md-radio>
  <md-radio name="animals" value="birds" checked></md-radio>
</form>
```

--------------------------------

### Create Secondary Tabs

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Use `<md-secondary-tab>` elements within `<md-tabs>` to create secondary tabs for content hierarchy. Icons can be included using the `slot="icon"`.

```html
<md-tabs>
  <md-secondary-tab>
    <md-icon slot="icon">flight</md-icon>
    Travel
  </md-secondary-tab>
  <md-secondary-tab>
    <md-icon slot="icon">hotel</md-icon>
    Hotel
  </md-secondary-tab>
  <md-secondary-tab>
    <md-icon slot="icon">hiking</md-icon>
    Activities
  </md-secondary-tab>
</md-tabs>
```

--------------------------------

### Leading Inset Divider Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Use `inset-start` to indent the divider with padding on the leading side, useful for separating related content within a section.

```html
<section>
  <p>Material 2</p>
  <md-divider inset-start></md-divider>
  <p>Material 3</p>
</section>
```

--------------------------------

### MdAssistChip Properties and Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

The MdAssistChip component represents an assist chip, offering properties for customization like elevation, link behavior, and disabled states, along with an `update-focus` event.

```APIDOC
## MdAssistChip

### Description
Represents an assist chip.

### Properties
#### Property
- `elevated` (boolean) - Optional - Whether the chip is elevated.
- `href` (string) - Optional - The URL for the link.
- `download` (string) - Optional - The filename for downloads when used as a link.
- `target` (string) - Optional - The target attribute for the link.
- `disabled` (boolean) - Optional - Whether or not the chip is disabled.
- `softDisabled` (boolean) - Optional - Whether or not the chip is "soft-disabled" (disabled but still focusable).
- `alwaysFocusable` (boolean) - Optional - When true, allow disabled chips to be focused with arrow keys.
- `label` (string) - Optional - The label of the chip.
- `hasIcon` (boolean) - Optional - Only needed for SSR. Add this attribute when a chip has a `slot="icon"` to avoid a Flash Of Unstyled Content.

### Events
#### Event
- `update-focus` (Event) - Dispatched when `disabled` is toggled.
```

--------------------------------

### Inline JavaScript for DSD Polyfill

Source: https://github.com/material-components/material-web/blob/main/catalog/site/stories/stories.html

Includes a JavaScript polyfill for Declarative Shadow DOM (DSD) to ensure compatibility across different environments. Ensure 'dsd-polyfill.js' is available.

```html
{% inlinejs "ssr-utils/dsd-polyfill.js" %}
```

--------------------------------

### Set Typeface CSS Variables

Source: https://github.com/material-components/material-web/blob/main/docs/theming/typography.md

Define CSS custom properties for brand and plain typefaces. Ensure the 'Roboto' font is loaded if no custom typeface is specified.

```css
@import url('https://fonts.googleapis.com/css2?family=Open%20Sans:wght@400;500;700&display=swap');

:root {
  --md-ref-typeface-brand: 'Open Sans';
  --md-ref-typeface-plain: system-ui;
}
```

--------------------------------

### FABs with Different Sizes

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

FABs can be sized small, medium (default), or large using the `size` attribute. Small FABs can optionally reduce their touch target.

```html
<md-fab size="small" touch-target="none" aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
<md-fab size="small" aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
<md-fab aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
<md-fab size="large" aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### Customize Typeface for Material Web

Source: https://github.com/material-components/material-web/blob/main/docs/theming/typography.md

Demonstrates how to set custom fonts for brand and plain typefaces, and how to map specific typescales to custom fonts. Prefer setting --md-ref-typeface-brand and --md-ref-typeface-plain to change all font families.

```css
:root {
  --my-brand-font: 'Open Sans';
  --my-headline-font: 'Montserrat';
  --my-title-font: 'Montserrat';
  --my-plain-font: system-ui;

  --md-ref-typeface-brand: var(--my-brand-font);
  --md-ref-typeface-plain: var(--my-plain-font);

  --md-sys-typescale-headline-font: var(--my-headline-font);
  --md-sys-typescale-title-font: var(--my-title-font);
}
```

--------------------------------

### Circular Progress Indicators

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Display circular progress indicators for determinate and indeterminate states. Use the 'value' attribute for determinate progress.

```html
<md-circular-progress indeterminate></md-circular-progress>
<md-circular-progress value="0.6"></md-circular-progress>
```

--------------------------------

### Open Document Menu with Click Event

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/usage-document.html

Attaches a click event listener to an anchor element to toggle the open state of a document menu. Ensure the menu element is correctly selected and has an 'open' property.

```javascript
const anchorEl = document.body.querySelector("#usage-document-anchor");
const menuEl = document.body.querySelector("#usage-document");

anchorEl.addEventListener("click", () => {
  menuEl.open = !menuEl.open;
});
```

--------------------------------

### Focusable Disabled Chips

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Configure disabled chips to be focusable using the 'soft-disabled' attribute. This is useful for discoverability of actions, especially when they depend on other conditions.

```html
<md-chip-set aria-label="Actions">
  <!--
    Disabled until text is selected. Since both are disabled by default, keep
    them focusable so that screen readers can discover the actions available.
  -->
  <md-assist-chip label="Copy" soft-disabled></md-assist-chip>
  <md-assist-chip label="Paste" soft-disabled></md-assist-chip>
</md-chip-set>
<md-outlined-text-field type="textarea"></md-outlined-text-field>
```

--------------------------------

### Standard Icon Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Standard icon buttons have no background or outline and offer the lowest emphasis. Use for general icon-based actions.

```html
<md-icon-button>
  <md-icon>check</md-icon>
</md-icon-button>
```

--------------------------------

### Render Top App Bar

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Renders the top app bar content. The `safe` filter ensures that HTML is rendered correctly.

```html
{% block topappbar %}{{ topappbar | safe }}{% endblock %}
```

--------------------------------

### MdRadio Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/radio.md

Methods available on the MdRadio element for programmatic control and validation.

```APIDOC
## MdRadio Methods

### Description
Methods available on the MdRadio element for programmatic control and validation.

### Methods
- **formResetCallback**() - void - Callback for form reset.
- **formStateRestoreCallback**(state) - void - Callback for form state restoration.
- **checkValidity**() - boolean - Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch an `invalid` event.
- **reportValidity**() - boolean - Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch a cancelable `invalid` event. If not canceled, a the current `validationMessage` will be reported to the user.
- **setCustomValidity**(error) - void - Sets the element's constraint validation error message. When set to a non-empty string, `validity.customError` will be true and `validationMessage` will display the provided error.
```

--------------------------------

### MdDivider API Reference

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Reference for the MdDivider component's properties, attributes, and their descriptions.

```APIDOC
## MdDivider <md-divider>

### Description
A thin line that groups content in lists and containers.

### Properties
#### Properties
- **inset** (boolean) - Default: `false` - Indents the divider with equal padding on both sides.
- **insetStart** (boolean) - Default: `false` - Indents the divider with padding on the leading side.
- **insetEnd** (boolean) - Default: `false` - Indents the divider with padding on the trailing side.

### Attributes
#### Attributes
- **inset** (boolean) - Indent the divider with equal padding on both sides.
- **inset-start** (boolean) - Indent the divider with padding on the leading side.
- **inset-end** (boolean) - Indent the divider with padding on the trailing side.
```

--------------------------------

### MdSubMenu Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Configurable properties for the MdSubMenu component that control its behavior and appearance.

```APIDOC
## MdSubMenu Properties

### Description

Properties that can be set on the `md-sub-menu` component to customize its behavior.

### Properties

- **anchorCorner** (Corner) - Default: `Corner.START_END` - The anchorCorner to set on the submenu.
- **menuCorner** (Corner) - Default: `Corner.START_START` - The menuCorner to set on the submenu.
- **hoverOpenDelay** (number) - Default: `400` - The delay between mouseenter and submenu opening.
- **hoverCloseDelay** (number) - Default: `400` - The delay between pointerleave and the submenu closing.
- **isSubMenu** (boolean) - Default: `true` - READONLY: self-identifies as a menu item and sets its identifying attribute.
- **item** (MenuItem) - Default: `undefined` - 
- **menu** (Menu) - Default: `undefined` - 
```

--------------------------------

### MdTabs Component API

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

API documentation for the `<md-tabs>` component, covering its properties, methods, and events.

```APIDOC
## MdTabs Component API

### Description
API documentation for the `<md-tabs>` component.

### Properties
#### Path Parameters
- `autoActivate` (boolean) - Default: `false` - Whether or not to automatically select a tab when it is focused.
- `activeTabIndex` (number) - Default: `undefined` - 
- `tabs` (Tab[]) - Default: `undefined` - The tabs of this tab bar.
- `activeTab` (Tab) - Default: `undefined` - 

### Methods
#### Path Parameters
- `scrollToTab` (tabToScrollTo) - Returns: `Promise<void>` - Scrolls the toolbar, if overflowing, to the active tab, or the provided tab.

### Events
#### Path Parameters
- `change` (Event) - Fired when the selected tab changes. The target's `activeTabIndex` or `activeTab` provide information about the selection change. The change event is fired when a user interaction like a space/enter key or click cause a selection change. The tab selection based on these actions can be cancelled by calling preventDefault on the triggering `keydown` or `click` event.
```

--------------------------------

### Apply Icon Theming and Styles

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

Use this HTML and CSS to apply different Material Symbols fonts (Outlined, Rounded, Sharp) and styles (filled) to md-icon components. Ensure the necessary Google Fonts are linked.

```html
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL@20..48,100..700,0..1" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL@20..48,100..700,0..1"  rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Sharp:opsz,wght,FILL@20..48,100..700,0..1" rel="stylesheet">

<style>
md-icon {
  color: #006A6A;
  --md-icon-size: 48px;
}
.rounded {
  --md-icon-font: 'Material Symbols Rounded';
}
.sharp {
  --md-icon-font: 'Material Symbols Sharp';
}
md-icon[filled] {
  font-variation-settings: 'FILL' 1;
}
</style>

<h3>Outlined</h3>
<span>
  <md-icon>settings</md-icon>
  <md-icon>check_box</md-icon>
  <md-icon>house</md-icon>
  <md-icon filled>settings</md-icon>
  <md-icon filled>check_box</md-icon>
  <md-icon filled>house</md-icon>
</span>
<h3>Rounded</h3>
<span class="rounded">
  <md-icon>settings</md-icon>
  <md-icon>check_box</md-icon>
  <md-icon>house</md-icon>
  <md-icon filled>settings</md-icon>
  <md-icon filled>check_box</md-icon>
  <md-icon filled>house</md-icon>
</span>
<h3>Sharp</h3>
<span class="sharp">
  <md-icon>settings</md-icon>
  <md-icon>check_box</md-icon>
  <md-icon>house</md-icon>
  <md-icon filled>settings</md-icon>
  <md-icon filled>check_box</md-icon>
  <md-icon filled>house</md-icon>
</span>
```

--------------------------------

### Style a Material Web Menu

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/theming.html

Apply custom styles to a Material Web menu and its items using CSS custom properties. Ensure the menu container and item shapes are defined.

```css
.styled-example {
  background-color: #f4fbfa;
}

.styled-example md-filled-button {
  --md-sys-color-primary: #006a6a;
  --md-sys-color-on-primary: #ffffff;
}

.styled-example.menu md-menu {
  --md-menu-container-color: #f4fbfa;
  --md-menu-container-shape: 0px;
  --md-sys-color-on-surface: #161d1d;
  --md-sys-typescale-body-large-font: system-ui;
}

.styled-example.menu md-menu-item {
  border-radius: 28px;
}

.styled-example.menu md-menu-item::part(focus-ring) {
  border-radius: 28px;
}
```

--------------------------------

### Four-Color Progress Indicators

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Enable the 'four-color' attribute on indeterminate progress indicators to cycle through primary, primary container, tertiary, and tertiary container colors.

```html
<md-circular-progress four-color indeterminate></md-circular-progress>
<md-linear-progress four-color indeterminate></md-linear-progress>
```

--------------------------------

### Attach Focus Ring Imperatively

Source: https://github.com/material-components/material-web/blob/main/docs/components/focus-ring.md

Programmatically attach the focus ring to a control using JavaScript. This provides dynamic control over focus ring association. The parent container requires `position: relative`.

```html
<div style="position: relative">
  <md-focus-ring id="ring" style="--md-focus-ring-shape: 8px"></md-focus-ring>
  <button id="ring-control"></button>
</div>
<script>
  const focusRing = document.querySelector('#ring');
  const control = document.querySelector('#ring-control');
  focusRing.attach(control);
</script>
```

--------------------------------

### Menu Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Events fired by the md-menu component.

```APIDOC
## Menu Events

### `opening`
- **Type**: `Event`
- **Bubbles**: No
- **Composed**: No
- **Description**: Fired before the opening animation begins.

### `opened`
- **Type**: `Event`
- **Bubbles**: No
- **Composed**: No
- **Description**: Fired once the menu is open, after any animations.

### `closing`
- **Type**: `Event`
- **Bubbles**: No
- **Composed**: No
- **Description**: Fired before the closing animation begins.

### `closed`
- **Type**: `Event`
- **Bubbles**: No
- **Composed**: No
- **Description**: Fired once the menu is closed, after any animations.
```

--------------------------------

### Compare maps with `map_ext.difference`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `map_ext.difference` to find keys that differ between two maps. Divergence occurs if a key is unique to the second map or if a key exists in both but has a different value.

```scss
$mapA: ('foo': red, 'bar': yellow, 'baz': 10);
$mapB: ('foo': red, 'bar': green,  'baz': 10, 'fooBar': blue);

$differences: map_ext.difference($mapA, $mapB);
@debug $differences; // ('bar', 'fooBar')
```

--------------------------------

### MdMenuItem Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Configuration properties for the md-menu-item component.

```APIDOC
## MdMenuItem Properties

### `disabled`
- **Attribute**: `disabled`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Disables the item and makes it non-selectable and non-interactive.

### `type`
- **Attribute**: `type`
- **Type**: `MenuItemType`
- **Default**: `'menuitem'`
- **Description**: Sets the behavior and role of the menu item, defaults to "menuitem".

### `href`
- **Attribute**: `href`
- **Type**: `string`
- **Default**: `''`
- **Description**: Sets the underlying `HTMLAnchorElement`'s `href` resource attribute.

### `target`
- **Attribute**: `target`
- **Type**: `"" | "_blank" | "_parent" | "_self" | "_top"`
- **Default**: `''`
- **Description**: Sets the underlying `HTMLAnchorElement`'s `target` attribute when `href` is set.

### `keepOpen`
- **Attribute**: `keep-open`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Keeps the menu open if clicked or keyboard selected.

### `selected`
- **Attribute**: `selected`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Sets the item in the selected visual state when a submenu is opened.

### `typeaheadText`
- **Type**: `string`
- **Default**: `undefined`
- **Description**: Text used for typeahead navigation.
```

--------------------------------

### Pick specific keys from a map

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Creates a new map containing only the specified keys from the original map. Useful for extracting a subset of properties.

```scss
map_ext.pick($map, $keys) //=> map

/// @param {map} $map - The Map to split.
/// @param {list} $keys - List of keys to include in the new Map.
/// @return {map} Map with only the keys provided.
```

```scss
$map: (
  'focus': blue,
  'focus-within': blue,
  'hover': teal,
  'active': green,
);

$map-with-focus-keys: map_ext.pick($map, ('focus', 'focus-within'));
@debug $map-with-focus-keys; // ('focus': blue, 'focus-within': blue)
```

--------------------------------

### MdDialog Component API

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

The `<md-dialog>` component provides a way to display modal dialogs. This section outlines its properties, methods, and events.

```APIDOC
## MdDialog API

### Properties

| Property | Attribute | Type | Default | Description |
| --- | --- | --- | --- | --- |
| `quick` | `quick` | `boolean` | `false` | Skips the opening and closing animations. |
| `returnValue` |  | `string` | `''` | Gets or sets the dialog's return value, usually to indicate which button a user pressed to close it.<br>https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/returnValue |
| `type` | `type` | `string` | `undefined` | The type of dialog for accessibility. Set this to `alert` to announce a dialog as an alert dialog. |
| `noFocusTrap` | `no-focus-trap` | `boolean` | `false` | Disables focus trapping, which by default keeps keyboard Tab navigation within the dialog.<br>When disabled, after focusing the last element of a dialog, pressing Tab again will release focus from the window back to the browser (such as the URL bar).<br>Focus trapping is recommended for accessibility, and should not typically be disabled. Only turn this off if the use case of a dialog is more accessible without focus trapping. |
| `open` | `open` | `boolean` | `undefined` |  |
| `getOpenAnimation` |  | `() => DialogAnimation` | `function { ... }` | Gets the opening animation for a dialog. Set to a new function to customize the animation. |
| `getCloseAnimation` |  | `() => DialogAnimation` | `function { ... }` | Gets the closing animation for a dialog. Set to a new function to customize the animation. |

### Methods

| Method | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `show` | _None_ | `Promise<void>` | Opens the dialog and fires a cancelable `open` event. After a dialog's animation, an `opened` event is fired.<br>Add an `autofocus` attribute to a child of the dialog that should receive focus after opening. |
| `close` | `returnValue` | `Promise<void>` | Closes the dialog and fires a cancelable `close` event. After a dialog's animation, a `closed` event is fired. |

### Events

| Event | Type | [Bubbles](https://developer.mozilla.org/en-US/docs/Web/API/Event/bubbles) | [Composed](https://developer.mozilla.org/en-US/docs/Web/API/Event/composed) | Description |
| --- | --- | --- | --- | --- |
| `open` | `Event` | No | No | Dispatched when the dialog is opening before any animations. |
| `opened` | `Event` | No | No | Dispatched when the dialog has opened after any animations. |
| `close` | `Event` | No | No | Dispatched when the dialog is closing before any animations. |
| `closed` | `Event` | No | No | Dispatched when the dialog has closed after any animations. |
| `cancel` | `Event` | No | No | Dispatched when the dialog has been canceled by clicking on the scrim or pressing Escape. |
```

--------------------------------

### Replace first occurrence of pattern with `string_ext.replace`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.replace` to substitute the first instance of a pattern within a string with a new value. If the pattern is not found, the original string is returned.

```scss
@debug string_ext.replace('foo bar baz', 'bar', 'quux'); // "foo quux baz"
```

--------------------------------

### Specify Icons by Name, Codepoint, or SVG

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

Icons can be specified using their ligature name, a Unicode codepoint, or by embedding an SVG element directly. Use this for basic icon implementation.

```html
<md-icon>settings</md-icon>
<md-icon>&#xe834</md-icon>
<md-icon><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48"><path d="M10 40V24H4L24 6l10 8.85V9h4v9.55L44 24h-6v16H26.5V28h-5v12Zm3-3h5.5V25h11v12H35V19.95l-11-10-11 10Zm5.5-12h11-11Zm1.25-5.5h8.5q0-1.65-1.275-2.725Q25.7 15.7 24 15.7q-1.7 0-2.975 1.075Q19.75 17.85 19.75 19.5Z"/></svg></md-icon>
```

--------------------------------

### Basic md-tabs Usage

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Use md-tabs to create a tab bar with primary or secondary tab children. Ensure all tabs within a tab bar are of the same type.

```html
<md-tabs>
  <md-primary-tab>Video</md-primary-tab>
  <md-primary-tab>Photos</md-primary-tab>
  <md-primary-tab>Audio</md-primary-tab>
</md-tabs>

<md-tabs>
  <md-secondary-tab>Birds</md-secondary-tab>
  <md-secondary-tab>Cats</md-secondary-tab>
  <md-secondary-tab>Dogs</md-secondary-tab>
</md-tabs>
```

--------------------------------

### string_ext.replace

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Replaces the first occurrence of a pattern within a string with a specified replacement.

```APIDOC
## `string_ext.replace`

### Description
Returns a new string with the first match of a pattern replaced by a given string.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to be searched.
- **$pattern** (string) - Required - The pattern to search for.
- **$replacement** (string) - Required - The value to replace the pattern.

### Return Value
- **string** - The string with the first match of pattern replaced by the replacement or the initial string itself.

### Example
```scss
@debug string_ext.replace('foo bar baz', 'bar', 'quux'); // "foo quux baz"
```
```

--------------------------------

### Apply Typescale Classes in LitElement

Source: https://github.com/material-components/material-web/blob/main/docs/theming/typography.md

Import and apply Material Web's typescale styles within a LitElement component. This allows using predefined classes like '.md-typescale-display-large' for styling text elements.

```typescript
import {styles as typescaleStyles} from '@material/web/typography/md-typescale-styles.js';

// `typescaleStyles.styleSheet` is a `CSSStyleSheet` that can be added to a
// document or shadow root's `adoptedStyleSheets` to use the `.md-typescale-*`
// classes.
document.adoptedStyleSheets.push(typescaleStyles.styleSheet);

// `typescaleStyles` can also be added to a `LitElement` component's styles.
class App extends LitElement {
  static styles = [typescaleStyles, css`...`];

  render() {
    return html`
      <h1 class="md-typescale-display-large">Large display</h1>
      <p class="md-typescale-body-medium">Body text</p>
    `;
  }
}
```

--------------------------------

### MdRipple Component API

Source: https://github.com/material-components/material-web/blob/main/docs/components/ripple.md

The MdRipple component provides visual feedback for user interactions. This section details its properties and methods.

```APIDOC
## MdRipple Component API

### Description
Provides visual feedback for user interactions through ripple effects.

### Properties
#### Properties
- **`disabled`** (boolean) - `false` - Disables the ripple.
- **`htmlFor`** (string) - `undefined` - 
- **`control`** (HTMLElement) - `undefined` - 

### Methods
#### Methods
- **`attach`** (control: HTMLElement) - `void` - Attaches the ripple to a control element.
- **`detach`** (_None_) - `void` - Detaches the ripple from its control element.
```

--------------------------------

### string_ext.replace-all

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Replaces all occurrences of a pattern within a string with a specified replacement.

```APIDOC
## `string_ext.replace-all`

### Description
Returns a new string with all matches of a pattern replaced by a given string.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to be searched.
- **$pattern** (string) - Required - The pattern to search for.
- **$replacement** (string) - Required - The value to replace the pattern.

### Return Value
- **string** - The string with all matches of pattern replaced by the replacement or the initial string itself.

### Example
```scss
@debug string_ext.replace-all('foo bar baz', 'ba', 'qua'); // "foo quar quaz"
```
```

--------------------------------

### MdSubMenu Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Events emitted by the MdSubMenu component for interaction feedback.

```APIDOC
## MdSubMenu Events

### Description

Events emitted by the `md-sub-menu` component to communicate state changes or request actions from parent components.

### Events

- **deactivate-items**: Type: `Event`, Bubbles: Yes, Composed: Yes - Requests the parent menu to deselect other items when a submenu opens.
- **request-activation**: Type: `Event`, Bubbles: Yes, Composed: Yes - Requests the parent to make the slotted item focusable and focus the item. Used internally for menu keyboard navigation; most applications do not need to listen for this event. It is exposed for authors building their own menu-item replacements, or for advanced cases where you want to call `preventDefault`/`stopPropagation` on it to override default focus handling.
- **deactivate-typeahead**: Type: `Event`, Bubbles: Yes, Composed: Yes - Requests the parent menu to deactivate the typeahead functionality when a submenu opens.
- **activate-typeahead**: Type: `Event`, Bubbles: Yes, Composed: Yes - Requests the parent menu to activate the typeahead functionality when a submenu closes.
```

--------------------------------

### MdIconButton Properties and Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

This snippet details the properties and events available for the `<md-icon-button>` component, allowing for customization of its behavior and appearance.

```APIDOC
## MdIconButton Properties and Events

### Description

Provides an interface for configuring the behavior and appearance of the `md-icon-button` component. This includes properties for controlling its disabled state, link behavior, toggle functionality, and ARIA attributes, as well as events for tracking state changes.

### Properties

#### `softDisabled`
- **Type**: `boolean`
- **Attribute**: `soft-disabled`
- **Default**: `false`
- **Description**: "Soft-disables" the icon button (disabled but still focusable). Use this when an icon button needs increased visibility when disabled. See https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/#kbd_disabled_controls for more guidance on when this is needed.

#### `flipIconInRtl`
- **Type**: `boolean`
- **Attribute**: `flip-icon-in-rtl`
- **Default**: `false`
- **Description**: Flips the icon if it is in an RTL context at startup.

#### `href`
- **Type**: `string`
- **Attribute**: `href`
- **Default**: `''`
- **Description**: Sets the underlying `HTMLAnchorElement`'s `href` resource attribute.

#### `download`
- **Type**: `string`
- **Attribute**: `download`
- **Default**: `''`
- **Description**: The filename to use when downloading the linked resource. If not specified, the browser will determine a filename. This is only applicable when the icon button is used as a link (`href` is set).

#### `target`
- **Type**: `"" \| LinkTarget`
- **Attribute**: `target`
- **Default**: `''`
- **Description**: Sets the underlying `HTMLAnchorElement`'s `target` attribute.

#### `ariaLabelSelected`
- **Type**: `string`
- **Attribute**: `aria-label-selected`
- **Default**: `''`
- **Description**: The `aria-label` of the button when the button is toggleable and selected.

#### `toggle`
- **Type**: `boolean`
- **Attribute**: `toggle`
- **Default**: `false`
- **Description**: When true, the button will toggle between selected and unselected states.

#### `selected`
- **Type**: `boolean`
- **Attribute**: `selected`
- **Default**: `false`
- **Description**: Sets the selected state. When false, displays the default icon. When true, displays the selected icon, or the default icon If no `slot="selected"` icon is provided.

#### `name`
- **Type**: `string`
- **Attribute**: `name`
- **Default**: `undefined`
- **Description**: The HTML name to use in form submission.

#### `disabled`
- **Type**: `boolean`
- **Attribute**: `disabled`
- **Default**: `undefined`
- **Description**: Whether or not the element is disabled.

#### `type`
- **Type**: `string`
- **Attribute**: `type`
- **Default**: `'submit'`
- **Description**: A string indicating the form submission behavior of the element. - submit: The element submits the form. This is the default value if the attribute is not specified, or if it is dynamically changed to an empty or invalid value. - reset: The element resets the form. - button: The element does nothing.

#### `value`
- **Type**: `string`
- **Attribute**: `value`
- **Default**: `''`
- **Description**: The value of the button. When combined with a `name`, the submitting button's name/value will be added to the form.

#### `form`
- **Type**: `HTMLFormElement`
- **Default**: `undefined`
- **Description**: The associated form element with which this element's value will submit.

#### `labels`
- **Type**: `NodeList`
- **Default**: `undefined`
- **Description**: The labels this element is associated with.

### Events

#### `input`
- **Type**: `InputEvent`
- **Bubbles**: Yes
- **Composed**: Yes
- **Description**: Dispatched when a toggle button toggles.

#### `change`
- **Type**: `Event`
- **Bubbles**: Yes
- **Composed**: No
- **Description**: Dispatched when a toggle button toggles.
```

--------------------------------

### Outlined Icon Button Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Customize the container size, shape, icon size, outline width, and color for an outlined icon button. Adjusts tokens for the outlined variant.

```html
<style>
:root {
  --md-outlined-icon-button-container-width: 80px;
  --md-outlined-icon-button-container-height: 80px;
  --md-outlined-icon-button-container-shape: 0px;
  --md-outlined-icon-button-icon-size: 40px;
  --md-outlined-icon-button-outline-width: 4px;
  --md-sys-color-outline: #006A6A;
}
</style>
<md-outlined-icon-button>
  <md-icon>check</md-icon>
</md-outlined-icon-button>
```

--------------------------------

### Filled Icon Button Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Customize the container size, shape, icon size, and color for a filled icon button. Demonstrates overriding specific tokens for a filled variant.

```html
<style>
:root {
  --md-filled-icon-button-container-width: 80px;
  --md-filled-icon-button-container-height: 80px;
  --md-filled-icon-button-icon-size: 40px;
  --md-filled-icon-button-container-shape: 0px;
  --md-sys-color-primary: #dc362e;
  background-color: #fff8f6;
}
</style>
<md-filled-icon-button>
  <md-icon>check</md-icon>
</md-filled-icon-button>
```

--------------------------------

### Small Branded FAB

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

A small branded FAB with a Google logo icon. Use for compact UIs where space is limited.

```html
<md-branded-fab size="small" aria-label="Add">
  <svg slot="icon" viewBox="0 0 36 36">
    <path fill="#34A853" d="M16 16v14h4V20z"></path>
    <path fill="#4285F4" d="M30 16H20l-4 4h14z"></path>
    <path fill="#FBBC05" d="M6 16v4h10l4-4z"></path>
    <path fill="#EA4335" d="M20 16V6h-4v14z"></path>
    <path fill="none" d="M0 0h36v36H0z"></path>
  </svg>
</md-branded-fab>
```

--------------------------------

### MdCircularProgress API

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

API documentation for the md-circular-progress component, including its properties.

```APIDOC
## MdCircularProgress <md-circular-progress>

### Description
Provides a circular progress indicator.

### Properties
#### Path Parameters
- **value** (number) - Optional - Progress to display, a fraction between 0 and `max`.
- **max** (number) - Optional - Maximum progress to display, defaults to 1.
- **indeterminate** (boolean) - Optional - Whether or not to display indeterminate progress, which gives no indication to how long an activity will take.
- **fourColor** (boolean) - Optional - Whether or not to render indeterminate mode using 4 colors instead of one.
```

--------------------------------

### Accessibility with aria-label

Source: https://github.com/material-components/material-web/blob/main/docs/components/checkbox.md

Illustrates how to use the 'aria-label' attribute for checkboxes that lack visible labels or require more descriptive labels for accessibility. This is crucial for screen readers and assistive technologies.

```html
<md-checkbox aria-label="Select all checkboxes"></md-checkbox>

<label>
  <md-checkbox aria-label="Agree to terms and conditions"></md-checkbox>
  Agree
</label>
```

--------------------------------

### Apply Theming to Ripple Component

Source: https://github.com/material-components/material-web/blob/main/docs/components/ripple.md

Use CSS custom properties to theme the ripple's hover and pressed states. Ensure the ripple is contained within a relative positioned element.

```html
<style>
:root {
  --md-sys-color-primary: #006A6A;
  --md-ripple-hover-color: var(--md-sys-color-primary);
  --md-ripple-pressed-color: var(--md-sys-color-primary);
}

.container {
  position: relative;
}
</style>
<button class="container">
  <md-ripple></md-ripple>
</button>
```

--------------------------------

### MdFilledTonalIconButton Properties and Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

This snippet details the properties and events available for the `md-filled-tonal-icon-button` component. Properties can be set directly or via attributes, and events are dispatched for specific user interactions.

```APIDOC
## MdFilledTonalIconButton

### Description
Represents a filled tonal icon button component. It supports various properties for customization and state management, and emits events for user interactions.

### Properties

#### Property Details

- **`softDisabled`** (boolean) - Optional - Default: `false` - "Soft-disables" the icon button (disabled but still focusable).
- **`flipIconInRtl`** (boolean) - Optional - Default: `false` - Flips the icon if it is in an RTL context at startup.
- **`href`** (string) - Optional - Default: `''` - Sets the underlying `HTMLAnchorElement`'s `href` resource attribute.
- **`download`** (string) - Optional - Default: `''` - The filename to use when downloading the linked resource. Applicable when `href` is set.
- **`target`** (string) - Optional - Default: `''` - Sets the underlying `HTMLAnchorElement`'s `target` attribute.
- **`ariaLabelSelected`** (string) - Optional - Default: `''` - The `aria-label` of the button when the button is toggleable and selected.
- **`toggle`** (boolean) - Optional - Default: `false` - When true, the button will toggle between selected and unselected states.
- **`selected`** (boolean) - Optional - Default: `false` - Sets the selected state. When false, displays the default icon. When true, displays the selected icon, or the default icon if no `slot="selected"` icon is provided.
- **`name`** (string) - Optional - Default: `undefined` - The HTML name to use in form submission.
- **`disabled`** (boolean) - Optional - Default: `undefined` - Whether or not the element is disabled.
- **`type`** (string) - Optional - Default: `'submit'` - A string indicating the form submission behavior: `submit`, `reset`, or `button`.
- **`value`** (string) - Optional - Default: `''` - The value of the button. When combined with a `name`, the submitting button's name/value will be added to the form.
- **`form`** (HTMLFormElement) - Optional - Default: `undefined` - The associated form element with which this element's value will submit.
- **`labels`** (NodeList) - Optional - Default: `undefined` - The labels this element is associated with.

### Events

#### Event Details

- **`input`** (InputEvent) - Bubbles: Yes, Composed: Yes - Dispatched when a toggle button toggles.
- **`change`** (Event) - Bubbles: Yes, Composed: No - Dispatched when a toggle button toggles.
```

--------------------------------

### string_ext.trim-end

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Removes whitespace from the end of a string.

```APIDOC
## `string_ext.trim-end`

### Description
Trims trailing whitespace from the end of a string.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to trim.

### Return Value
- **string** - The string with trailing whitespace trimmed from the end.

### Example
```scss
@debug string_ext.trim-end('  foo bar  '); // "  foo bar"
```
```

--------------------------------

### string_ext.replace-end

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Replaces a specified suffix at the end of a string with a new replacement string.

```APIDOC
## `string_ext.replace-end`

### Description
Returns a new string that replaces a suffix at the end of the string.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to be searched.
- **$suffix** (string) - Required - The suffix string to replace.
- **$replacement** (string) - Required - The value to replace the suffix.

### Return Value
- **string** - The string with the suffix trimmed from the end.

### Example
```scss
@debug string_ext.replace-end('var(--foo)', ')', ''); // "var(--foo"
```
```

--------------------------------

### Toggle Menu with ID Reference

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/usage.html

Use this method when you have direct access to the DOM elements by their IDs. It toggles the menu's open state on click.

```javascript
const anchorEl = document.body.querySelector("#usage-anchor");
const menuEl = document.body.querySelector("#usage-menu");

anchorEl.addEventListener("click", () => {
  menuEl.open = !menuEl.open;
});
```

--------------------------------

### Remove-Only Input Chips

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Shows how to create input chips that are only removable. These are useful when the chip's primary function is to represent a selected item that can be deselected.

```html
<h3>Favorite movies</h3>
<md-chip-set>
  <md-input-chip label="Star Wars" remove-only></md-input-chip>
  <md-input-chip label="Star Trek" remove-only></md-input-chip>
</md-chip-set>
```

--------------------------------

### Text Field with Required Indicator and Error Text

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Sets up a filled text field with a required indicator and error text, which is displayed when the field is not filled.

```html
<md-filled-text-field
    label="Name"
    required
    supporting-text="*required"
    error-text="Please fill out this field">
</md-filled-text-field>
```

--------------------------------

### MdFilledIconButton Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Provides a list of configurable properties for the MdFilledIconButton, including their types, default values, and descriptions. These properties control aspects like disabled states, link behavior, toggle functionality, and form submission.

```APIDOC
## MdFilledIconButton Properties

### Properties

- **softDisabled** (boolean) - Optional - Defaults to `false`. "Soft-disables" the icon button (disabled but still focusable).
- **flipIconInRtl** (boolean) - Optional - Defaults to `false`. Flips the icon if it is in an RTL context at startup.
- **href** (string) - Optional - Defaults to `''`. Sets the underlying `HTMLAnchorElement`'s `href` resource attribute.
- **download** (string) - Optional - Defaults to `''`. The filename to use when downloading the linked resource. This is only applicable when the icon button is used as a link (`href` is set).
- **target** (string) - Optional - Defaults to `''`. Sets the underlying `HTMLAnchorElement`'s `target` attribute.
- **ariaLabelSelected** (string) - Optional - Defaults to `''`. The `aria-label` of the button when the button is toggleable and selected.
- **toggle** (boolean) - Optional - Defaults to `false`. When true, the button will toggle between selected and unselected states.
- **selected** (boolean) - Optional - Defaults to `false`. Sets the selected state.
- **name** (string) - Optional - Defaults to `undefined`. The HTML name to use in form submission.
- **disabled** (boolean) - Optional - Defaults to `undefined`. Whether or not the element is disabled.
- **type** (string) - Optional - Defaults to `'submit'`. A string indicating the form submission behavior of the element.
- **value** (string) - Optional - Defaults to `''`. The value of the button.
- **form** (HTMLFormElement) - Optional - Defaults to `undefined`. The associated form element with which this element's value will submit.
- **labels** (NodeList) - Optional - Defaults to `undefined`. The labels this element is associated with.
```

--------------------------------

### Basic Icon Button with Aria Label

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Use the `aria-label` attribute to provide a descriptive label for icon buttons when the icon alone is not sufficient.

```html
<md-icon-button aria-label="Search for Contact">
  <md-icon>search</md-icon>
</md-icon-button>
```

--------------------------------

### map_ext.difference

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Compares two maps and returns a list of keys where the second map diverges from the first. Divergence includes keys present in the second map but not the first, or keys with different values.

```APIDOC
## `map_ext.difference`

### Description
Compares two maps and returns a list of keys where the second map diverges from the first. Divergence occurs when a key exists in the second map but not the first, or when a key exists in both maps but with different values.

### Parameters
#### Path Parameters
- **$mapA** (map) - Required - The reference map.
- **$mapB** (map) - Required - The map to compare against the reference.

### Return Value
- **list** - A list of keys where $mapB diverges from $mapA.

### Example
```scss
$mapA: ('foo': red, 'bar': yellow, 'baz': 10);
$mapB: ('foo': red, 'bar': green,  'baz': 10, 'fooBar': blue);

$differences: map_ext.difference($mapA, $mapB);
@debug $differences; // ('bar', 'fooBar')
```
```

--------------------------------

### MdOutlinedIconButton Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Defines the configurable properties for the `md-outlined-icon-button` component, including attributes, types, default values, and descriptions.

```APIDOC
## MdOutlinedIconButton Properties

### Description

Provides a list of properties that can be configured for the `md-outlined-icon-button` component. These properties control the button's appearance, behavior, and accessibility.

### Properties

#### Property Details

- **softDisabled** (boolean) - `false` - "Soft-disables" the icon button (disabled but still focusable). Use this when an icon button needs increased visibility when disabled. See https://www.w3.org/WAI/ARIA/apg/practices/keyboard-interface/#kbd_disabled_controls for more guidance on when this is needed.
- **flipIconInRtl** (boolean) - `false` - Flips the icon if it is in an RTL context at startup.
- **href** (string) - `''` - Sets the underlying `HTMLAnchorElement`'s `href` resource attribute.
- **download** (string) - `''` - The filename to use when downloading the linked resource. If not specified, the browser will determine a filename. This is only applicable when the icon button is used as a link (`href` is set).
- **target** (string) - `''` - Sets the underlying `HTMLAnchorElement`'s `target` attribute.
- **ariaLabelSelected** (string) - `''` - The `aria-label` of the button when the button is toggleable and selected.
- **toggle** (boolean) - `false` - When true, the button will toggle between selected and unselected states.
- **selected** (boolean) - `false` - Sets the selected state. When false, displays the default icon. When true, displays the selected icon, or the default icon If no `slot="selected"` icon is provided.
- **name** (string) - `undefined` - The HTML name to use in form submission.
- **disabled** (boolean) - `undefined` - Whether or not the element is disabled.
- **type** (string) - `'submit'` - A string indicating the form submission behavior of the element. - submit: The element submits the form. This is the default value if the attribute is not specified, or if it is dynamically changed to an empty or invalid value. - reset: The element resets the form. - button: The element does nothing.
- **value** (string) - `''` - The value of the button. When combined with a `name`, the submitting button's name/value will be added to the form.
- **form** (HTMLFormElement) - `undefined` - The associated form element with which this element's value will submit.
- **labels** (NodeList) - `undefined` - The labels this element is associated with.
```

--------------------------------

### Define Typeface Reference Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/theming/README.md

Define `--md-ref-typeface` tokens to globally change font families and weights across all system and component tokens. Ensure the specified fonts are available.

```css
:root {
  --md-ref-typeface-brand: 'Open Sans';
  --md-ref-typeface-plain: 'Open Sans';
}
```

--------------------------------

### MdMenu Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

The MdMenu component exposes several properties that can be set to control its behavior, such as anchoring, positioning, animations, and visibility.

```APIDOC
## MdMenu Properties

### anchor
- **Type**: `string`
- **Attribute**: `anchor`
- **Default**: `''`
- **Description**: The ID of the element in the same root node in which the menu should align to. Overrides setting `anchorElement = elementReference`. NOTE: anchor or anchorElement must either be an HTMLElement or resolve to an HTMLElement in order for menu to open.

### positioning
- **Type**: `"absolute" | "fixed" | "document" | "popover"`
- **Attribute**: `positioning`
- **Default**: `'absolute'`
- **Description**: Whether the positioning algorithm should calculate relative to the parent of the anchor element (`absolute`), relative to the window (`fixed`), or relative to the document (`document`). `popover` will use the popover API to render the menu in the top-layer. If your browser does not support the popover API, it will fall back to `fixed`. NOTE: Fixed menus will not scroll with the page and will be fixed to the window instead.

### quick
- **Type**: `boolean`
- **Attribute**: `quick`
- **Default**: `false`
- **Description**: Skips the opening and closing animations.

### hasOverflow
- **Type**: `boolean`
- **Attribute**: `has-overflow`
- **Default**: `false`
- **Description**: Displays overflow content like a submenu. Not required in most cases when using `positioning="popover"`. NOTE: This may cause adverse effects if you set `md-menu {max-height:...}` and have items overflowing items in the "y" direction.

### open
- **Type**: `boolean`
- **Attribute**: `open`
- **Default**: `false`
- **Description**: Opens the menu and makes it visible. Alternative to the `.show()` and `.close()` methods.

### xOffset
- **Type**: `number`
- **Attribute**: `x-offset`
- **Default**: `0`
- **Description**: Offsets the menu's inline alignment from the anchor by the given number in pixels. This value is direction aware and will follow the LTR / RTL direction.

### yOffset
- **Type**: `number`
- **Attribute**: `y-offset`
- **Default**: `0`
- **Description**: Offsets the menu's block alignment from the anchor by the given number in pixels.

### noHorizontalFlip
- **Type**: `boolean`
- **Attribute**: `no-horizontal-flip`
- **Default**: `false`
- **Description**: Disable the `flip` behavior that usually happens on the horizontal axis when the surface would render outside the viewport.

### noVerticalFlip
- **Type**: `boolean`
- **Attribute**: `no-vertical-flip`
- **Default**: `false`
- **Description**: Disable the `flip` behavior that usually happens on the vertical axis when the surface would render outside the viewport.

### typeaheadDelay
- **Type**: `number`
- **Attribute**: `typeahead-delay`
- **Default**: `200`
- **Description**: The max time between the keystrokes of the typeahead menu behavior before it clears the typeahead buffer.

### anchorCorner
- **Type**: `Corner`
- **Attribute**: `anchor-corner`
- **Default**: `Corner.END_START`
- **Description**: The corner of the anchor which to align the menu in the standard logical property style of <block>-<inline> e.g. `'end-start'`. NOTE: This value may not be respected by the menu positioning algorithm if the menu would render outisde the viewport. Use `no-horizontal-flip` or `no-vertical-flip` to force the usage of the value.

### menuCorner
- **Type**: `Corner`
- **Attribute**: `menu-corner`
- **Default**: `Corner.START_START`
- **Description**: The corner of the menu which to align the anchor in the standard logical property style of <block>-<inline> e.g. `'start-start'`. NOTE: This value may not be respected by the menu positioning algorithm if the menu would render outisde the viewport. Use `no-horizontal-flip` or `no-vertical-flip` to force the usage of the value.

### stayOpenOnOutsideClick
- **Type**: `boolean`
- **Attribute**: `stay-open-on-outside-click`
- **Default**: `false`
- **Description**: Keeps the user clicks outside the menu. NOTE: clicking outside may still cause focusout to close the menu so see `stayOpenOnFocusout`.
```

--------------------------------

### Apply Custom Styles to Material Components

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/checkbox/theming.html

Use CSS custom properties to override default styles and apply system or component tokens. Ensure the selector targets the component correctly.

```css
.styled-example {
  background-color: white;
  border-radius: 28px;
  /* System tokens */
  --md-sys-color-primary: #006a6a;
  --md-sys-color-on-primary: #ffffff;
  --md-sys-color-on-surface-variant: #3f4948;
  /* Component tokens */
  --md-checkbox-container-shape: 0px;
}
```

--------------------------------

### MdSelectOption Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Events fired by MdSelectOption elements to communicate with the parent MdSelect.

```APIDOC
## MdSelectOption Events

### Description
Events fired by MdSelectOption elements.

### Events

- `close-menu` (Type: `CustomEvent<{initiator: SelectOption, reason: Reason, itemPath: SelectOption[]}>`, Bubbles: Yes, Composed: Yes)
  Closes the encapsulating menu on closable interaction.

- `request-selection` (Type: `Event`, Bubbles: Yes, Composed: Yes)
  Requests the parent md-select to select this element (and deselect others if single-selection) when `selected` changed to `true`.

- `request-deselection` (Type: `Event`, Bubbles: Yes, Composed: Yes)
  Requests the parent md-select to deselect this element when `selected` changed to `false`.
```

--------------------------------

### Load Material Symbols Sharp Font

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

Include this link tag in your HTML to load the Material Symbols Sharp font. This is required before using sharp icons.

```html
<link href="https://fonts.googleapis.com/icon?family=Material+Symbols+Sharp" rel="stylesheet">
```

--------------------------------

### Open Popover Menu on Click

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/usage-popover.html

Attaches a click event listener to an anchor element to toggle the open state of a popover menu. Ensure the anchor and menu elements are present in the DOM.

```javascript
const anchorEl = document.body.querySelector("#usage-popover-anchor");
const menuEl = document.body.querySelector("#usage-popover");

anchorEl.addEventListener("click", () => {
  menuEl.open = !menuEl.open;
});
```

--------------------------------

### MdSlider API

Source: https://github.com/material-components/material-web/blob/main/docs/components/slider.md

The MdSlider component exposes a rich API for controlling its behavior and appearance. Users can configure properties like min, max, value, range, and more. It also supports form integration through specific methods and emits standard input and change events.

```APIDOC
## MdSlider

### Description
Represents a Material Design slider component that allows users to select a value or a range of values.

### Properties

#### `min`
- **Type**: `number`
- **Default**: `0`
- **Description**: The slider minimum value.

#### `max`
- **Type**: `number`
- **Default**: `100`
- **Description**: The slider maximum value.

#### `value`
- **Type**: `number`
- **Default**: `undefined`
- **Description**: The slider value displayed when range is false.

#### `valueStart`
- **Type**: `number`
- **Default**: `undefined`
- **Description**: The slider start value displayed when range is true.

#### `valueEnd`
- **Type**: `number`
- **Default**: `undefined`
- **Description**: The slider end value displayed when range is true.

#### `valueLabel`
- **Type**: `string`
- **Default**: `''`
- **Description**: An optional label for the slider's value displayed when range is false; if not set, the label is the value itself.

#### `valueLabelStart`
- **Type**: `string`
- **Default**: `''`
- **Description**: An optional label for the slider's start value displayed when range is true; if not set, the label is the valueStart itself.

#### `valueLabelEnd`
- **Type**: `string`
- **Default**: `''`
- **Description**: An optional label for the slider's end value displayed when range is true; if not set, the label is the valueEnd itself.

#### `ariaLabelStart`
- **Type**: `string`
- **Default**: `''`
- **Description**: Aria label for the slider's start handle displayed when range is true.

#### `ariaValueTextStart`
- **Type**: `string`
- **Default**: `''`
- **Description**: Aria value text for the slider's start value displayed when range is true.

#### `ariaLabelEnd`
- **Type**: `string`
- **Default**: `''`
- **Description**: Aria label for the slider's end handle displayed when range is true.

#### `ariaValueTextEnd`
- **Type**: `string`
- **Default**: `''`
- **Description**: Aria value text for the slider's end value displayed when range is true.

#### `step`
- **Type**: `number`
- **Default**: `1`
- **Description**: The step between values.

#### `ticks`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Whether or not to show tick marks.

#### `labeled`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Whether or not to show a value label when activated.

#### `range`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Whether or not to show a value range. When false, the slider displays a slideable handle for the value property; when true, it displays slideable handles for the valueStart and valueEnd properties.

#### `name`
- **Type**: `string`
- **Default**: `undefined`
- **Description**: The HTML name to use in form submission.

#### `disabled`
- **Type**: `boolean`
- **Default**: `undefined`
- **Description**: Whether or not the element is disabled.

#### `nameStart`
- **Type**: `string`
- **Default**: `undefined`

#### `nameEnd`
- **Type**: `string`
- **Default**: `undefined`

#### `form`
- **Type**: `HTMLFormElement`
- **Default**: `undefined`
- **Description**: The associated form element with which this element's value will submit.

#### `labels`
- **Type**: `NodeList`
- **Default**: `undefined`
- **Description**: The labels this element is associated with.

### Methods

#### `formResetCallback()`
- **Parameters**: None
- **Returns**: `void`
- **Description**: Callback for form reset.

#### `formStateRestoreCallback(state)`
- **Parameters**:
  - `state`: The state to restore.
- **Returns**: `void`
- **Description**: Callback for form state restoration.

### Events

#### `change`
- **Type**: `Event`
- **Bubbles**: Yes
- **Composed**: No
- **Description**: The native `change` event on [`<input>`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/change_event).

#### `input`
- **Type**: `InputEvent`
- **Bubbles**: Yes
- **Composed**: Yes
- **Description**: The native `input` event on [`<input>`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/input_event).
```

--------------------------------

### Text Field Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Provides methods to interact with the text field's content and state.

```APIDOC
## select

### Description
Selects all the text in the text field.

### Method
`select()`

### Parameters
_None_

### Returns
`void`

### Example
```javascript
textField.select();
```

## setRangeText

### Description
Sets a range of text in the text field.

### Method
`setRangeText(args)`

### Parameters
- **args**: The arguments for setting the text range.

### Returns
`void`

## setSelectionRange

### Description
Sets the start and end positions of a selection in the text field.

### Method
`setSelectionRange(start, end, direction)`

### Parameters
- **start** (number) - The starting position of the selection.
- **end** (number) - The ending position of the selection.
- **direction** (string) - The direction of the selection.

### Returns
`void`

### Example
```javascript
textField.setSelectionRange(0, 5, 'forward');
```

## showPicker

### Description
Shows the browser picker for an input element of type "date", "time", etc.

### Method
`showPicker()`

### Parameters
_None_

### Returns
`void`

### Example
```javascript
textField.showPicker();
```

## stepDown

### Description
Decrements the value of a numeric type text field by `step` or `n` `step` number of times.

### Method
`stepDown(stepDecrement)`

### Parameters
- **stepDecrement** (number) - The number of steps to decrement by.

### Returns
`void`

### Example
```javascript
textField.stepDown(2);
```

## stepUp

### Description
Increments the value of a numeric type text field by `step` or `n` `step` number of times.

### Method
`stepUp(stepIncrement)`

### Parameters
- **stepIncrement** (number) - The number of steps to increment by.

### Returns
`void`

### Example
```javascript
textField.stepUp(3);
```

## reset

### Description
Resets the text field to its default value.

### Method
`reset()`

### Parameters
_None_

### Returns
`void`

### Example
```javascript
textField.reset();
```

## formResetCallback

### Description
Callback for form reset.

### Method
`formResetCallback()`

### Parameters
_None_

### Returns
`void`

## formStateRestoreCallback

### Description
Callback for form state restoration.

### Method
`formStateRestoreCallback(state)`

### Parameters
- **state**: The state to restore.

### Returns
`void`

## checkValidity

### Description
Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch an `invalid` event.

### Method
`checkValidity()`

### Parameters
_None_

### Returns
`boolean`

### Example
```javascript
const isValid = textField.checkValidity();
```

## reportValidity

### Description
Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch a cancelable `invalid` event. If not canceled, a the current `validationMessage` will be reported to the user.

### Method
`reportValidity()`

### Parameters
_None_

### Returns
`boolean`

### Example
```javascript
const isValid = textField.reportValidity();
```

## setCustomValidity

### Description
Sets the element's constraint validation error message. When set to a non-empty string, `validity.customError` will be true and `validationMessage` will display the provided error. Use this method to customize error messages reported.

### Method
`setCustomValidity(error)`

### Parameters
- **error** (string) - The custom error message.

### Returns
`void`

### Example
```javascript
textField.setCustomValidity('Please enter a valid email.');
```
```

--------------------------------

### Divider Accessibility

Source: https://github.com/material-components/material-web/blob/main/docs/components/divider.md

Guidelines for making dividers accessible using ARIA roles.

```APIDOC
## Accessibility

Dividers are decorative by default and not announced by assistive technology.

Add a `role="separator"` attribute to non-decorative dividers that should be announced.

```html
<ul>
  <li>Item one</li>
  <md-divider inset></md-divider>
  <li>Item two</li>
  <md-divider role="separator"></md-divider>
  <li>Item three</li>
  <md-divider inset></md-divider>
  <li>Item four</li>
</ul>
```
```

--------------------------------

### Tabs with Icons and Labels

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Tabs can display an icon in addition to a label. Use the 'slot="icon"' on an md-icon element within the tab for this.

```html
<md-tabs>
  <md-primary-tab>
    <md-icon slot="icon">piano</md-icon>
    Keyboard
  </md-primary-tab>
  <md-primary-tab>
    <md-icon slot="icon">tune</md-icon>
    Guitar
  </md-primary-tab>
</md-tabs>
```

--------------------------------

### list_ext.difference

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Returns the difference between two lists. All items in the first list that are not present in the second list are returned.

```APIDOC
## list_ext.difference

### Description
Returns the difference between two lists. All items in $listA that are not in $listB are returned.

### Method
SCSS Function

### Endpoint
N/A (Sass Module Function)

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
@use '@material/web/sass/ext/list_ext';

$listA: ('apple', 'banana', 'cherry', 'date');
$listB: ('banana', 'cherry', 'apple');
$listC: ('apple', 'banana', 'date');

@debug list_ext.difference($listA, $listB); // ('date')
@debug list_ext.difference($listA, $listC); // ('cherry')
```

### Response
#### Success Response (200)
- **list** - All items in $listA that are not in $listB.

#### Response Example
```scss
// ('date')
// ('cherry')
```

### Error Handling
N/A (Assumes valid list inputs)
```

--------------------------------

### MdList

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Represents a Material Design list. It manages a collection of list items and provides methods for navigating and activating them.

```APIDOC
## MdList <md-list>

### Description
Represents a Material Design list. It manages a collection of list items and provides methods for navigating and activating them.

### Properties

- **items** (ListItem[]) - Optional - The list of items contained within the MdList.

### Methods

- **activateNextItem**(): ListItem - Activates the next item in the list. If at the end of the list, the first item will be activated.
- **activatePreviousItem**(): ListItem - Activates the previous item in the list. If at the start of the list, the last item will be activated.
```

--------------------------------

### Selecting a Tab with 'active' Attribute

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Add the 'active' attribute to an md-primary-tab or md-secondary-tab to set it as the initially selected tab.

```html
<md-tabs>
  <md-primary-tab>Video</md-primary-tab>
  <md-primary-tab active>Photos</md-primary-tab>
  <md-primary-tab>Audio</md-primary-tab>
</md-tabs>
```

--------------------------------

### Lowered FAB with Icon

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Implement a Floating Action Button with a lowered elevation using the `lowered` attribute. This is useful for specific visual contexts.

```html
<md-fab lowered aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### MdMenuItem Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/menu.md

Events fired by the md-menu-item component.

```APIDOC
## MdMenuItem Events

### `close-menu`
- **Type**: `CustomEvent<{initiator: SelectOption, reason: Reason, itemPath: SelectOption[]}>`
- **Bubbles**: Yes
- **Composed**: Yes
- **Description**: Closes the encapsulating menu on closable interaction.
```

--------------------------------

### Themed Linear Progress Indicator

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Apply custom theming to a linear progress indicator using CSS custom properties. Ensure the styles are applied within a scope that affects the md-linear-progress element.

```html
<style>
:root {
  --md-linear-progress-track-height: 8px;
  --md-linear-progress-track-shape: 8px;
  --md-linear-progress-active-indicator-height: 8px;
  --md-sys-color-primary: #006A6A;
  --md-sys-color-surface-container-highest: #DDE4E3;
}
</style>

<md-linear-progress value="0.5"></md-linear-progress>
```

--------------------------------

### throw.get-error

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Checks if any of the provided values are error strings. Useful for testing error behavior with sass-true.

```APIDOC
## `throw.get-error`

### Description
Checks if any of the provided values are error strings. Useful for testing error behavior with sass-true.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
N/A

### Response
#### Success Response (200)
- **string|boolean**: The error string if any value is an error, or false otherwise.

#### Response Example
```scss
// A function that may return an "ERROR:" string in a test.
@function get-value($map, $key) {
  @if meta.type-of($map) != 'map' {
    // Identical to `@error 'ERROR: Arg is not a map'` outside of tests.
    @return throw.error('Arg is not a map');
  }
  @return map.get($map, $key);
}

// A function that needs to handle potential errors from other functions.
@function mix-primary-on-surface($values) {
  $primary: get-value($values, 'primary');
  $surface: get-value($values, 'surface');
  $error: throw.get-error($primary, $surface);
  @if $error {
    // Return early to guard logic against additional errors since
    // $primary or $surface may be a string instead of a color.
    @return $error;
  }

  @return color.mix($primary, $surface, 10%);
}
```

```scss
// In a `sass-true` test, `throw.get-error()` can be used to assert that
// an error is thrown.
@use 'true' as test with ($catch-errors: true);

@include test.describe('module.get-value()') {
  @include test.it('throws an error if the value is not a map') {
    $result: module.get-value('not a map', 'primary');
    @include test.assert-truthy(throw.get-error($result), '$result is an error');
  }
}
```
```

--------------------------------

### Themed Outlined Text Field

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Demonstrates theming an outlined text field by customizing shape, typography, and colors using CSS custom properties.

```html
<style>
:root {
  --md-outlined-text-field-container-shape: 0px;
  --md-sys-typescale-body-large: 400 1rem system-ui;
  --md-sys-color-primary: #006a6a;
  --md-outlined-text-field-label-text-color: #3f4948;
  --md-outlined-text-field-input-text-color: #161d1d;
}
</style>

<md-outlined-text-field label="Outlined" value="Value"></md-outlined-text-field>
```

--------------------------------

### Text Field Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Methods available on the Text Field component for programmatic control.

```APIDOC
## Text Field Methods

This section details the available methods for the Text Field component.

### Methods

- **`select()`** - `void` - Selects all the text in the text field.
- **`setRangeText(args)`** - `void` - 
- **`setSelectionRange(start, end, direction)`** - `void` - Sets the start and end positions of a selection in the text field.
- **`showPicker()`** - `void` - Shows the browser picker for an input element of type "date", "time", etc.
- **`stepDown(stepDecrement)`** - `void` - Decrements the value of a numeric type text field by `step` or `n` `step` number of times.
- **`stepUp(stepIncrement)`** - `void` - Increments the value of a numeric type text field by `step` or `n` `step` number of times.
- **`reset()`** - `void` - Reset the text field to its default value.
- **`formResetCallback()`** - `void` - 
- **`formStateRestoreCallback(state)`** - `void` - 
- **`checkValidity()`** - `boolean` - Checks the element's constraint validation and returns true if the element is valid or false if not.
- **`reportValidity()`** - `boolean` - Checks the element's constraint validation and returns true if the element is valid or false if not.
- **`setCustomValidity(error)`** - `void` - Sets the element's constraint validation error message.
```

--------------------------------

### Accessible Progress Indicators

Source: https://github.com/material-components/material-web/blob/main/docs/components/progress.md

Add an 'aria-label' attribute to progress indicators to provide a descriptive name for screen readers. This is crucial for accessibility.

```html
<md-circular-progress value="0.5" aria-label="Page refresh progress"></md-circular-progress>

<md-linear-progress value="0.5" aria-label="Download progress"></md-linear-progress>
```

--------------------------------

### Chip Set with aria-labelledby

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

Use aria-labelledby to associate a chip set with a heading for screen reader users. Chips can also have individual aria-labels for more descriptive labels.

```html
<h3 id="dates-label">Dates</h3>
<md-chip-set aria-labelledby="dates-label">
  <md-filter-chip label="Mon" aria-label="Monday"></md-filter-chip>
  <md-filter-chip label="Tue" aria-label="Tuesday"></md-filter-chip>
  <md-filter-chip label="Wed" aria-label="Wednesday"></md-filter-chip>
</md-chip-set>
```

--------------------------------

### Switch Accessibility with aria-label

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Use the `aria-label` attribute for switches that lack visible labels or require more descriptive labels for accessibility.

```html
<md-switch aria-label="Lights"></md-switch>

<label>
  All
  <md-switch aria-label="All notifications"></md-switch>
</label>
```

--------------------------------

### MdFilledSelect Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

The MdFilledSelect component exposes various properties to control its functionality, including required state, error handling, labeling, and menu positioning.

```APIDOC
## MdFilledSelect Properties

### Description

Provides a list of properties for the `md-filled-select` component.

### Properties

- **quick** (boolean) - Optional - Opens the menu synchronously with no animation.
- **required** (boolean) - Optional - Whether or not the select is required.
- **errorText** (string) - Optional - The error message that replaces supporting text when `error` is true. If `errorText` is an empty string, then the supporting text will continue to show. This error message overrides the error message displayed by `reportValidity()`.
- **label** (string) - Optional - The floating label for the field.
- **noAsterisk** (boolean) - Optional - Disables the asterisk on the floating label, when the select is required.
- **supportingText** (string) - Optional - Conveys additional information below the select, such as how it should be used.
- **error** (boolean) - Optional - Gets or sets whether or not the select is in a visually invalid state. This error state overrides the error state controlled by `reportValidity()`.
- **menuPositioning** (string) - Optional - Whether or not the underlying md-menu should be position: fixed to display in a top-level manner, or position: absolute. position:fixed is useful for cases where select is inside of another element with stacking context and hidden overflows such as `md-dialog`.
- **clampMenuWidth** (boolean) - Optional - Clamps the menu-width to the width of the select.
- **typeaheadDelay** (number) - Optional - The max time between the keystrokes of the typeahead select / menu behavior before it clears the typeahead buffer.
- **hasLeadingIcon** (boolean) - Optional - Whether or not the text field has a leading icon. Used for SSR.
- **displayText** (string) - Optional - Text to display in the field. Only set for SSR.
- **menuAlign** (string) - Optional - Whether the menu should be aligned to the start or the end of the select's textbox.
- **value** (string) - Optional - The current value of the select.
- **selectedIndex** (number) - Optional - The index of the currently selected option.
- **name** (string) - Optional - The HTML name to use in form submission.
- **disabled** (boolean) - Optional - Whether or not the element is disabled.
- **options** (SelectOption[]) - Optional - An array of SelectOption objects representing the available options.
- **selectedOptions** (SelectOption[]) - Optional - An array of SelectOption objects representing the currently selected options.
- **form** (HTMLFormElement) - Optional - The associated form element with which this element's value will submit.
- **labels** (NodeList) - Optional - The labels this element is associated with.
- **validity** (ValidityState) - Optional - Returns a ValidityState object that represents the validity states of the element.
- **validationMessage** (string) - Optional - Returns a validation error message or an empty string if the element is valid.
- **willValidate** (boolean) - Optional - Returns whether an element will successfully validate based on forms validation rules and constraints. Disabled and readonly elements will not validate.
```

--------------------------------

### MdFab Component

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

The MdFab component represents a standard Floating Action Button. It supports various properties to control its appearance, such as variant, size, label, and elevation.

```APIDOC
## MdFab <md-fab>

### Description
Represents a standard Floating Action Button.

### Properties

- **variant** (FabVariant) - Optional - The FAB color variant to render. Defaults to 'surface'.
- **size** (FabSize) - Optional - The size of the FAB. Defaults to 'medium'. Note: Branded FABs cannot be sized to 'small', and Extended FABs do not have different sizes.
- **label** (string) - Optional - The text to display on the FAB. Defaults to an empty string.
- **lowered** (boolean) - Optional - Lowers the FAB's elevation. Defaults to false.
```

--------------------------------

### Themed Filled Text Field

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Demonstrates theming a filled text field by customizing shape, typography, and colors using CSS custom properties.

```html
<style>
:root {
  --md-filled-text-field-container-shape: 0px;
  --md-sys-typescale-body-large: 400 1rem system-ui;
  --md-sys-color-primary: #006a6a;
  --md-sys-color-surface-container-highest: #e0e3e2;
  --md-filled-text-field-label-text-color: #3f4948;
  --md-filled-text-field-input-text-color: #161d1d;
}
</style>

<md-filled-text-field label="Filled" value="Value">
</md-filled-text-field>
```

--------------------------------

### Tab Panels with ARIA Controls

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Associate tab panels with their respective tabs using `id` and `aria-controls` attributes. Tab panels should have `role="tabpanel"` and be labeled with `aria-label` or `aria-labelledby`.

```html
<md-tabs aria-label="Content to view">
  <md-primary-tab id="photos-tab" aria-controls="photos-panel">
    Photos
  </md-primary-tab>
  <md-primary-tab id="videos-tab" aria-controls="videos-panel">
    Videos
  </md-primary-tab>
  <md-primary-tab id="music-tab" aria-controls="music-panel">
    Music
  </md-primary-tab>
</md-tabs>

<div id="photos-panel" role="tabpanel" aria-labelledby="photos-tab">
  ...
</div>
<div id="videos-panel" role="tabpanel" aria-labelledby="videos-tab" hidden>
  ...
</div>
<div id="music-panel" role="tabpanel" aria-labelledby="music-tab" hidden>
  ...
</div>
```

--------------------------------

### Toggle Menu Open State

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/menu/usage-submenu.html

Attaches a click listener to an anchor element to toggle the open state of a menu element. Ensure the menu element has an ID that matches the selector.

```javascript
const anchorEl = document.body.querySelector("#usage-submenu-anchor");
const menuEl = document.body.querySelector("#usage-submenu");

anchorEl.addEventListener("click", () => {
  menuEl.open = !menuEl.open;
});
```

--------------------------------

### var.set-fallback

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Creates a new custom property var() string and returns it with the specified new fallback value.

```APIDOC
## `set-fallback`

### Description
Creates a new custom property `var()` string and returns it with the specified new fallback value.

### Method
SCSS Function

### Endpoint
N/A

### Parameters
#### Arguments
- **$var** (string) - Required - A custom property `var()` string.
- **$new-fallback** (*) - Required - The new fallback value. May be null to remove a value.

### Request Example
```scss
$var: var(--foo, var(--bar, 8px));
$new-var: set-fallback($var, 16px);
@debug $new-var; // "var(--foo, 16px)"
```

### Response
#### Return Value
- **string** - A custom property `var()` string with the new fallback value.

#### Response Example
```scss
"var(--foo, 16px)"
```

### Throws
- If the value is not a custom property.
```

--------------------------------

### MdCheckbox Methods

Source: https://github.com/material-components/material-web/blob/main/docs/components/checkbox.md

These are the callable methods for the MdCheckbox component.

```APIDOC
## MdCheckbox Methods

### Description

These methods provide functionality for interacting with and validating the MdCheckbox component.

### Methods

- **formResetCallback** (_None_) - `void` - Resets the form associated with the checkbox.
- **formStateRestoreCallback** (`state`) - `void` - Restores the state of the checkbox.
- **checkValidity** (_None_) - `boolean` - Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch an `invalid` event.
- **reportValidity** (_None_) - `boolean` - Checks the element's constraint validation and returns true if the element is valid or false if not. If invalid, this method will dispatch a cancelable `invalid` event. If not canceled, a the current `validationMessage` will be reported to the user.
- **setCustomValidity** (`error`) - `void` - Sets the element's constraint validation error message. When set to a non-empty string, `validity.customError` will be true and `validationMessage` will display the provided error. Use this method to customize error messages reported.
```

--------------------------------

### Text Field with Prefix and Suffix Text

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Adds prefix and suffix text to an outlined number field to provide context for the input value, such as currency.

```html
<md-outlined-text-field
    label="Dollar amount"
    type="number"
    value="0"
    prefix-text="$"
    suffix-text=".00">
</md-outlined-text-field>
```

--------------------------------

### type.matches

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Checks if a value matches a given type string. Supports multiple types separated by '|'.

```APIDOC
## `type.matches`

### Description
Checks if a value matches a given type string. Supports multiple types separated by '|'.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
@function is-empty($value) {
  @if type.matches($value, 'list|map') {
    @return list.length($value) == 0;
  }
  @if type.matches($value, 'string') {
    @return $value == '';
  }
  @return type.matches($value, 'null');
}
```

### Response
#### Success Response (200)
- **boolean**: True if the value matches the type string.

#### Response Example
N/A
```

--------------------------------

### MdListItem

Source: https://github.com/material-components/material-web/blob/main/docs/components/list.md

Represents an individual item within an MdList. It can be configured as text, a link, or a button, and supports various states like disabled.

```APIDOC
## MdListItem <md-list-item>

### Description
Represents an individual item within an MdList. It can be configured as text, a link, or a button, and supports various states like disabled.

### Properties

- **disabled** (boolean) - Optional - Disables the item and makes it non-selectable and non-interactive. Defaults to `false`.
- **type** (ListItemType) - Optional - Sets the behavior of the list item, defaults to "text". Change to "link" or "button" for interactive items.
- **href** (string) - Optional - Sets the underlying `HTMLAnchorElement`'s `href` resource attribute.
- **target** (string) - Optional - Sets the underlying `HTMLAnchorElement`'s `target` attribute when `href` is set. Possible values: `""`, `"_blank"`, `"_parent"`, `"_self"`, `"_top"`.

### Methods

- **click**(): void - Programmatically triggers a click event on the list item.

### Events

- **request-activation**: Event - Requests the list to set `tabindex=0` on the item and focus it. Used internally for list keyboard navigation; most applications do not need to listen for this event. It is exposed for authors building their own list-item replacements or wrapping items in a custom controller.
```

--------------------------------

### MdOutlinedTextField Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Configuration properties for the `md-outlined-text-field` component, controlling its appearance, behavior, and validation.

```APIDOC
## MdOutlinedTextField <md-outlined-text-field>

### Description
Represents an outlined text field component that allows users to input text with Material Design styling.

### Properties
#### Path Parameters
- **error** (boolean) - Optional - Gets or sets whether or not the text field is in a visually invalid state. This error state overrides the error state controlled by `reportValidity()`.
- **errorText** (string) - Optional - The error message that replaces supporting text when `error` is true. If `errorText` is an empty string, then the supporting text will continue to show. This error message overrides the error message displayed by `reportValidity()`.
- **label** (string) - Optional - The floating Material label of the textfield component. It informs the user about what information is requested for a text field. It is aligned with the input text, is always visible, and it floats when focused or when text is entered into the textfield. This label also sets accessibilty labels, but the accessible label is overriden by `aria-label`.
- **noAsterisk** (boolean) - Optional - Disables the asterisk on the floating label, when the text field is required.
- **required** (boolean) - Optional - Indicates that the user must specify a value for the input before the owning form can be submitted and will render an error state when `reportValidity()` is invoked when value is empty. Additionally the floating label will render an asterisk `*` when true.
- **value** (string) - Optional - The current value of the text field. It is always a string.
- **prefixText** (string) - Optional - An optional prefix to display before the input value.
- **suffixText** (string) - Optional - An optional suffix to display after the input value.
- **hasLeadingIcon** (boolean) - Optional - Whether or not the text field has a leading icon. Used for SSR.
- **hasTrailingIcon** (boolean) - Optional - Whether or not the text field has a trailing icon. Used for SSR.
- **supportingText** (string) - Optional - Conveys additional information below the text field, such as how it should be used.
- **textDirection** (string) - Optional - Override the input text CSS `direction`. Useful for RTL languages that use LTR notation for fractions.
- **rows** (number) - Optional - The number of rows to display for a `type="textarea"` text field. Defaults to 2.
- **cols** (number) - Optional - The number of cols to display for a `type="textarea"` text field. Defaults to 20.
- **inputMode** (string) - Optional - Specifies how a URL should be processed when the input is to be typed.
- **max** (string) - Optional - Defines the greatest value in the range of permitted values.
- **maxLength** (number) - Optional - The maximum number of characters a user can enter into the text field. Set to -1 for none.
- **min** (string) - Optional - Defines the most negative value in the range of permitted values.
- **minLength** (number) - Optional - The minimum number of characters a user can enter into the text field. Set to -1 for none.
- **noSpinner** (boolean) - Optional - When true, hide the spinner for `type="number"` text fields.
- **pattern** (string) - Optional - A regular expression that the text field's value must match to pass constraint validation.
```

--------------------------------

### Standard Extended FAB

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Extended FABs can include a label for additional emphasis and can omit their icon.

```html
<md-fab label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### MdChipSet Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

The MdChipSet component manages a collection of chips. It exposes a `chips` property to access the array of chips within the set.

```APIDOC
## MdChipSet

### Description
Manages a collection of chips.

### Properties
#### Property
- `chips` (Chip[]) - Optional - The array of chips within the chip set.
```

--------------------------------

### MdSwitch Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Events fired by the MdSwitch component in response to user interaction.

```APIDOC
## MdSwitch Events

### `input`
- **Type**: `InputEvent`
- **Description**: Fired whenever `selected` changes due to user interaction.

### `change`
- **Type**: `Event`
- **Description**: Fired whenever `selected` changes due to user interaction.
```

--------------------------------

### Extended Branded FAB with Label

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

An extended branded FAB that includes a text label for additional emphasis. Always displays the logo icon.

```html
<md-branded-fab label="Add">
  <svg slot="icon" viewBox="0 0 36 36">
    <path fill="#34A853" d="M16 16v14h4V20z"></path>
    <path fill="#4285F4" d="M30 16H20l-4 4h14z"></path>
    <path fill="#FBBC05" d="M6 16v4h10l4-4z"></path>
    <path fill="#EA4335" d="M20 16V6h-4v14z"></path>
    <path fill="none" d="M0 0h36v36H0z"></path>
  </svg>
</md-branded-fab>
```

--------------------------------

### Create CSS custom property var() string

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `var.create` to generate a CSS `var()` string for a custom property, optionally including a fallback value. This function returns a string suitable for use in Sass or CSS.

```scss
var.create($name, $fallback) //=> string

/// @param {string} $name - The name of the custom property.
/// @param {*} $fallback [null] - Optional `var()` fallback value.
/// @return {string} A custom property `var()` string.
```

```scss
@debug var.create(--foo); // "var(--foo)"
@debug var.create(--foo, 8px); // "var(--foo, 8px)"
```

--------------------------------

### MdFilledIconButton Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Details the events dispatched by the MdFilledIconButton component, including their types and propagation behavior. These events are useful for reacting to user interactions, particularly when the button is in a toggleable state.

```APIDOC
## MdFilledIconButton Events

### Events

- **input** (InputEvent) - Bubbles: Yes, Composed: Yes - Dispatched when a toggle button toggles.
- **change** (Event) - Bubbles: Yes, Composed: No - Dispatched when a toggle button toggles.
```

--------------------------------

### Tabs with Icons Only

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Tabs can display only an icon without a label. Ensure the md-icon is placed within the tab and assigned to the 'icon' slot.

```html
<md-tabs>
  <md-primary-tab>
    <md-icon slot="icon">piano</md-icon>
  </md-primary-tab>
  <md-primary-tab>
    <md-icon slot="icon">tune</md-icon>
  </md-primary-tab>
</md-tabs>
```

--------------------------------

### Attach Focus Ring to Parent Element

Source: https://github.com/material-components/material-web/blob/main/docs/components/focus-ring.md

Use this method when the focus ring should be directly associated with the parent container of the element receiving focus. Ensure the parent has `position: relative`.

```html
<button style="position: relative">
  <md-focus-ring style="--md-focus-ring-shape: 8px"></md-focus-ring>
</button>
```

--------------------------------

### Check if two lists are equal

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Compares two lists for equality, ignoring element order. Use when the presence of elements is more important than their sequence.

```scss
list_ext.are-equal($listA, $listB) //=> boolean

/// @param {list} $listA - The first list to compare.
/// @param {list} $listB - The second list to compare.
/// @return {boolean} `true` if the lists contain the same elements, otherwise `false`.
```

```scss
$listA: ('apple', 'banana', 'cherry');
$listB: ('banana', 'cherry', 'apple');
$listC: ('apple', 'banana', 'date');

@debug list_ext.are-equal($listA, $listB); // true
@debug list_ext.are-equal($listA, $listC); // false
```

--------------------------------

### var.deep-set-fallback

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Creates a new custom property var() string and returns it with the specified new deep fallback value. If the provided var() string's fallback value is another var(), this function will set the final fallback value in the chain.

```APIDOC
## `deep-set-fallback`

### Description
Creates a new custom property `var()` string and returns it with the specified new deep fallback value. If the provided `var()` string's fallback value is another `var()`, this function will set the final fallback value in the chain.

### Method
SCSS Function

### Endpoint
N/A

### Parameters
#### Arguments
- **$var** (string) - Required - A custom property `var()` string.
- **$new-fallback** (*) - Required - The new fallback value. May be null to remove a value.

### Request Example
```scss
$var: var(--foo, var(--bar, 8px));
$new-var: var.deep-set-fallback($var, 16px);
@debug $new-var; // "var(--foo, var(--bar, 16px))"
```

### Response
#### Return Value
- **string** - A custom property `var()` string with the new deep fallback value.

#### Response Example
```scss
"var(--foo, var(--bar, 16px))"
```

### Throws
- If the value is not a custom property.
```

--------------------------------

### MdOutlinedIconButton Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Details the events emitted by the `md-outlined-icon-button` component, including event types, bubbling, and composition.

```APIDOC
## MdOutlinedIconButton Events

### Description

Lists the events that can be dispatched by the `md-outlined-icon-button` component, providing information about their type and propagation.

### Events

#### Event Details

- **input** (InputEvent) - Bubbles: Yes, Composed: Yes - Dispatched when a toggle button toggles.
- **change** (Event) - Bubbles: Yes, Composed: No - Dispatched when a toggle button toggles.
```

--------------------------------

### Include Inline JavaScript

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Includes JavaScript from an external file. This is useful for adding client-side interactivity or utility functions.

```html
{% inlinejs "inline/apply-saved-theme.js" %}
```

```html
{% inlinejs "ssr-utils/dsd-polyfill.js" %}
```

--------------------------------

### MdSwitch Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/switch.md

Properties that can be set on the MdSwitch component to control its behavior and appearance.

```APIDOC
## MdSwitch Properties

### `selected`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Puts the switch in the selected state and sets the form submission value to the `value` property.

### `icons`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Shows both the selected and deselected icons.

### `showOnlySelectedIcon`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: Shows only the selected icon, and not the deselected icon. If `true`, overrides the behavior of the `icons` property.

### `required`
- **Type**: `boolean`
- **Default**: `false`
- **Description**: When true, require the switch to be selected when participating in form submission.

### `value`
- **Type**: `string`
- **Default**: `'on'`
- **Description**: The value associated with this switch on form submission. `null` is submitted when `selected` is `false`.

### `name`
- **Type**: `string`
- **Default**: `undefined`
- **Description**: The HTML name to use in form submission.

### `disabled`
- **Type**: `boolean`
- **Default**: `undefined`
- **Description**: Whether or not the element is disabled.

### `form`
- **Type**: `HTMLFormElement`
- **Default**: `undefined`
- **Description**: The associated form element with which this element's value will submit.

### `labels`
- **Type**: `NodeList`
- **Default**: `undefined`
- **Description**: The labels this element is associated with.

### `validity`
- **Type**: `ValidityState`
- **Default**: `undefined`
- **Description**: Returns a ValidityState object that represents the validity states of the element.

### `validationMessage`
- **Type**: `string`
- **Default**: `undefined`
- **Description**: Returns a validation error message or an empty string if the element is valid.

### `willValidate`
- **Type**: `boolean`
- **Default**: `undefined`
- **Description**: Returns whether an element will successfully validate based on forms validation rules and constraints. Disabled and readonly elements will not validate.
```

--------------------------------

### Imperatively Attach Ripple

Source: https://github.com/material-components/material-web/blob/main/docs/components/ripple.md

Attach a ripple to a control element programmatically using JavaScript. The container requires `position: relative`.

```html
<style>
  .container {
    position: relative;
  }
</style>
<div class="container">
  <md-ripple id="ripple"></md-ripple>
  <button id="ripple-control"></button>
</div>
<script>
  const ripple = document.querySelector('#ripple');
  const control = document.querySelector('#ripple-control');
  ripple.attach(control);
</script>
```

--------------------------------

### var.fallback

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Returns the fallback value of a `var()` string. May be null if no fallback is present.

```APIDOC
## `var.fallback`

### Description
Returns the fallback value of a `var()` string. May be null if no fallback is present.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$var: var(--foo, var(--bar, 8px));
@debug var.fallback($var); // "var(--bar, 8px)"
```

### Response
#### Success Response (200)
- **string|null**: The fallback value of the `var()` string, or null if none exists.

#### Response Example
N/A
```

--------------------------------

### Filled Tonal Icon Button Theming

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Customize the container size, shape, icon size, and color for a filled tonal icon button. Overrides tokens to alter the tonal variant's appearance.

```html
<style>
:root {
  --md-filled-tonal-icon-button-container-width: 80px;
  --md-filled-tonal-icon-button-container-height: 80px;
  --md-filled-tonal-icon-button-container-shape: 0px;
  --md-filled-tonal-icon-button-icon-size: 40px;
  --md-sys-color-secondary-container: #006A6A;
}
</style>
<md-filled-tonal-icon-button>
  <md-icon>check</md-icon>
</md-filled-tonal-icon-button>
```

--------------------------------

### MdCheckbox Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/checkbox.md

These are the configurable properties for the MdCheckbox component.

```APIDOC
## MdCheckbox Properties

### Description

These properties control the state and behavior of the MdCheckbox component.

### Properties

- **checked** (boolean) - `false` - Whether or not the checkbox is selected.
- **indeterminate** (boolean) - `false` - Whether or not the checkbox is indeterminate.
- **required** (boolean) - `false` - When true, require the checkbox to be selected when participating in form submission.
- **value** (string) - `'on'` - The value of the checkbox that is submitted with a form when selected.
- **name** (string) - `undefined` - The HTML name to use in form submission.
- **disabled** (boolean) - `undefined` - Whether or not the element is disabled.
- **form** (HTMLFormElement) - `undefined` - The associated form element with which this element's value will submit.
- **labels** (NodeList) - `undefined` - The labels this element is associated with.
- **validity** (ValidityState) - `undefined` - Returns a ValidityState object that represents the validity states of the element.
- **validationMessage** (string) - `undefined` - Returns a validation error message or an empty string if the element is valid.
- **willValidate** (boolean) - `undefined` - Returns whether an element will successfully validate based on forms validation rules and constraints.
```

--------------------------------

### Basic Outlined Text Field

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

A standard outlined text field component with a label and a pre-filled value.

```html
<md-outlined-text-field label="Outlined" value="Value"></md-outlined-text-field>
```

--------------------------------

### Radio Button with Associated Label

Source: https://github.com/material-components/material-web/blob/main/docs/components/radio.md

Shows how to associate a label with a radio button using the 'for' attribute on the label and the 'id' attribute on the radio button. This improves accessibility and usability.

```html
<md-radio id="cats-radio" name="animals" value="cats"></md-radio>
<label for="cats-radio">Cats</label>

<md-radio id="dogs-radio" name="animals" value="dogs"></md-radio>
<label for="dogs-radio">Dogs</label>
```

--------------------------------

### Load Material Symbols Outlined Font

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

Include this link tag in your HTML to load the Material Symbols Outlined font. This is required before using outlined icons.

```html
<link href="https://fonts.googleapis.com/icon?family=Material+Symbols+Outlined" rel="stylesheet">
```

--------------------------------

### Slider with Value Label

Source: https://github.com/material-components/material-web/blob/main/docs/components/slider.md

Displays the specific value corresponding to the handle's placement. The label appears when a handle is selected.

```html
<md-slider labeled></md-slider>
```

--------------------------------

### List Difference with Sass Ext

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

The `list_ext.difference` function returns all items in the first list that are not present in the second list. It takes two lists as arguments and returns a new list containing the unique elements.

```scss
$listA: ('apple', 'banana', 'cherry', 'date');
$listB: ('banana', 'cherry', 'apple');
$listC: ('apple', 'banana', 'date');

@debug list_ext.difference($listA, $listB); // ('date')
@debug list_ext.difference($listA, $listC); // ('cherry')
```

--------------------------------

### Define Component Tokens

Source: https://github.com/material-components/material-web/blob/main/docs/theming/README.md

Define component tokens to assign design attributes to elements. These can be system tokens or concrete values. Component tokens are not prefixed with 'comp'.

```css
:root {
  --md-filled-button-container-shape: 0px;
}

md-filled-button.error {
  --md-filled-button-container-color: var(--md-sys-color-error);
  --md-filled-button-label-text-color: var(--md-sys-color-on-error);
}
```

--------------------------------

### Associating Labels with Checkboxes

Source: https://github.com/material-components/material-web/blob/main/docs/components/checkbox.md

Shows two methods for associating labels with checkboxes: nesting the md-checkbox within a label element, or using the 'for' attribute on a label to link to a checkbox's ID. Both methods ensure proper accessibility and user interaction.

```html
<label>
  <md-checkbox touch-target="wrapper"></md-checkbox>
  Checkbox one
</label>

<md-checkbox id="checkbox-two" touch-target="wrapper"></md-checkbox>
<label for="checkbox-two">Checkbox two</label>
```

--------------------------------

### Split map into two based on keys

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Divides a map into two new maps: one containing specified keys and the other containing the remaining keys. Useful for separating configuration or properties.

```scss
map_ext.split($map, $keys) //=> list

/// @param {map} $map - The Map to split.
/// @param {list} $keys - List of keys to split the Map by.
/// @return {list} A List pair with two new Maps: the first with the keys provided, and the second with the remaining keys.
```

```scss
$map: (
  'focus': blue,
  'focus-within': blue,
  'hover': teal,
  'active': green,
);

$pair: map_ext.split($map, ('focus', 'focus-within'));

$map-with-focus-keys: list.nth($pair, 1);
@debug $map-with-focus-keys; // ('focus': blue, 'focus-within': blue)

$map-with-remaining-keys: list.nth($pair, 2);
@debug $map-with-remaining-keys; // ('hover': teal, 'active': green)
```

--------------------------------

### Text Field Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Properties that can be set on the Text Field component to configure its appearance and behavior.

```APIDOC
## Text Field Properties

This section details the configurable properties for the Text Field component.

### Properties

- **`step`** (string) - `''` - Returns or sets the element's step attribute, which works with min and max to limit the increments at which a numeric or date-time value can be set.
- **`type`** (TextFieldType | UnsupportedTextFieldType) - `'text'` - The `<input>` type to use, defaults to "text". The type greatly changes how the text field behaves.
- **`autocomplete`** (string) - `''` - Describes what, if any, type of autocomplete functionality the input should provide.
- **`name`** (string) - `undefined` - The HTML name to use in form submission.
- **`disabled`** (boolean) - `undefined` - Whether or not the element is disabled.
- **`selectionDirection`** ("none" | "forward" | "backward") - `undefined` - 
- **`selectionEnd`** (number) - `undefined` - 
- **`selectionStart`** (number) - `undefined` - 
- **`valueAsNumber`** (number) - `undefined` - 
- **`valueAsDate`** (Date) - `undefined` - 
- **`form`** (HTMLFormElement) - `undefined` - The associated form element with which this element's value will submit.
- **`labels`** (NodeList) - `undefined` - The labels this element is associated with.
- **`validity`** (ValidityState) - `undefined` - Returns a ValidityState object that represents the validity states of the element.
- **`validationMessage`** (string) - `undefined` - Returns a validation error message or an empty string if the element is valid.
- **`willValidate`** (boolean) - `undefined` - Returns whether an element will successfully validate based on forms validation rules and constraints.
```

--------------------------------

### Trim trailing whitespace with `string_ext.trim-end`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.trim-end` to remove whitespace characters from the end of a string. This is useful for cleaning up data or ensuring consistent formatting.

```scss
@debug string_ext.trim-end('  foo bar  '); // "  foo bar"
```

--------------------------------

### Continuous Slider

Source: https://github.com/material-components/material-web/blob/main/docs/components/slider.md

Use for selecting a value along a subjective range without predefined steps.

```html
<md-slider min="0" max="100" value="50"></md-slider>
```

--------------------------------

### string_ext.ends-with

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Checks if a given string ends with a specified suffix.

```APIDOC
## `string_ext.ends-with`

### Description
Checks if a string ends with a given suffix.

### Parameters
#### Path Parameters
- **$string** (string) - Required - The string to test.
- **$suffix** (string) - Required - The suffix to check.

### Return Value
- **boolean** - True if the string ends with the given suffix, false otherwise.

### Example
```scss
@debug string_ext.ends-with('var(--foo)', ')'); // true
```
```

--------------------------------

### Check value type with type.matches

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `type.matches` to verify if a value conforms to a specified type string, supporting multiple types separated by '|'. This is useful for conditional logic based on data types.

```scss
type.matches($value, $type-string) //=> boolean

/// @param {*} $value - The value to check the type of.
/// @param {string} $type-string - The type to check. May be multiple types separated by `|`.
/// @return {boolean} True if the value matches the type string.
```

```scss
@function is-empty($value) {
  @if type.matches($value, 'list|map') {
    @return list.length($value) == 0;
  }
  @if type.matches($value, 'string') {
    @return $value == '';
  }
  @return type.matches($value, 'null');
}
```

--------------------------------

### Extended FAB with Label and Aria Label

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Extended FABs use their `label` for accessibility. An `aria-label` can be added for extra context. Providing the `label` attribute ensures the icon is not announced.

```html
<md-fab label="Edit" aria-label="Edit Comment">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### FABs with Different Variants

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

FAB colors can be changed using the `variant` attribute, which accepts 'surface' (default), 'primary', 'secondary', or 'tertiary'.

```html
<md-fab variant="primary" aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
<md-fab variant="secondary" aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
<md-fab variant="tertiary" aria-label="Edit">
  <md-icon slot="icon">edit</md-icon>
</md-fab>
```

--------------------------------

### Basic Filled Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Use filled buttons for important, final actions that complete a flow. This is the most visually impactful button type after the FAB.

```html
<md-filled-button>Filled</md-filled-button>
```

--------------------------------

### Textarea with Vertical Resize

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Configures a filled textarea to allow vertical resizing. Use CSS 'resize' property to control behavior.

```html
<style>
  md-filled-text-field {
    resize: vertical;
  }
</style>
<md-filled-text-field
    type="textarea"
    label="Vertical resize"
    rows="3">
</md-filled-text-field>
```

--------------------------------

### Basic Filled Tonal Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Filled tonal buttons offer a middle ground between filled and outlined buttons. Use them for lower-priority actions that need more emphasis than an outline.

```html
<md-filled-tonal-button>Tonal</md-filled-tonal-button>
```

--------------------------------

### assert.not-type

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Asserts that the argument is not a specific type. The argument is returned if it does not match. An error is thrown if the argument matches the type.

```APIDOC
## assert.not-type

### Description
Asserts that the argument is not a specific type. The argument is returned if it does not match. An error is thrown if the argument matches the type.

### Method
SCSS Function/Mixin

### Endpoint
N/A (Sass Module Function)

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
@use '@material/web/sass/ext/assert';
@use 'sass:map';

@function get-strict($map, $key) {
  @return assert.not-type(
    map.get($map, $key),
    'null',
    $message: 'Key must be in the map'
  );
}
```

### Response
#### Success Response (200)
- **The argument** (type) - The argument if it does not match the type string.

#### Response Example
```scss
// Example usage within a function
// get-strict({"a": 1}, "a") returns 1.
// get-strict({"a": 1}, "b") throws an error because map.get returns null, which matches 'null'.
```

### Error Handling
- **Error** - Thrown if the argument matches the specified type string.
```

--------------------------------

### var.name

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Returns the custom property variable name from a `var()` string.

```APIDOC
## `var.name`

### Description
Returns the custom property variable name from a `var()` string.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$var: var(--foo);
@debug var.name($var); // "--foo"
```

### Response
#### Success Response (200)
- **string**: The custom property variable name.

#### Response Example
N/A
```

--------------------------------

### Required Select

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Shows how to make a select component mandatory by adding the 'required' attribute. This ensures a selection must be made.

```html
<md-filled-select required>
  <md-select-option value="one">
    <div slot="headline">One</div>
  </md-select-option>
  <md-select-option value="two">
    <div slot="headline">Two</div>
  </md-select-option>
</md-filled-select>
```

--------------------------------

### Discrete Slider with Ticks

Source: https://github.com/material-components/material-web/blob/main/docs/components/slider.md

Use for selecting specific values from a predetermined range, optionally showing tick marks.

```html
<md-slider step="5" ticks min="0" max="20"></md-slider>
```

--------------------------------

### Test error handling with throw.get-error

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `throw.get-error` to check if a value is an error string, primarily for testing with `sass-true`. It returns the error string if found, otherwise false. Avoid using in production; prefer `@error`.

```scss
throw.get-error($error, $errors)

/// @param {*} $error - The value to check.
/// @param {list} $errors - Additional values to check. Useful for checking multiple errors at the same time.
/// @return {string|boolean} The error string if any value is an error, or false otherwise.
```

```scss
// A function that may return an "ERROR:" string in a test.
@function get-value($map, $key) {
  @if meta.type-of($map) != 'map' {
    // Identical to `@error 'ERROR: Arg is not a map'` outside of tests.
    @return throw.error('Arg is not a map');
  }
  @return map.get($map, $key);
}

// A function that needs to handle potential errors from other functions.
@function mix-primary-on-surface($values) {
  $primary: get-value($values, 'primary');
  $surface: get-value($values, 'surface');
  $error: throw.get-error($primary, $surface);
  @if $error {
    // Return early to guard logic against additional errors since
    // $primary or $surface may be a string instead of a color.
    @return $error;
  }

  @return color.mix($primary, $surface, 10%);
}
```

```scss
// In a `sass-true` test, `throw.get-error()` can be used to assert that
// an error is thrown.
@use 'true' as test with ($catch-errors: true);

@include test.describe('module.get-value()') {
  @include test.it('throws an error if the value is not a map') {
    $result: module.get-value('not a map', 'primary');
    @include test.assert-truthy(throw.get-error($result), '$result is an error');
  }
}
```

--------------------------------

### Text Field with Trailing Icon Button for Password Visibility

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Implements a password text field with a toggleable icon button at the trailing end to show or hide the password.

```html
<md-outlined-text-field label="Password" type="password">
  <md-icon-button toggle slot="trailing-icon">
    <md-icon>visibility</md-icon>
    <md-icon slot="selected">visibility_off</md-icon>
  </md-icon-button>
</md-outlined-text-field>
```

--------------------------------

### Extract fallback value from var() string

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `var.fallback` to retrieve the immediate fallback value from a CSS `var()` string. Returns null if no fallback is present. Throws an error for invalid input.

```scss
var.fallback($var) //=> string

/// @param {string} $var - A custom property `var()` string.
/// @return {string} The fallback value of the `var()` string. May be null if the `var()` does not have a fallback value.
/// @throw If the value is not a custom property.
```

```scss
$var: var(--foo, var(--bar, 8px));
@debug var.fallback($var); // "var(--bar, 8px)"
```

--------------------------------

### Text Field Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

This snippet details the properties available for the Text Field component, outlining their types, default values, and functionalities.

```APIDOC
## Text Field Component Properties

This section details the properties available for the Text Field component.

### Properties

- **placeholder** (string) - Optional - Defines the text displayed in the textfield when it has no value. Provides a brief hint to the user as to the expected type of data that should be entered into the control. Unlike `label`, the placeholder is not visible and does not float when the textfield has a value. (MDN: https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/placeholder)
- **readOnly** (boolean) - Optional - Indicates whether or not a user should be able to edit the text field's value. Defaults to `false`. (MDN: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#readonly)
- **multiple** (boolean) - Optional - Indicates that input accepts multiple email addresses. Defaults to `false`. (MDN: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/email#multiple)
- **step** (string) - Optional - Returns or sets the element's step attribute, which works with min and max to limit the increments at which a numeric or date-time value can be set. Defaults to `''`. (MDN: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#step)
- **type** (TextFieldType | UnsupportedTextFieldType) - Optional - The `<input>` type to use, defaults to "text". The type greatly changes how the text field behaves. Text fields support a limited number of `<input>` types: text, textarea, email, number, password, search, tel, url. (See MDN: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#input_types for more details on each input type.)
- **autocomplete** (string) - Optional - Describes what, if any, type of autocomplete functionality the input should provide. Defaults to `''`. (MDN: https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete)
- **name** (string) - Optional - The HTML name to use in form submission. Defaults to `undefined`.
- **disabled** (boolean) - Optional - Whether or not the element is disabled. Defaults to `undefined`.
- **selectionDirection** (string) - Optional - Possible values: "none", "forward", "backward". Defaults to `undefined`.
- **selectionEnd** (number) - Optional - Defaults to `undefined`.
- **selectionStart** (number) - Optional - Defaults to `undefined`.
- **valueAsNumber** (number) - Optional - Defaults to `undefined`.
- **valueAsDate** (Date) - Optional - Defaults to `undefined`.
- **form** (HTMLFormElement) - Optional - The associated form element with which this element's value will submit. Defaults to `undefined`.
- **labels** (NodeList) - Optional - The labels this element is associated with. Defaults to `undefined`.
- **validity** (ValidityState) - Optional - Returns a ValidityState object that represents the validity states of the element. (MDN: https://developer.mozilla.org/en-US/docs/Web/API/ValidityState) Defaults to `undefined`.
- **validationMessage** (string) - Optional - Returns a validation error message or an empty string if the element is valid. (MDN: https://developer.mozilla.org/en-US/docs/Web/API/ElementInternals/validationMessage) Defaults to `undefined`.
- **willValidate** (boolean) - Optional - Returns whether an element will successfully validate based on forms validation rules and constraints. Disabled and readonly elements will not validate. (MDN: https://developer.mozilla.org/en-US/docs/Web/API/ElementInternals/willValidate) Defaults to `undefined`.
```

--------------------------------

### var.is-var

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Indicates whether or not a value is a custom property var() string.

```APIDOC
## `is-var`

### Description
Indicates whether or not a value is a custom property `var()` string.

### Method
SCSS Function

### Endpoint
N/A

### Parameters
#### Arguments
- **$var** (*) - Required - The value to test.

### Request Example
```scss
$var: var(--foo);
@debug var.is-var($var); // true
```

### Response
#### Return Value
- **boolean** - True if the value is a custom property `var()` string, or false if not.

#### Response Example
```scss
true
```
```

--------------------------------

### MdRadio Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/radio.md

Properties that can be set or read on an MdRadio element to control its state and behavior.

```APIDOC
## MdRadio Properties

### Description
Properties that can be set or read on an MdRadio element to control its state and behavior.

### Properties
- **required** (boolean) - Optional - Whether or not the radio is required. If any radio is required in a group, all radios are implicitly required.
- **value** (string) - Optional - The element value to use in form submission when checked. Defaults to 'on'.
- **checked** (boolean) - Optional - Whether the radio is currently checked.
- **name** (string) - Optional - The HTML name to use in form submission.
- **disabled** (boolean) - Optional - Whether or not the element is disabled.
- **form** (HTMLFormElement) - Optional - The associated form element with which this element's value will submit.
- **labels** (NodeList) - Optional - The labels this element is associated with.
- **validity** (ValidityState) - Optional - Returns a ValidityState object that represents the validity states of the element.
- **validationMessage** (string) - Optional - Returns a validation error message or an empty string if the element is valid.
- **willValidate** (boolean) - Optional - Returns whether an element will successfully validate based on forms validation rules and constraints.
```

--------------------------------

### CSS for Pending DSD Elements

Source: https://github.com/material-components/material-web/blob/main/catalog/site/stories/stories.html

This CSS rule ensures that elements with the 'dsd-pending' attribute are displayed, which is useful during the initialization of custom elements.

```css
body[dsd-pending] {
  display: block !important;
}
```

--------------------------------

### assert.is-type

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Asserts that the argument is a specific type. If it is, the argument is returned, otherwise an error is thrown.

```APIDOC
## assert.is-type

### Description
Asserts that the argument is a specific type. If it is, the argument is returned, otherwise an error is thrown.

### Method
SCSS Function/Mixin

### Endpoint
N/A (Sass Module Function)

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
@use '@material/web/sass/ext/assert';

@mixin multiply($a, $b) {
  $a: assert.is-type($a, 'number');
  $b: assert.is-type($b, 'number');
  @return $a * $b;
}

@function is-empty($value) {
  $value: assert.is-type(
    $value,
    'list|map|null',
    $message: '$value must be a list, map, or null',
    $source: 'is-empty'
  );
  @return $value and list.length($value) == 0;
}
```

### Response
#### Success Response (200)
- **The argument** (type) - The argument if it matches the type string.

#### Response Example
```scss
// Example usage within a mixin or function
// If $a and $b are numbers, multiply($a, $b) returns their product.
// If $value is a list, map, or null, is-empty($value) returns true if it's empty, false otherwise.
```

### Error Handling
- **Error** - Thrown if the argument does not match the specified type string.
```

--------------------------------

### Attach Focus Ring to Referenced Element

Source: https://github.com/material-components/material-web/blob/main/docs/components/focus-ring.md

Attach the focus ring to a specific element using the `for` attribute, linking it to an input or other control. The container must have `position: relative`.

```html
<div style="position: relative">
  <md-focus-ring for="control" style="--md-focus-ring-shape: 8px"></md-focus-ring>
  <input id="control">
</div>
```

--------------------------------

### MdFilledButton Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

The MdFilledButton component offers various properties to control its appearance and behavior, such as disabling the button, setting link behavior, and managing icons.

```APIDOC
## MdFilledButton Properties

### Description

Properties for the `md-filled-button` component.

### Properties

- **softDisabled** (boolean) - Optional - Whether or not the button is "soft-disabled" (disabled but still focusable).
- **href** (string) - Optional - The URL that the link button points to.
- **download** (string) - Optional - The filename to use when downloading the linked resource.
- **target** (string) - Optional - Where to display the linked `href` URL for a link button.
- **trailingIcon** (boolean) - Optional - Whether to render the icon at the inline end of the label rather than the inline start.
- **hasIcon** (boolean) - Optional - Whether to display the icon or not.
- **name** (string) - Optional - The HTML name to use in form submission.
- **disabled** (boolean) - Optional - Whether or not the element is disabled.
- **type** (string) - Optional - A string indicating the form submission behavior of the element.
- **value** (string) - Optional - The value of the button.
- **form** (HTMLFormElement) - Optional - The associated form element with which this element's value will submit.
- **labels** (NodeList) - Optional - The labels this element is associated with.
```

--------------------------------

### Style Filled Select Component

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/select/theming-filled.html

Applies custom styles to a filled select component, including container shape, color, and input text properties. Also styles the menu container.

```css
.styled-example.filled { --md-filled-select-text-field-container-shape: 0px; --md-filled-select-text-field-container-color: #f7faf9; --md-filled-select-text-field-input-text-color: #005353; --md-filled-select-text-field-input-text-font: system-ui; } .styled-example.filled md-filled-select::part(menu) { --md-menu-container-color: #f4fbfa; --md-menu-container-shape: 0px; }
```

--------------------------------

### Unbounded Ripple Styles

Source: https://github.com/material-components/material-web/blob/main/docs/components/ripple.md

Apply these styles to create an unbounded circular ripple centered on an element. The container must have `position: relative`.

```css
.container {
  display: flex;
  place-content: center;
  place-items: center;
  position: relative;
}

md-ripple.unbounded {
  border-radius: 50%;
  inset: unset;
  height: var(--state-layer-size);
  width: var(--state-layer-size);
}
```

--------------------------------

### Check if string ends with suffix using `string_ext.ends-with`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.ends-with` to verify if a string concludes with a given suffix. This is helpful for checking string endings or validating formats.

```scss
@debug string_ext.ends-with('var(--foo)', ')'); // true
```

--------------------------------

### MdSelect Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Events emitted by the MdSelect component for various user interactions and state changes.

```APIDOC
## MdSelect Events

### `change`

#### Description
The native `change` event on `<input>`.

#### Type
* `Event`

#### Bubbles
* Yes

#### Composed
* No

### `input`

#### Description
The native `input` event on `<input>`.

#### Type
* `InputEvent`

#### Bubbles
* Yes

#### Composed
* Yes

### `opening`

#### Description
Fired when the select's menu is about to open.

#### Type
* `Event`

#### Bubbles
* No

#### Composed
* No

### `opened`

#### Description
Fired when the select's menu has finished animations and opened.

#### Type
* `Event`

#### Bubbles
* No

#### Composed
* No

### `closing`

#### Description
Fired when the select's menu is about to close.

#### Type
* `Event`

#### Bubbles
* No

#### Composed
* No

### `closed`

#### Description
Fired when the select's menu has finished animations and closed.

#### Type
* `Event`

#### Bubbles
* No

#### Composed
* No
```

--------------------------------

### MdFilledTonalButton Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

The MdFilledTonalButton component provides similar customization options to MdFilledButton, including soft disabling, link functionality, and icon placement.

```APIDOC
## MdFilledTonalButton Properties

### Description

Properties for the `md-filled-tonal-button` component.

### Properties

- **softDisabled** (boolean) - Optional - Whether or not the button is "soft-disabled" (disabled but still focusable).
- **href** (string) - Optional - The URL that the link button points to.
- **download** (string) - Optional - The filename to use when downloading the linked resource.
- **target** (string) - Optional - Where to display the linked `href` URL for a link button.
- **trailingIcon** (boolean) - Optional - Whether to render the icon at the inline end of the label rather than the inline start.
- **hasIcon** (boolean) - Optional - Whether to display the icon or not.
- **name** (string) - Optional - The HTML name to use in form submission.
- **disabled** (boolean) - Optional - Whether or not the element is disabled.
- **type** (string) - Optional - A string indicating the form submission behavior of the element.
- **value** (string) - Optional - The value of the button.
- **form** (HTMLFormElement) - Optional - The associated form element with which this element's value will submit.
- **labels** (NodeList) - Optional - The labels this element is associated with.
```

--------------------------------

### Text Field with Character Counter

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Implements an outlined text field with a character counter that displays the current input length against the maximum allowed length.

```html
<md-outlined-text-field label="Title" value="Short" maxlength="10">
</md-outlined-text-field>
```

--------------------------------

### Basic Filled Text Field

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

A standard filled text field component with a label and a pre-filled value.

```html
<md-filled-text-field label="Filled" value="Value"></md-filled-text-field>
```

--------------------------------

### Radio Group Accessibility

Source: https://github.com/material-components/material-web/blob/main/docs/components/radio.md

Demonstrates how to structure radio buttons within a group using `role="radiogroup"` and `aria-labelledby` for accessibility. Ensure each radio has an `aria-label`.

```html
<div role="radiogroup" aria-labelledby="group-title">
  <h3 id="group-title">Starting position</h3>
  <div>
    <md-radio id="first-radio" name="group" value="1"
        aria-label="First"></md-radio>
    <label for="first-radio">1st</label>
  </div>
  <div>
    <md-radio id="second-radio" name="group" value="2"
        aria-label="Second"></md-radio>
    <label for="second-radio">2nd</label>
  </div>
</div>
```

--------------------------------

### Toggle Icon Button with Aria Labels

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

For toggle icon buttons, use `aria-label` for the unselected state and `aria-label-selected` for the selected state to convey the button's status.

```html
<md-icon-button toggle
  aria-label="Unselected"
  aria-label-selected="Selected">
  <md-icon>close</md-icon>
  <md-icon slot="selected">check</md-icon>
</md-icon-button>
```

--------------------------------

### Set Fallback for Custom Property

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `set-fallback` to create a new custom property string with a specified fallback value. It can be used when the original fallback needs to be replaced.

```scss
$var: var(--foo, var(--bar, 8px));
$new-var: set-fallback($var, 16px);
@debug $new-var; // "var(--foo, 16px)"
```

--------------------------------

### Range Slider

Source: https://github.com/material-components/material-web/blob/main/docs/components/slider.md

Use for selecting a minimum and maximum value within a range, indicated by two handles.

```html
<md-slider range value-start="25" value-end="50"></md-slider>
```

--------------------------------

### MdFilledTextField Properties

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

The MdFilledTextField component exposes several properties that control its state, appearance, and behavior. These can be set directly as properties on the element or as corresponding HTML attributes.

```APIDOC
## MdFilledTextField API

### Properties

This table lists the configurable properties for the `md-filled-text-field` component.

| Property | Attribute | Type | Default | Description |
| --- | --- | --- | --- | --- |
| `error` | `error` | `boolean` | `false` | Gets or sets whether or not the text field is in a visually invalid state. This error state overrides the error state controlled by `reportValidity()`. |
| `errorText` | `error-text` | `string` | `''` | The error message that replaces supporting text when `error` is true. If `errorText` is an empty string, then the supporting text will continue to show. This error message overrides the error message displayed by `reportValidity()`. |
| `label` | `label` | `string` | `''` | The floating Material label of the textfield component. It informs the user about what information is requested for a text field. It is aligned with the input text, is always visible, and it floats when focused or when text is entered into the textfield. This label also sets accessibilty labels, but the accessible label is overriden by `aria-label`. Learn more about floating labels from the Material Design guidelines: https://m3.material.io/components/text-fields/guidelines |
| `noAsterisk` | `no-asterisk` | `boolean` | `false` | Disables the asterisk on the floating label, when the text field is required. |
| `required` | `required` | `boolean` | `false` | Indicates that the user must specify a value for the input before the owning form can be submitted and will render an error state when `reportValidity()` is invoked when value is empty. Additionally the floating label will render an asterisk `"*"` when true. https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/required |
| `value` | `value` | `string` | `''` | The current value of the text field. It is always a string. |
| `prefixText` | `prefix-text` | `string` | `''` | An optional prefix to display before the input value. |
| `suffixText` | `suffix-text` | `string` | `''` | An optional suffix to display after the input value. |
| `hasLeadingIcon` | `has-leading-icon` | `boolean` | `false` | Whether or not the text field has a leading icon. Used for SSR. |
| `hasTrailingIcon` | `has-trailing-icon` | `boolean` | `false` | Whether or not the text field has a trailing icon. Used for SSR. |
| `supportingText` | `supporting-text` | `string` | `''` | Conveys additional information below the text field, such as how it should be used. |
| `textDirection` | `text-direction` | `string` | `''` | Override the input text CSS `direction`. Useful for RTL languages that use LTR notation for fractions. |
| `rows` | `rows` | `number` | `2` | The number of rows to display for a `type="textarea"` text field. Defaults to 2. |
| `cols` | `cols` | `number` | `20` | The number of cols to display for a `type="textarea"` text field. Defaults to 20. |
| `inputMode` | `inputmode` | `string` | `''` |  |
| `max` | `max` | `string` | `''` | Defines the greatest value in the range of permitted values. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#max |
| `maxLength` | `maxlength` | `number` | `-1` | The maximum number of characters a user can enter into the text field. Set to -1 for none. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#maxlength |
| `min` | `min` | `string` | `''` | Defines the most negative value in the range of permitted values. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#min |
| `minLength` | `minlength` | `number` | `-1` | The minimum number of characters a user can enter into the text field. Set to -1 for none. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#minlength |
| `noSpinner` | `no-spinner` | `boolean` | `false` | When true, hide the spinner for `type="number"` text fields. |
| `pattern` | `pattern` | `string` | `''` | A regular expression that the text field's value must match to pass constraint validation. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#pattern |
| `placeholder` | `placeholder` | `string` | `''` | Defines the text displayed in the textfield when it has no value. Provides a brief hint to the user as to the expected type of data that should be entered into the control. Unlike `label`, the placeholder is not visible and does not float when the textfield has a value. https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/placeholder |
| `readOnly` | `readonly` | `boolean` | `false` | Indicates whether or not a user should be able to edit the text field's value. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#readonly |
| `multiple` | `multiple` | `boolean` | `false` | Indicates that input accepts multiple email addresses. https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/email#multiple |
```

--------------------------------

### Listening for Tab Change Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Listen for the 'change' event on the md-tabs element to detect tab selections. The event target is the md-tabs element, and activeTabIndex property indicates the index of the selected tab.

```ts
tabs.addEventListener('change', (event: Event) => {
  if (event.target.activeTabIndex === 2) {
    // ... perform logic only if index of selected item is 2.
  }
});
```

--------------------------------

### Render Table of Contents

Source: https://github.com/material-components/material-web/blob/main/catalog/site/_includes/default.html

Conditionally renders the table of contents if the `hasToc` variable is true. The `toc` block contains the ToC content.

```html
{% if hasToc %}

{% block toc %}{% endblock %}

{% endif %}
```

--------------------------------

### Text Field with Leading Search Icon

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Adds a search icon to the leading side of an outlined text field for search functionality.

```html
<md-outlined-text-field placeholder="Search for messages">
  <md-icon slot="leading-icon">search</md-icon>
</md-outlined-text-field>
```

--------------------------------

### var.deep-fallback

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Returns the final concrete fallback value in a chain of nested `var()` strings. May be null.

```APIDOC
## `var.deep-fallback`

### Description
Returns the final concrete fallback value in a chain of nested `var()` strings. May be null.

### Method
Sass Function

### Endpoint
N/A

### Parameters
#### Path Parameters
N/A

#### Query Parameters
N/A

#### Request Body
N/A

### Request Example
```scss
$var: var(--foo, var(--bar, 8px));
@debug var.deep-fallback($var); // "8px"
```

### Response
#### Success Response (200)
- **string|null**: The deep fallback value of the `var()` string, or null if none exists.

#### Response Example
N/A
```

--------------------------------

### Rename keys in a map

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Returns a map with specified keys renamed according to a provided mapping. Useful for updating map structures or conforming to new naming conventions.

```scss
map_ext.rename-keys($map, $keys-to-rename) //=> map

/// @param {map} $map - The map to rename keys within.
/// @param {map} $keys-to-rename - A map of keys and their new names.
/// @return {map} The map with any matching keys renamed.
```

```scss
$map: ('foo': red);

$new-map: map_ext.rename-keys($map, ('foo': 'bar'));
@debug $new-map; // ('bar': red)
```

--------------------------------

### Attach Ripple to Parent Element

Source: https://github.com/material-components/material-web/blob/main/docs/components/ripple.md

Use this method to attach a ripple to the parent element. Ensure the parent has `position: relative`.

```html
<style>
  .container {
    position: relative;
  }
</style>
<button class="container">
  <md-ripple></md-ripple>
</button>
```

--------------------------------

### Load Material Symbols Rounded Font

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

Include this link tag in your HTML to load the Material Symbols Rounded font. This is required before using rounded icons.

```html
<link href="https://fonts.googleapis.com/icon?family=Material+Symbols+Rounded" rel="stylesheet">
```

--------------------------------

### Attach Ripple to Referenced Element

Source: https://github.com/material-components/material-web/blob/main/docs/components/ripple.md

Attach a ripple to a referenced element using the `for` attribute. The parent container needs `position: relative`.

```html
<style>
  .container {
    position: relative;
  }
</style>
<div class="container">
  <md-ripple for="control"></md-ripple>
  <input id="control">
</div>
```

--------------------------------

### Basic Elevated Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

An elevated button is a filled tonal button with a shadow, used for visual separation from patterned backgrounds when necessary.

```html
<md-elevated-button>Elevated</md-elevated-button>
```

--------------------------------

### MdFilterChip Properties and Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/chip.md

The MdFilterChip component is designed for filtering, providing properties for selection, removability, and disabled states, along with `remove` and `update-focus` events.

```APIDOC
## MdFilterChip

### Description
Represents a filter chip.

### Properties
#### Property
- `elevated` (boolean) - Optional - Whether the chip is elevated.
- `removable` (boolean) - Optional - Whether the chip is removable.
- `selected` (boolean) - Optional - Whether the chip is selected.
- `hasSelectedIcon` (boolean) - Optional - Only needed for SSR. Add this attribute when a filter chip has a `slot="selected-icon"` to avoid a Flash Of Unstyled Content.
- `disabled` (boolean) - Optional - Whether or not the chip is disabled.
- `softDisabled` (boolean) - Optional - Whether or not the chip is "soft-disabled" (disabled but still focusable).
- `alwaysFocusable` (boolean) - Optional - When true, allow disabled chips to be focused with arrow keys.
- `label` (string) - Optional - The label of the chip.
- `hasIcon` (boolean) - Optional - Only needed for SSR. Add this attribute when a chip has a `slot="icon"` to avoid a Flash Of Unstyled Content.
- `handleTrailingActionFocus` (Function) - Optional - Handler for trailing action focus.
- `ariaLabelRemove` (string) - Optional - Aria label for the remove button.

### Events
#### Event
- `remove` (Event) - Dispatched when the remove button is clicked.
- `update-focus` (Event) - Dispatched when `disabled` is toggled.
```

--------------------------------

### MdSelect Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/select.md

Events fired by the MdSelect component to indicate state changes or user interactions.

```APIDOC
## MdSelect Events

### Description
Events fired by the MdSelect component.

### Events

- `change` (Type: `Event`, Bubbles: Yes, Composed: No)
  The native `change` event on [`<input>`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/change_event)

- `input` (Type: `InputEvent`, Bubbles: Yes, Composed: Yes)
  The native `input` event on [`<input>`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/input_event)

- `opening` (Type: `Event`, Bubbles: No, Composed: No)
  Fired when the select's menu is about to open.

- `opened` (Type: `Event`, Bubbles: No, Composed: No)
  Fired when the select's menu has finished animations and opened.

- `closing` (Type: `Event`, Bubbles: No, Composed: No)
  Fired when the select's menu is about to close.

- `closed` (Type: `Event`, Bubbles: No, Composed: No)
  Fired when the select's menu has finished animations and closed.
```

--------------------------------

### Dialog with Aria-Label

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

For dialogs without a headline, provide an 'aria-label' attribute for accessibility. This ensures screen readers can announce the dialog's purpose.

```html
<md-dialog aria-label="Error message">
  <div slot="content">An error occurred, details ...</div>
</md-dialog>
```

--------------------------------

### Set Elevation Level

Source: https://github.com/material-components/material-web/blob/main/docs/components/elevation.md

Use the `--md-elevation-level` CSS custom property to set the elevation of a surface. Ensure the parent element has `position: relative`.

```html
<style>
  .surface {
    position: relative;
    border-radius: 16px;
    height: 64px;
    width: 64px;

    --md-elevation-level: 3;
  }
</style>
<div class="surface">
  <md-elevation></md-elevation>
  <!-- Content -->
</div>
```

--------------------------------

### Alert Dialog Type

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

Use type="alert" for dialogs that require a user's response to an important message, such as confirmation prompts or error messages. This is indicated by the ARIA 'alertdialog' role.

```html
<md-dialog type="alert">
  <div slot="headline">Confirm deletion</div>
  <form slot="content" id="form-id" method="dialog">
    Are you sure you wish to delete this item?
  </form>
  <div slot="actions">
    <md-text-button form="form-id" value="cancel">Cancel</md-text-button>
    <md-text-button form="form-id" value="delete">Delete</md-text-button>
  </div>
</md-dialog>
```

--------------------------------

### MdCheckbox Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/checkbox.md

These are the events emitted by the MdCheckbox component.

```APIDOC
## MdCheckbox Events

### Description

These events are emitted by the MdCheckbox component to signal state changes or user interactions.

### Events

- **change** (`Event`) - Bubbles: Yes, Composed: No - The native `change` event on `<input>`.
- **input** (`InputEvent`) - Bubbles: Yes, Composed: Yes - The native `input` event on `<input>`.
```

--------------------------------

### Programmatically Close Dialog

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

Close a dialog programmatically by calling its 'close()' method. This method returns a Promise that resolves after the dialog's animation finishes.

```javascript
closeButton.addEventListener('click', async () => {
  await dialog.close();
});
```

--------------------------------

### Assert Argument Type with Sass Ext

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

The `assert.is-type` mixin asserts that an argument matches a specified type. It returns the argument if the type matches, otherwise it throws an error. Useful for validating function and mixin arguments.

```scss
@mixin multiply($a, $b) {
  $a: assert.is-type($a, 'number');
  $b: assert.is-type($b, 'number');
  @return $a * $b;
}

@function is-empty($value) {
  $value: assert.is-type(
    $value,
    'list|map|null',
    $message: '$value must be a list, map, or null',
    $source: 'is-empty'
  );
  @return $value and list.length($value) == 0;
}
```

--------------------------------

### Dialog Return Value

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

Identify which button closed the dialog by checking the 'returnValue' property. Buttons with a 'value' attribute set will populate this property.

```html
<md-dialog open>
  <form slot="content" id="form-id" method="dialog">...</form>
  <div slot="actions">
    <md-text-button form="form-id" value="cancel">Cancel</md-text-button>
    <md-text-button form="form-id" value="ok">Ok</md-text-button>
  </div>
</md-dialog>
```

--------------------------------

### Focusable Disabled Buttons in Toolbar

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

For toolbars, consider making disabled buttons focusable using the 'soft-disabled' attribute to improve discoverability. This follows ARIA guidelines for disabled controls.

```html
<div role="toolbar">
  <md-text-button>Copy</md-text-button>
  <md-text-button>Cut</md-text-button>
  <!--
    This button is disabled but kept focusable to improve its discoverability
    in the toolbar.
  -->
  <md-text-button soft-disabled>Paste</md-text-button>
</div>
```

--------------------------------

### MdRadio Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/radio.md

Events dispatched by the MdRadio element in response to user interaction or state changes.

```APIDOC
## MdRadio Events

### Description
Events dispatched by the MdRadio element in response to user interaction or state changes.

### Events
- **input** - `InputEvent` - Dispatched when the value changes from user interaction.
- **change** - `Event` - Dispatched when the value changes from user interaction.
```

--------------------------------

### Primary Tabs with Inline Icons

Source: https://github.com/material-components/material-web/blob/main/docs/components/tabs.md

Primary tabs can display their icons inline with the label by adding the 'inline-icon' attribute to the md-primary-tab element.

```html
<md-tabs>
  <md-primary-tab inline-icon>
    <md-icon slot="icon">piano</md-icon>
    Keyboard
  </md-primary-tab>
  <md-primary-tab inline-icon>
    <md-icon slot="icon">tune</md-icon>
    Guitar
  </md-primary-tab>
</md-tabs>
```

--------------------------------

### Extended FAB without Icon

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

Extended FABs are the only type that can be used without an icon, relying solely on their label.

```html
<md-fab label="Reroute"></md-fab>
```

--------------------------------

### Filled Icon Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Filled icon buttons provide higher visual impact and are suitable for high-emphasis actions. Use when a strong visual cue is needed.

```html
<md-filled-icon-button>
  <md-icon>check</md-icon>
</md-filled-icon-button>
```

--------------------------------

### Omit specific keys from a map

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Creates a new map excluding the specified keys from the original map. Useful for removing unwanted properties.

```scss
map_ext.omit($map, $keys) //=> map

/// @param {map} $map - The Map to split.
/// @param {list} $keys - List of keys to exclude from the new Map.
/// @return {map} Map without the keys provided.
```

```scss
$map: (
  'focus': blue,
  'focus-within': blue,
  'hover': teal,
  'active': green,
);

$map-without-focus-keys: map_ext.omit($map, ('focus', 'focus-within'));
@debug $map-without-focus-keys; // ('hover': teal, 'active': green)
```

--------------------------------

### Disable Focus Ring Animation

Source: https://github.com/material-components/material-web/blob/main/docs/components/focus-ring.md

Customize or disable focus ring animations using CSS custom properties. Setting `--md-focus-ring-duration` to `0s` effectively disables the animation.

```html
<style>
  md-focus-ring {
    --md-focus-ring-duration: 0s; /* disabled animation */
  }
</style>
```

--------------------------------

### Set Custom Font Family for Sharp Icons

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

To use sharp icons, set the CSS `font-family` property to 'Material Symbols Sharp'. This ensures the correct icon style is applied.

```css
font-family: 'Material Symbols Sharp';
```

--------------------------------

### Focusable Disabled Icon Button in Toolbar

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

In toolbars, consider making disabled icon buttons focusable using the `soft-disabled` attribute to improve discoverability, following ARIA guidelines for disabled controls.

```html
<div role="toolbar">
  <md-icon-button aria-label="Copy">
    <md-icon>copy</md-icon>
  </md-icon-button>
  <md-icon-button aria-label="Cut">
    <md-icon>cut</md-icon>
  </md-icon-button>
  <!--
    This icon button is disabled but kept focusable to improve its
    discoverability in the toolbar.
  -->
  <md-icon-button aria-label="Paste" soft-disabled>
    <md-icon>paste</md-icon>
  </md-icon-button>
</div>
```

--------------------------------

### MdBrandedFab Component

Source: https://github.com/material-components/material-web/blob/main/docs/components/fab.md

The MdBrandedFab component is a specialized Floating Action Button with branding considerations. It shares most properties with MdFab but may have specific behaviors or constraints.

```APIDOC
## MdBrandedFab <md-branded-fab>

### Description
Represents a branded Floating Action Button.

### Properties

- **variant** (FabVariant) - Optional - The FAB color variant to render. Defaults to 'surface'.
- **size** (FabSize) - Optional - The size of the FAB. Defaults to 'medium'. Note: Branded FABs cannot be sized to 'small', and Extended FABs do not have different sizes.
- **label** (string) - Optional - The text to display on the FAB. Defaults to an empty string.
- **lowered** (boolean) - Optional - Lowers the FAB's elevation. Defaults to false.

### Methods

#### getRenderClasses

- **Description**: Returns render classes for the component.
- **Parameters**: None
- **Returns**: `{ primary: boolean; secondary: boolean; tertiary: boolean; small: boolean; lowered: boolean; large: boolean; extended: boolean; }`
```

--------------------------------

### Text Field Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Events emitted by the text field component.

```APIDOC
## select Event

### Description
The native `select` event on `<input>`.

### Type
`Event`

### Bubbles
Yes

### Composed
No

### Example
```javascript
textField.addEventListener('select', (event) => {
  console.log('Text selected:', event);
});
```

## change Event

### Description
The native `change` event on `<input>`.

### Type
`Event`

### Bubbles
Yes

### Composed
No

### Example
```javascript
textField.addEventListener('change', (event) => {
  console.log('Value changed:', event.target.value);
});
```

## input Event

### Description
The native `input` event on `<input>`.

### Type
`InputEvent`

### Bubbles
Yes

### Composed
Yes

### Example
```javascript
textField.addEventListener('input', (event) => {
  console.log('Input event:', event.data);
});
```
```

--------------------------------

### Filled Tonal Icon Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon-button.md

Filled tonal icon buttons offer a middle ground in emphasis between filled and outlined buttons. Use for secondary actions requiring more emphasis than an outline but less than a filled button.

```html
<md-filled-tonal-icon-button>
  <md-icon>check</md-icon>
</md-filled-tonal-icon-button>
```

--------------------------------

### Text Field with Trailing Error Icon and Error Text

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Displays an outlined text field with a trailing error icon and helper error text to indicate an invalid state.

```html
<md-outlined-text-field
    label="Username"
    error
    error-text="Username not available">
  <md-icon slot="trailing-icon">error</md-icon>
</md-outlined-text-field>
```

--------------------------------

### Assert Argument Not Type with Sass Ext

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

The `assert.not-type` function asserts that an argument does not match a specified type. It returns the argument if it does not match, otherwise it throws an error. Useful for ensuring arguments are not of certain types.

```scss
@function get-strict($map, $key) {
  @return assert.not-type(
    map.get($map, $key),
    'null',
    $message: 'Key must be in the map'
  );
}
```

--------------------------------

### Add aria-label to Button

Source: https://github.com/material-components/material-web/blob/main/docs/components/button.md

Use the aria-label attribute to provide a more descriptive label for buttons when the visible text is insufficient.

```html
<md-elevated-button aria-label="Add a new contact">Add</md-elevated-button>
```

--------------------------------

### Replace suffix at end of string with `string_ext.replace-end`

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `string_ext.replace-end` to replace a specific suffix at the end of a string with a new value. This is helpful for modifying string endings, such as removing or changing closing characters.

```scss
@debug string_ext.replace-end('var(--foo)', ')', ''); // "var(--foo"
```

--------------------------------

### Form with Constraint Validation

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Use constraint validation for text fields within a form. Fields will validate on submission or when `reportValidity()` is called.

```html
<form>
  <md-filled-text-field
      name="name"
      label="Name"
      required>
  </md-filled-text-field>
  <md-filled-text-field
      name="email"
      label="Email"
      pattern="[\w\d-]+"
      suffix-text="@gmail.com">
  </md-filled-text-field>
</form>
```

--------------------------------

### Text Field Events

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Events emitted by the text field component, indicating user interactions or state changes.

```APIDOC
## Text Field Events

### Description
Events that can be dispatched by the text field component.

### Events
#### Path Parameters
- **select** (Event) - Bubbles: Yes, Composed: No - The native `select` event on `<input>`.
- **change** (Event) - Bubbles: Yes, Composed: No - The native `change` event on `<input>`.
- **input** (InputEvent) - Bubbles: Yes, Composed: Yes - The native `input` event on `<input>`.
```

--------------------------------

### Dialog Close Event Listener

Source: https://github.com/material-components/material-web/blob/main/docs/components/dialog.md

Listen for the 'close' event on the dialog to determine which button was clicked. The 'returnValue' property will indicate the value of the button that closed the dialog.

```javascript
dialog.addEventListener('close', () => {
  const cancelClicked = dialog.returnValue === 'cancel';
  const okClicked = dialog.returnValue === 'ok';
});
```

--------------------------------

### Inward Focus Ring Animation

Source: https://github.com/material-components/material-web/blob/main/docs/components/focus-ring.md

Apply the `inward` attribute to make the focus ring animate inwards. This is suitable for components like list items. The element must be within a `position: relative` container.

```html
<button style="position: relative">
  <md-focus-ring inward style="--md-focus-ring-shape: 8px"></md-focus-ring>
</button>
```

--------------------------------

### Switch DOM Structure

Source: https://github.com/material-components/material-web/blob/main/switch/internal/README.md

This is the outermost button element for the switch component. It contains the track and the input checkbox. It is intentionally left without a border to simplify focus-ring application.

```html
<button class="switch">
  <div class="track">
    ::before
    <div class="handle">
      ::before
      <div class="icons">
      </div>
    </div>
  </div>
  <input type="checkbox" aria-hidden="true">
</button>
```

--------------------------------

### Deep Set Fallback for Custom Property

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `deep-set-fallback` to set the fallback value of a nested custom property. This is useful when the fallback itself is another `var()` string.

```scss
$var: var(--foo, var(--bar, 8px));
$new-var: var.deep-set-fallback($var, 16px);
@debug $new-var; // "var(--foo, var(--bar, 16px))"
```

--------------------------------

### Extract variable name from var() string

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `var.name` to extract the variable name from a CSS `var()` string. This function throws an error if the input is not a valid `var()` string.

```scss
var.name($var) //=> string

/// @param {string} $var - A custom property `var()` string.
/// @return {string} The custom property variable name.
/// @throw If the value is not a custom property.
```

```scss
$var: var(--foo);
@debug var.name($var); // "--foo"
```

--------------------------------

### Apply Fill Variation to Material Symbols

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

Set the `font-variation-settings` CSS property to `'FILL' 1` to apply a filled style to Material Symbols icons. This is useful for indicating states or emphasis.

```css
font-variation-settings: 'FILL' 1;
```

--------------------------------

### Text Field Accessibility with aria-label

Source: https://github.com/material-components/material-web/blob/main/docs/components/text-field.md

Use the aria-label attribute for text fields with external labels or when a more descriptive label is needed for accessibility.

```html
<div>Username</div>
<md-filled-text-field aria-label="Username"></md-filled-text-field>

<md-filled-text-field label="First" aria-label="First name"></md-filled-text-field>
```

--------------------------------

### Set Custom Font for Rounded Icons

Source: https://github.com/material-components/material-web/blob/main/docs/components/icon.md

To use rounded icons, set the CSS custom property `--md-icon-font` to 'Material Symbols Rounded'. This applies the rounded style to Material icons.

```css
--md-icon-font: 'Material Symbols Rounded';
```

--------------------------------

### Prevent Scrollbars in Dropdown Menus

Source: https://github.com/material-components/material-web/blob/main/docs/components/figures/select/usage.html

Apply this CSS to the main element of a dropdown menu to prevent scrollbars from appearing when the menu is open. It also resets page-specific list item shape styles.

```css
main > .figure-wrapper {
  /* prevent scrolling bars on dropdown menu open */
  overflow: visible;
  /* unset page styles */
  --md-list-item-container-shape: 0px;
}
```

--------------------------------

### Check if Value is Custom Property

Source: https://github.com/material-components/material-web/blob/main/docs/sass/sass-ext.md

Use `is-var` to determine if a given value is a Sass custom property string. This function returns a boolean.

```scss
$var: var(--foo);
@debug var.is-var($var); // true
```

=== COMPLETE CONTENT === This response contains all available snippets from this library. No additional content exists. Do not make further requests.